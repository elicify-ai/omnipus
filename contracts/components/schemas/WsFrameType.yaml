title: WsFrameType
description: >
  Discriminator enum of every WebSocket frame type string. The value in the
  `type` field of every frame must be one of these strings.
type: string
enum:
  # ── Client → server ─────────────────────────────────────────────────────────
  - auth
  - message
  - cancel
  - ping
  - attach_session
  - device_pairing_response
  - session_close
  # ── Server → client ─────────────────────────────────────────────────────────
  - session_started
  - token
  - done
  - error
  - tool_call_start
  - tool_call_result
  - tool_result_projection
  - subagent_start
  - subagent_end
  - task_status_changed
  - replay_message
  - rate_limit
  - media
  - agent_switched
  - tool_approval_required
  - session_state
  - system_overload
  - replay_warning
  - cancel_stage
  - pong
  # ── Server → client (internal / ack) ────────────────────────────────────────
  - session_close_ack
  - device_pairing_request
