# Textarea

Textarea is the native multi-line form primitive. It preserves native IDs, names, controlled or uncontrolled values, read-only, required, disabled and invalid semantics. It uses the dark library theme and leaves resize policy available through caller classes.
