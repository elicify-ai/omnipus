# Input

Input is the native single-line form primitive. It preserves native IDs, names, controlled or uncontrolled values, read-only, required, disabled and invalid semantics. It uses the dark library theme, a 44px coarse-pointer height and compact pointer height without owning form state.
