# Empty State

`EmptyState` preserves the existing centered icon-and-message geometry. The icon is decorative; the message owns the meaning. Its optional action is a type-enforced pair: `actionLabel` and `onAction` must both be supplied or both omitted. The caller owns empty-state detection and action effects.
