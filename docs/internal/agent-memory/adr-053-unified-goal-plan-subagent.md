---
name: adr-053-unified-goal-plan-subagent
description: "ADR-053 (Accepted, grill PASS) unified goal/plan/subagent delivery — Phase 0 done, building Phase 1+ on feature/plan-swimlane-board"
metadata: 
  node_type: memory
  type: project
  originSessionId: c5168a99-b178-4ff7-aeff-ee84342fadd4
  modified: 2026-07-31T11:19:09.573Z
---

Delivering the v2.2 unified goal/plan/subagent system per the delivery brief `docs/internal/design/unified-goal-plan-subagent-DELIVERY-GOAL.md` (operator /goal 2026-07-22: "all DoDs, follow process esp. QA, parallel worktree teams"). **REVIEW-MODEL DECISION (operator 2026-07-22): running on glm is fine — the 7-reviewer + final 14-reviewer gates run on glm (general-purpose/glm-5.2) and STAND; NO opus is owed, no Phase-3 opus re-run.** Successor to [[adr-052-autonomous-plan-execution]]. Branch = `feature/plan-swimlane-board` (integrate continuously, land once via ONE human-gated → main PR, never admin-merge). Model policy: dev waves = sonnet, ALL review gates = opus. Worktrees on `/home/dev/worktrees/<agent>` (never /tmp).

Design (the what) = `docs/internal/design/unified-goal-plan-subagent-target-design-v2.2.html` (13 sections, 8 diagrams t0/t1/t2/t3/g4/g5/g6/g7, G-1..G-16 acceptance). Delivery contract (how/order/proof) = the DELIVERY-GOAL.md. Locked decisions D1-D17 + ~46 folded findings; unifying thesis = ONE goal core, THREE bindings (chat goal / standalone task / plan DoD); S1-S6 build-once spine (unified goal record · durable session record+8-state enum · SessionMessage envelope family · owner↔Judge↔messaging↔plan-engine interlock S4=highest-risk · budget triple · claim/marker family).

PHASE 0 COMPLETE (2026-07-22):
- **F2 round-burn fix SHIPPED** (commit `02171db1`): awaiting-owner-correction gate in `pkg/agent/plan_engine.go` — in-memory `lastUnmetTerminalSignature` map (mu-guarded, no wire change); gate placed AFTER exhaustion ceiling check; cleared on approved→running. Regression test fails pre-fix (6 rounds/5 idle ticks), passes post-fix (1 + reopen). G-9. **Worker go-test GREEN at 23355b3c** (ALL GATES GREEN; pkg/agent -p4 contention flake passed isolated = known pre-existing SessionManager-singleton race, not F2).
- **go-git spike = GO** (report `/home/dev/worktrees/gogit-spike/GOGIT-SPIKE-REPORT.md` + scratchpad copy): +4.40 MiB raw / +3.04 MiB stripped (under 10MB), no cgo. 3 caveats fold into Phase 1 git layer: (1) media bloats ~1:1 in .git → app-level retention/size-guard (OBS-3); (2) NO `worktree add`, merge is FF-ONLY → isolation ladder's go-git-clone rung isolates writes but joins assemble via shard/subdir not 3-way merge; (3) D17 deny-by-op needs a Landlock/bash-policy `.git/` block (real git CLI can --amend a go-git repo) = security-lead dependency. NOTICE: adopting go-git needs a new NOTICE file (skeema/knownhosts).
- **ADR-053 ACCEPTED** (commit `23355b3c`): `docs/internal/architecture/ADR-053-unified-goal-plan-subagent.md`. Grill: REVISE (0C/3MAJ/3MIN, F-1..F-6) → all fixed → re-grill PASS (0/0/0). Supersede-in-part notes applied to ADR-049 (D4 owner-wake + D7 round-accounting + FR-5/D6 cadence/cardinality) and ADR-052 (chat-goal after-every-turn → claim-or-idle). §8 has 8+3 residual under-specs routed to plan-spec (D9 no-runtime-net, D2 answer-vs-escalate, D12 budget mechanics, git no-go semantics, D1 context snapshot, needs_input reconstructability, D6 latency, S4 state machine, round-accounting, enum crosswalk, multi-goal cardinality).

PHASE 0 COMPLETE + CI-CONFIRMED (2026-07-22). Epic branch feature/plan-swimlane-board pushed through cedf1825. Commits: F2 02171db1, ADR 23355b3c, spec ac797cc3, §6 contracts cedf1825. Worker gates GREEN: go-test @23355b3c (F2), contracts @cedf1825 (verify-contracts exit 0). SPEC = docs/internal/specs/unified-goal-plan-subagent-spec.md (17 US / 78 FR phase-tagged / 74 BDD / 61 tests / S4 INV-1..9): plan-spec → grill FAIL (C1+8 MAJOR) → revise → REVISE (M-1) → revise → lead-verified clean (2 full grills + targeted verify; committed). §6 CONTRACTS landed: 39 new schemas + 4 inline oneOf (SessionMessage 12 variants, DelegateActionRequest 9 actions, MessageParentRequest 6 kinds, DelegateInboxResponse) + 6 extended (Plan/Task/GoalStatusFrame/PlanCreateRequest), generated Go/TS/Zod, no dup (DoD-11), no hand-written wire types. GoalIndicator minimal 8-state compat (full FE-1 = Phase 2).

PHASE 1 SUBSTRATE LAUNCHED (Wave 1, 4 parallel worktree agents, sonnet, /home/dev/worktrees/w1-*): w1-git (pkg/gitevidence: go-git work/ init, write-set-scoped boundary commits, per-attempt diff, size guard, isolation ladder ff-only), w1-gitsec (pkg/sandbox: .git Landlock block per spike caveat3, D17 op-deny, MIN-5 secrets scan, MIN-6 nested-repo, NOTICE), w1-lint (pkg/plan: write-set disjointness reject-at-approve, join-member required, conflict→correction event G-16), w1-xport (BIGGEST: SessionMessage over bus reusing steering.go+async_notifier, durable 8-state session record, delegate run|status→9 actions, message_parent, D15 ceiling, D16 inbox, D1 snapshot, needs_input TTL). Wave 2 = w2-boot (boot-sweep, depends on xport durable record + git checkpoint; persists last_unmet_terminal_signature + plan_phase=awaiting_owner_correction + owner_session_id per C1 → closes standalone-F2 restart gap). Then 7-reviewer opus gate + fix wave + §9 CI + g4/g6/boot conformance. Wave plan: scratchpad/phase1-substrate-wave-plan.md. Disk ~74%.

WAVE 1 DONE + 7-REVIEWER PARTIAL: all 4 substrate components + seams integrated (8f3797e8, build+verify-contracts green, pushed). 7-reviewer opus gate ran: 2/7 COMPLETED with findings (silent-failure: MAJ1 delegate cancel fail-open on Load err, MAJ2 respond fail-open on Drain err, MAJ3 Commit fail-open secrets-no-scanner, MINOR4-7; type-design: MAJ1 OwnerScopeKind domain vs wire value divergence parent_session_id vs parent_session, MINOR1-5). 5/7 reviewers KILLED by Anthropic WEEKLY QUOTA LIMIT (resets Jul 26 5pm UTC) — also killed W2-boot agent (clean, no salvage). Worker go-test @8f3797e8 RED: 6 real failures (my regressions) — 5 fixed+committed 513ce0b3 (4 plan-tool-wiring tests pass rationale on create_plan; defaults_test count 82->83 for message_parent catalog); 6th = TestAgentLoop_EmitsContextCompressEventOnRetry proactive_budget vs retry (A1 root-causing — hypothesis: message_parent tool grew per-turn payload -> proactive threshold trips).

API-LIMIT ADAPTATION (operator redirect 2026-07-22: "continue multi-wave subagents, don't solo"): Anthropic quota exhausted -> opus/sonnet/haiku/fable agents fail. Dev waves now run on glm-5.2 via subagent_type: general-purpose (inherits session model, routes Z.ai not Anthropic) — TEMPORARY DEVIATION from CLAUDE.md "sonnet dev/opus review" policy; opus review gates re-run in Phase 3 when quota resets. A1 (fix tests+compress, pkg/agent+config) + A2 (fix 4 MAJORs, pkg/tools+session+gitevidence) launched general-purpose/glm-5.2, disjoint write-sets. PHASE 1 SUBSTRATE COMPLETE + INTEGRATED (2026-07-22, epic pushed through 819706e8). All on feature/plan-swimlane-board via parallel subagent waves (glm-5.2/general-purpose — Anthropic weekly quota exhausted until Jul 26, deviating from sonnet/opus policy; opus review gates deferred to Phase 3).
- Commits: substrate 7e1c31f5/6e852041/0c89cf72/92261522 + seams 8f3797e8; fix wave A1 1dabf90a (compress test root-cause: message_parent catalog grew tool-def tokens past proactive threshold) + A2 8c387601 (4 MAJOR: delegate cancel/respond fail-closed, Commit refuse-no-scanner, OwnerScopeKind align); Fix-D 28591965 (disjoint docs: gitguard -c parse, pathIsUnder, secretscan/loop/commit docs); W2-boot 29fefeb9 (boot_sweep.go + durable C1: PhaseAwaitingOwnerCorrection+LastUnmetTerminalSignature+OwnerSessionID persisted, bootReconcile rehydrates gate -> no re-judge across restart, 2 exemptions, intent_log.go FR-148/M4, N-15); Fix-B 819706e8 (Mutate RMW primitive on LifecycleStore closes cancel-vs-complete + parkNeedsInput races S4 INV-3, Drain cursor fix, 3P-respond->cancelled, inbox/peek fail-closed, egress on path fields, strict OwnerScopeKind).
- 7-REVIEWER GATE COMPLETE: 0 BLOCKERs, architecture COHERENT (BOM anti-parallel held, seams correct, no import cycle), all MAJORs fixed + verified. Worker go-test GREEN at 8c387601 (pre-W2/Fix-D/Fix-B); scoped + race tests green on integrated tree. **Worker go-test full-suite on 819706e8 = ALL GATES GREEN (§9 gate MET — one pkg/agent load-contention FLAKE passed isolated, known SessionManager-singleton baseline, not this work). PHASE 1 SUBSTRATE CLOSED.**
- Findings log: scratchpad/phase1-substrate-review-findings.md. Phase-2 wave plan: scratchpad/phase2-behaviors-wave-plan.md. DEFERRED (Phase 2, sound): owner-loop WRITE path to intent-log, session.failed->plan re-judge event-bus coupling, deriveQuestionAuthority content-upgrade (FR-139), live kill-switches/retention sweeps (FR-197), FR-126 needs_input TTL auto-handback consumer, R§8.6 clause-4 snapshot byte-count, true D1 transcript isolation. Architecture note: S2/S3 plane is built-but-INERT until Phase-2 boot-wires stores + SessionMessageChan consumer atomically.

PHASE 2 IN PROGRESS (2026-07-22). Epic pushed through c62705d7.
- Wave 1 INTEGRATED (parallel, glm-5.2): 538b37de boot-wire (S2/S3 plane LIVE: gateway MessageInboxStore+SetSessionMessagingStores+consumer; session_messaging_wire.go re-wires delegate+message_parent per-agent, injects Waker+egress filter; kind-router consumer drains SessionMessageChan; config/session_messaging.go 21 keys + live kill switch; g7 round-trip test green) + c62705d7 goal-compile/budget (goal_compile.go compileGoalIntent engine-invoked schema-validated + co-located marker parser + feasibility gate D9 + classifyNonVerdict FR-137 unable_to_verify/criterion_unjudgeable; budget.go TokenBudget ONE atomic pool agent-agnostic no-IsPrivilegedAgent-exemption graceful-wind-down-at-boundary cap0=unbounded brake failed(budget_exhausted); echo-confirm D11 + amendment diff N-6 + /goal clear N-12; 12 tests incl 100-goroutine race). Cherry-pick auto-merged (loop.go touched by both, diff sections). gofmt+no-conflict-markers clean.
- PENDING: worker go-test full-suite on c62705d7 (§9 gate for Wave 1, task bqzgx91zv).
- **Wave-1 worker gate GREEN @ c62705d7.** Wave 2 behavior agents landed: owner-loop 9727710c (persistent owner session + 3 correction verbs via intent-log WRITE + Play-from-commit), triggers ddc76e21 (claim-or-idle/idle/pause/bounce + per-goal-id cardinality F5), judge 1f682921 (blocked-check honesty via classifyNonVerdict + real Repo.AttemptDiff + MAJ-13). **Phase-2 backend behaviors worker gate GREEN @ 1f682921 (ALL GATES GREEN).** TRACKED FLAKE: TestDurableC1_RestartChangedStateReJudges fails under -p4 contention, passes isolated (W2-boot test; pkg/agent SessionManager load-contention pattern) — stabilize if it recurs.
- Frontend wave IN FLIGHT (parallel, glm-5.2): FE-A (FE-1 pill+FE-8 confirm+FE-3 question), FE-B (FE-2 plan->Graph+FE-4 no-per-member-controls), FE-C (FE-5 Agent-View+FE-7 sanitization), FE-D (FE-6 Usage budget). src/ disjoint, builds on landed §6 wire types.

PHASE 2 FUNCTIONALLY COMPLETE (2026-07-22, epic pushed 0c1ea317). Both gates GREEN: worker go-test @1f682921 (backend behaviors) + full vitest 5881 passed 0 failed @0c1ea317 (frontend) + typecheck real exit 0. Phase-2 diff 819706e8..0c1ea317 = 70 files +10619. Frontend commits: 4684510d FE-6, 4b97e0a3 FE-2/4, ead7c748 FE-1/8/3, 0c01611c FE-5/7, 5839eb16 test-cache-prime fix (pre-existing ReactQuery race, NOT FE regression — bisected to baseline), 0c1ea317 tabIndex fix. ENV FIX: /home/dev/omnipus3/node_modules was a broken self-symlink (ELOOP) clobbered by an FE worktree ln — re-pointed to /home/dev/omnipus4/node_modules (real install); .gitignore `node_modules/` doesn't match a symlink so FE worktrees' node_modules symlinks must NOT be git-added (use `git add src/` only).
PHASE-2 REVIEW + FIX WAVE (2026-07-23): 7-reviewer gate ran (glm; pr-review-toolkit:review-pr is the sanctioned reviewer for FUTURE reviews per operator). Findings fixed + integrated through 91859e0d: B1 BLOCKER (planCannotProgress nil-safety, 78fa7f0a); Fix-1 silent-M1 budget brake @processPlan / silent-M2 kind:error / silent-M3 round-advance abort / arch-M2 kill-switch on sync paths; Fix-2 arch-M1 /settings/token-budget handler + arch-m1 removed inert 2nd budget key; FU-A corr-MAJOR-2 idleSettling-wedge-clear / corr-MAJOR-3 claim F5-race (CAS Register, ErrVerifierSessionHeld) + idle-path budget brake + bare-claim M3 sibling + UNBLOCKED -race on pkg/agent (moved testEventTimeout/testEventPoll off the !cgo file — the long-standing -race build break is FIXED); FU-B corr-MAJOR-4 Play-from-commit wired (gitevidence.LastCommitForTask + LastMemberCommitResolver + boot + ResumeFromCommit on task record; full per-member worktree checkout deferred to D10 rung); FU-C sec-MAJOR-3 consumer steer parent-of gate (producer already had verifyCallerOwnsSession) + sec-MINOR-1 sink-side N-10 egress. 2 WORKER TEST FAILURES @9fc0acab being stabilized (agent, p2-fix-tests2): M3 test rigs defeated by ROOT (chmod 0o500 doesn't block root → SetMeta succeeds → invariant fails) — fix = FS_IMMUTABLE_FL ladder (mirror pkg/gateway/rest_plan_stop_immutable_linux_test.go); + TestConcurrentSessions_TwoSessions_TimingProof (assess flaky vs FU-A CAS regression). TRACKED Phase-3: sec-MAJOR-2 (AppendCorrection owner-auth — add before any caller is wired; zero live callers today, safe). Findings log: scratchpad/phase2-review-findings.md.
PHASE 3 STATUS (2026-07-23, epic HEAD 75ab6eb2, pushed): 
- DONE: §9.1 conformance tests (g5/g6/g7 authored, t0/t1/t2/t3/g4/§5-boot mapped to existing tests, b734f883); docs sync DoD-9 (v2.2 "Implemented" banner, AS-IS §11 evidence-cited, e3284afa); 4 deferral issues #536-539 (owner-auth, D10 worktree checkout, SetCap, intent-log tamper); SPA embed synced locally to pkg/gateway/spa (gitignored build artifact, verified goal-pill+token-budget present, NOT committed — CI rebuilds); whole-epic pr-review-toolkit sign-off (code-reviewer+silent-failure-hunter+pr-test-analyzer, 0082f4d1..e3284afa) — no BLOCKER, found 1 MAJOR dual-budget (SEC-26 USD cap still live parallel to token budget, different IsPrivilegedAgent exemption — tracked #540, operator decision needed) + budget-corruption safety gap (FIXED) + traceability matrix phantom names (FIXED, 28 reconciled) + FR-178/FR-130 missing tests (FIXED) + boot-sweep hook-panic-not-logged (FIXED) + 2 more deferred (#541 9 real-LLM E2E shards TDD41-49, §9.1 full-path for 6 diagrams — issue creation partially failed, re-create if missing); final fix wave committed 049f5924.
- CANCEL-CASCADE E2E INVESTIGATION (75ab6eb2): worker e2e gate found llm-agents shard FAIL on T24b (sync-delegate cancel cascade, stop-btn stuck visible). Investigated via protocol (4 hypotheses: delegate-cancel-fail-closed REJECTED, boot-wiring-miswired REJECTED, CAS-Register-blocks-cancel REJECTED, CONFIRMED = pre-existing cancel.go escalation ladder graceful->3s hard-abort->+5s detach collides with T24b's 5s assertion window under llm-agents parallel-CI load, same flake class as ConcurrentSessions/FiveSessions already fixed this wave). NOT an ADR-053 regression. Fix: widened 5s->10s assertion + added TestRepro_SyncDelegateCancel_RequestCancel (permanent Go regression guard proving the REAL RequestCancel->sync spawnSubTurn cascade unblocks in ~3.03s via the production path).
- PENDING: worker go-test on 75ab6eb2 (task b06qwc1pk, running) to confirm final-fix-wave + cancel-fix compile/pass together; THEN worker e2e re-run to confirm T24b green with the widened window; THEN the single human-gated -> main PR (cite ADR-053 grill PASS, delivery brief, deferral issues #536-541+, NEVER admin-merge — human must approve regardless of CI).

NEXT: worker green -> dispatch triggers/judge/owner-loop + frontend -> Phase-2 gate -> Phase 3 (14-reviewer opus once quota resets Jul 26 + §9.1 conformance + live E2E + docs + -> main PR).

WAVE 1 PROGRESS (2026-07-22): §6-regen broke Go build (gen.Cleared→gen.ClearAllSessionsResponseStatusCleared, gen.StoppedByUser→gen.TaskCancelReasonStoppedByUser) — contracts gate only runs verify-contracts NOT go-build, go-test ran pre-regen → slipped through; FIXED 4d46e4ce. 3/4 Wave-1 components INTEGRATED (cherry-picked, build green, pushed 0c89cf72): 7e1c31f5 gitevidence (Open/Commit/Diff/CheckIntegrity/SelectRung; go-git v5.19.1; KEY: go-git PlainClone ALSO shells to system git → no-system-git degrades to subdir not clone), 6e852041 gitsec (D17 CheckGitOp + .git exec-block; KEY: Landlock UNAVAILABLE on this Fly kernel AND can't subtract sub-grants → .git block is exec-chokepoint fail-closed, LIM-02 obfuscated-shell residual documented; ProtectRepoWorkdir/CheckGitOp/audit.SecretScanner API), 0c89cf72 plan-lint (plan.Lint DAG-topological reject-at-approve, CorrectionEvent payload, Plan.Rationale, Task.WriteSet/Stream/IsJoin). W1-xport (session transport, BIGGEST) STILL BUILDING.

SEAMS TO RECONCILE after xport lands (fix wave): (1) W1-lint gap — create_task/update_task/rest_tasks NOT wired to set write_set/stream/is_join (member tasks can't receive write-sets from agents yet); (2) W1-git — migrate 4 os.MkdirAll sites (pkg/agent/workspace_reroot.go, loop_env.go, loop.go, pkg/gateway/rest_executor_smoketest.go) → workspace.EnsureWorkDir so git auto-init fires; (3) wire gitsec ProtectRepoWorkdir into gitevidence.Open + audit.SecretScanner into the commit path; (4) NOTICE added by gitsec; verify gofmt+verify-contracts on integrated whole. THEN 7-reviewer opus gate on full substrate diff (0c89cf72+xport vs cedf1825) → fix wave → §9 worker CI (go-test/vet/gofmt/lint/vulncheck) → g4/g6/boot conformance. W2-boot after.

(historical) SPEC grill FAIL C1 detail: C1 = KEY: shipped F2 in-memory signature has a RESTART GAP (re-judges once on boot) + awaiting-owner-correction not durable (not in S2 8-state enum) → boot sweep would sweep the legitimately-waiting owner session to failed(interrupted). Directed fix (durable version of F2, lands in this system): persist last_unmet_terminal_signature + awaiting_owner_correction on the PLAN record (keep S2 session enum at 8; owner-while-waiting = durable `paused`); boot sweep exempts awaiting-correction owner session + doesn't re-judge unchanged state; reconcile FR-118/G-13 vs INV-7/9. Other MAJORs: M1 classify unable-to-verify(re-run) vs unjudgeable(unmet+escalate) by verifier-turn outcome; M2 remediation=re-statement amendment, else honest failed(judge_rounds_exhausted); M3 authority tag runtime-derived fail-closed default owner_required; M4 atomic tail append via single plan-DAG-doc rewrite or intent-log; M5 SC-008 bounded-overshoot not never-negative; M6 worktree→highest-available-rung; M7 add BDD for 7 placeholder FRs; M8 add `engine` SessionMessage direction.

NEXT (after spec re-grill PASS): `/plan-spec` ADR-053 (resolve §8 residuals, detail §6 contract surface) → land §6 contract set contract-first (mostly a DELTA — most goal/plan/judge wire types already exist from ADR-052; new = SessionMessage inline oneOf, durable session 8-state enum, revision entry, mid-span subagent frames, write_sets+rationale, budget/pill enums, cancel/restart; decomposition in scratchpad/phase0-contracts-decomposition.md) → Phase 1 substrate (4 parallel worktree waves: git evidence · session-transport+records+tools+corrected delegate action set §5.1 · boot-sweep · plan-lint) → Phase 2 behaviors → Phase 3 conformance (§9.1 diagram gate + 14-reviewer + full CI + live E2E) → → main PR. Task tracking = harness tasks #1-7.

**LIVE UAT (2026-07-31, post-Phase-3) FOUND 12 REAL BUGS the review gates missed** — despite 7-reviewer + 14-reviewer sign-off and full CI green, an actual live agentic UAT of the delegation tools (message_parent/cancel/follow_up/steer/create_task+plan_id/update_task/bash-guard) on `omnipus-uat-swimlane` surfaced functional breakage the static review process never caught, because these are all runtime wiring/ID-namespace/schema-mismatch bugs invisible to code review and passing unit tests (mocks matched the bug, not reality). Two UAT rounds (one large single-session run that broke the chat UI, one batched 9-session re-run) independently confirmed the same defects. All root-caused via direct code inspection and filed:
- **P0** #576 `message_parent` always fails (wrong context-key accessor, `message_parent.go:381`), #577 `delegate cancel` has zero real effect (ID-namespace mismatch between `sessionKey` and `transcriptSessionID`, `session_messaging_wire.go:156`)
- **P1** #578 `create_task(plan_id=...)` always fails (plan store never wired for this one tool, `loop.go`), #579 `follow_up` drops the new instruction (reads wrong arg field), #580 `timeout_seconds` never parsed at all in `delegate.go`
- **P2** #581 steer-on-terminal TOCTOU race, #582 bash guard blanket-blocks any `$(...)` substitution, #584 `update_task` missing creator-authorization (sibling `delete_task` has it), #586 **Stop generation doesn't reliably kill a runaway backend turn** (confirmed via server logs, 26+ min post-Stop activity, causes cross-session notification bleed — arguably higher priority than the UI-freeze bug itself since it's the operator's only kill-switch for a runaway turn), #587 `delegate` accepts empty `agent_id`
- **P3** #583 `list_jobs actionable` resolver never wired, #588 bundle (3 minor messaging/schema gaps)
- Also: **chat UI freeze bug filed as #573** (P0, unmemoized/unbounded subagent-span render on every WS frame, `ChatScreen.tsx`/`chat.ts`) — batching into smaller sessions is a confirmed-working interim mitigation for the freeze specifically, but does NOT fix #586 (a confused model can still balloon a single turn to millions of tokens regardless of planned batch size).
- 5 UAT findings confirmed as correct by-design (Worker leaf-agent no-delegation, create_plan consent-gating, acceptance-criteria force-complete block) — bundled into docs-only issue #585.
- Full evidence trail: `docs/internal/uat/uat-root-cause-analysis-2026-07-31.md` (first round) and `uat-report-batched-rerun-2026-07-31.md` (second round). **None of these 12 bugs are fixed yet** — this is root-cause + tracking only. Also found (infra, not code): `omnipus-uat-swimlane` has no persistent Fly volume, so ANY machine restart (not just redeploy) wipes all runtime state — see [[fly-restart-wipes-volumeless-state]].
- **Implication for "is this epic done": no.** The epic's own core feature surface (delegation tool contracts) has multiple P0/P1 functional breaks in production-shaped use, discovered only by live agentic testing, not by the review/CI process this epic otherwise trusted. Recommend treating a live UAT pass as a mandatory gate before any future "Phase 3 complete" claim on this or similar epics, not just static review + unit tests.
