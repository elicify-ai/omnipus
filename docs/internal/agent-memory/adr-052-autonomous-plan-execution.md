---
name: adr-052-autonomous-plan-execution
description: "ADR-052 (Accepted) + spec for autonomous agent plan execution — all decisions locked 2026-07-21, awaiting implementation go"
metadata: 
  node_type: memory
  type: project
  originSessionId: b7a78e24-b362-40f9-bb84-d430cd72665b
  modified: 2026-07-22T12:08:27.391Z
---

ADR-052 "Autonomous agent plan authoring & execution" — **Accepted (r6)** after 3 grill rounds + operator closing interview (2026-07-21). Spec grilled 2× post-resync, all 40 findings fixed, **zero open questions**. Committed + pushed as `95088e5d` on `feature/plan-swimlane-board` (operator chose current branch over a release/v0.1.1 cut). Files: `docs/internal/architecture/ADR-052-autonomous-agent-plan-execution{,-review}.md`, `docs/internal/specs/autonomous-agent-plan-execution-spec{,-review}.md` (13 US / 41 FR / 14 SC / 27 BDD / 33 tests / 8 DS).

Interview-locked decisions (do NOT re-litigate):
- **No approval gate**; tool policy IS the gate. Jim `allow` for create_plan/execute_plan/run_task; **all other agents + global ceiling = explicit `ask`** (approval prompt), never deny.
- **No back-compat**: "we do not have existing installations" — breaking wire changes fine; target = **v0.1.1 release line** (NOT v0.3; "accelerate features into this version").
- **Soul/Rubric FULL unification**: delete `AgentConfig.Rubric`; one soul concept + editability flag (contract change in scope).
- **Judge = real agent, verifier role, OWN session** (not sub-turn, not the old `Provider.Chat` shortcut): memory-off, read_file/list_directory + scoped `inspect_session`, auto-fed last-**20k**-token window of the working session, tools only as rubric-gated fallback; three-rung ladder (machine-check / new `behavior` criteria kind / subjective); evidence-marker gate; fail-closed. Root motivation: observed blind-judge failure ("run 5 web searches" falsely unmet).
- **Stop** = chat cancel fanned out (planDecisionMu) over worker + registered verifier sessions; restart → `approved` (engine promotes under cap); cancelled = failed+stopped_by_user restartable; member-cancel → plan fails stopped_by_user once no progress possible.
- **Direct 3-caller conversion, no spike** — compensated by anti-pattern test suite (DS-8) + **LLM e2e eval tests** (new category, CI-worker e2e gate).

Process notes: operator wants questions **one at a time** (AskUserQuestion, options + recommendation). **NO taskify** (same as the Planning epic). **Model policy (operator 2026-07-21): dev-wave agents = repo defaults (backend/frontend/qa-lead = sonnet); ALL review gates (7-reviewer + final 14) = `model: opus` override.** Worktrees for wave agents MUST live on the workspace drive (`/home/dev/worktrees/<agent>`), never /tmp. Agents don't commit — the lead commits/merges. Related: [[planning-goals-epic]] (ADR-049 — partially superseded by ADR-052).

IMPLEMENTATION PROGRESS (2026-07-21, branch feature/plan-swimlane-board):
- Wave 1 (8 dev agents, worktrees) MERGED: contracts 222f8c04, wave branches merged through 20d25add, seam reconciliation + gateway fixup 4a4b2a97. 7× Opus gate → NOT CLEAN (blocker: verifier-registry key mismatch; memory seed missing; /goal clear cancel unimplemented; empty-output terminality CI red; + type/silent/comment findings).
- FIX WAVE (3 agents A/B/C in main checkout) COMMITTED `4a772291` + pushed: registry key unification (verifierUnitForPlan/ForTask/ForGoal) + seam-crossing test; Judge MemoryEnabled seeded false + re-enforced; /goal clear cancels verifier via registry; empty-claim fails closed pre-dispatch (CI test green); inspect_session ceiling→allow + per-agent deny (effective-resolution test matrix); CancelReason auto-clear on status-leaves-failed; restart guard validates ON-DISK reason; MinCount *int + EffectiveMinCount; execute_plan SD-A7 gate; behaviorFromWire helper; memory_enabled in toWireAgent + PUT. CI go-test GREEN at 4a772291 (one unrelated TestSinceCursor WS-replay flake, retried green).
- WAVE 2 MERGED + PUSHED as 8ea0ba65 (all 8 agents delivered; 3 worktree branches wave2/{rest,sessions-vis,caller-int} + main-checkout SPA/QA; seam reconciliation = NewVerifierSession helper + it.fails Zod-gap canaries). Full vitest 5727 green, typecheck 0, build clean.
- GATE 2 (7× Opus) COMPLETE at 8ea0ba65 → NOT CLEAN. BLOCKERS: (B1) verifier cancel-key drift — registry.Register(unitID, sessionKey) but cancel matches transcriptSessionID(=chatID); fix = register chatID + real seam test; (B2) evidence-gate livelock — [goal:evidence] never taught in buildTaskCompletionInstruction (both branches), steering in t.Result invisible (AttemptCount==0 gate), no rejection bound → infinite free re-dispatch; (B3) Stop on cap-queued approved plan rejected at 3 backend layers while SPA ships the button — ARCHITECT DECISION: backend accepts approved-Stop (plan.go approved→failed edge, StopPlan guard, handler relax). CI go-test RED: partial-failure test chmod defeated by root (fix = FS_IMMUTABLE_FL ladder; probe confirms immutable blocks root on worker overlayfs /tmp); shell-leaf test race (reaper "done" overwrites StopTask "canceled" relabel under package load). MAJORS: cancelled-pill CSS var()+1a invalid on 3 surfaces; creation-side verifier spoof untested; plan-restart handler any-reason member reset untested e2e; handlePlanStop comment false; sessionVisibilityParams header overstates SC-014 gap. Test 32 TestVerifierE2EEval = Wave 3 (confirmed absent). Deferred-with-issue: approve-gate extraction to pkg/plan.
- FIX WAVE 2 COMPLETE — all 8 agents delivered, committed 2224de75 (48 files) + 51d69adb (stale registers-before-dispatch assertion → asserts cancel-matchable session id). Notable: D's real root cause = shared SessionManager singleton + AgentLoop.Close() KillAll() cross-test race (fix: status-priority relabel, specific canceled/killed/timeout beats generic done + t.Parallel() removed); F found+fixed 2 extra soul bugs (3 response paths blanked soul for locked agents; SPA autosave stripped soul from payload); G ground truth: failed→in_progress was ALREADY legal (pinned). **CI go-test ALL GREEN at 51d69adb** (no flake filter engagement). Full vitest 5741 green, 0 todos, 2 canaries.
- WAVE 3 LAUNCHED (3 agents): evals (tests/e2e verifier-eval spec Test 32, 4 cases incl. control, shards.json assignment), embed+live-E2E (npm build → pkg/gateway/spa sync + playwright walkthrough on 127.0.0.1:5000, NEVER :8080), docs+issues (spec/ADR status + 4 verified deviations note, AS-IS accuracy fixes, 2 tracked issues: approve-gate extraction, FR-053 file-diffs).
- WAVE 3 LANDED at c40cd144: verifier-eval spec (4 cases, llm-verifier-eval shard), docs sync (spec=Implemented + 4 deviations; ADR Implementation section; issues #531 approve-gate extraction, #532 FR-053), live-E2E hotfix (createPlan 405 → workspace-scoped route — UI plan creation was silently broken, found ONLY by live browser E2E; Judge updatedAt telemetry; accordion warning). Live-E2E walkthrough otherwise PASS incl. full run→Stop→Cancelled→Play cycle. "Command Center" standalone screen no longer exists (root CLAUDE.md checklist drift, components live in AgentProfile/TaskDetailSlideOver).
- E2E GATE at c40cd144: all pre-existing shards GREEN (143 specs); ONLY new verifier-eval shard red — worker LLM REFUSED to emit crafted deceptive claims (alignment = defense layer 0), so Judge never invoked. Eval redesigned: external-CLI stub worker (subagent_3p, CLIPath exec'd directly, claude-code driver 1-JSON-line protocol) delivers claims verbatim, no LLM asked to lie; control probe moved in-sandbox.
- **PRODUCT BLOCKER found by eval smoke (fix in flight): Judge can NEVER adjudicate on a fresh install** — ADR-046 P1 resolveTurnWorkDirOrRefuse requires core_team workspace membership for every turn; System Agents are deliberately barred from rosters (gateway 400); runVerifierAdjudication threads no workspace → "turn refused: agent is not a member of any workspace {judge}" → D7 pause, all 3 callers. Unit harnesses skip the scan (why tests were green); gateway integration tests had seeded membership fixtures. backend-lead fixing: option (i) System-agent exemption→own home vs (ii) thread work-under-review workspace through JudgeCriteriaInput (tool reach + per-adjudication confinement); must ground in ADR-046/052, fail-on-pre-fix regression test with enforcement ACTIVE, live fresh-install verdict proof.
- BLOCKER FIXED (8a357e4d) + operator redirect "Judge = member of every workspace, keep it simple" → implicit-membership restructure (53da2f07): isImplicitMember in pkg/workspace/find_for_agent.go, uniform resolver, sorted-first fallback. go-test GREEN; e2e ALL 8 SHARDS GREEN incl. llm-verifier-eval (real Judge resisted spoof/injection/leniency, control met). GitHub CI note: PR #527 CLOSED pre-epic — GH Actions has NOT validated ADR-052 commits; worker = only CI authority until a PR is opened (operator decides).
- FINAL 14× OPUS SIGN-OFF at 53da2f07 COMPLETE: 10 CLEAN / 4 NOT CLEAN. 1 BLOCKER: handlePlanRestart 200-on-partial-member-reset-failure (Stop honesty not mirrored). 4 MAJORS: Stop TOCTOU (2× confirmed — verdict outcome writes can revive a Stopped task / record cancelled work done; fix = task-store backstop + UpdateIfStatus CAS + plan-write under lock); PUT sniff false-positives (fix = req.State != nil, also closes unicode bypass); persistent judge-Unavailable invisible (central consecutive-counter → ERROR at N=3); FR-027 documents inspect_session ceiling BACKWARDS (would lure re-breaking the Judge). Security review fully CLEAN. ~25 minors (graph-chip 4th CSS surface, stopPlan route untested, ListView NUL byte, partial_errors discarded, goal-scope sorted-first fallback hardening, comment rot batch, ADR-046/049 cross-refs, eval header stale, shard slot).
- FINAL FIX WAVE LAUNCHED (5 agents, disjoint): P1 REST honesty+sniff (rest_plans*), P2 Stop TOCTOU (task store+executor+engine), P3 judge visibility+goal fallback+comments (verifier_*/behavior_scan/goal_loop), P4 SPA (taskGraph chip, stopPlan test, NUL, partial_errors, derived union), P5 docs (FR-027 note 6, §10 five, ADR-046 P1 note, ADR-049 back-ref, #531/#532 cites, eval header rewrite, shard slot b).
- OPERATOR QUESTION to surface at end: Balanced tool-policy preset leaves execute_plan at allow (defaultPolicy) while gating bash to ask — FR-021 modal is the interlock; intended?
- FINAL FIX WAVE COMPLETE, committed 2d2b9c00 (36 files) + pushed. Lead consolidation: defaults.go comment truth, handleTaskStop 409 twin, #531/#532 verified live. **BOTH WORKER GATES ALL GREEN at 2d2b9c00 (go-test + full e2e incl. verifier eval). EPIC COMPLETE — awaiting operator review/PR decision.**
- LIVE DEPLOYMENT (2026-07-21): fresh install at /home/dev/omnipus-live, binary /home/dev/omnipus-release (built from 2d2b9c00), bound 0.0.0.0:8080, gateway.public_url=https://pod-omnipus.fly.dev, auto-generated master.key (BACK UP). Old omnipus-deploy data untouched. Restart-in-place for updates (keep operator's onboarding data).
- POST-DEPLOY UX FIXES in flight (operator live-testing feedback): eager Judge SOUL.md seed at boot (empty until first adjudication was the lazy design — wrong now that soul is editable); Judge shown in Team tab as non-removable implicit-member row (render-only, never in core_team payload); plan tile click → auto-switch to Graph view (deselect leaves view). After: rebuild + restart live gateway in place.
- OPEN OPERATOR DECISIONS: open the PR (GH CI validates only then); Balanced preset execute_plan=allow (modal is interlock) — intended?
- NEXT: GOAL-LOOP REDESIGN (operator-directed from live /goal test, ADR amendment or ADR-053 BEFORE implementation): (1) persistent goal = first-class artifact (raw + SMART-refined, survives restarts, restated verbatim in every steer); (2) SMART compilation at /goal set — LLM interprets intent → criteria ladder (behavior kind for countables e.g. "5 searches"→tool:search_web min_count:5, prose/machine-check for rest), refined goal echoed to user; (3) trigger = completion-claim (evidence marker) OR idle-settlement with no pending sub-tasks; question-to-user turn → waiting-on-user PAUSE (no verdict, no round burned); (3b) **UNIFIED GOAL MODEL (operator 2026-07-21): chat goal and task goal = SAME datamodel** — prompt + goal definition + acceptance criteria (ALL kinds incl. deterministic machine checks "all tests passing"→exit-code + behavior counts). ONLY difference = authorship: chat criteria compiled BY AN AGENT from user intent; task criteria set explicitly at creation. Same loop, same Judge, same ladder; tasks gain waiting-on-owner pause + idle-settlement trigger; no separate goal storage for tasks (goal lives in the task record). (3c) CHAT CANCEL SEMANTICS: /goal clear cancels ONLY the in-flight verifier + removes the goal (worker keeps running); the chat cancel/stop button is the cascading cancel (worker turn + subagents + shells + verifier). (3d) COMPLETION CLAIM = [goal:evidence] line + TASK_STATUS (task) / GOAL_STATUS: met (chat); IDLE SETTLEMENT = event-driven ~60s debounce, fires ONE adjudication when no turn/subagents/shells/sub-tasks AND not waiting-on-user; re-arms only on new activity. (3e-pre) PLANNING STEP 1 (operator 2026-07-22): inputs = prompt + goal definition + potential REFERENCE DOCUMENTS; plan owner breaks them down into tasks (with or WITHOUT subagent help — research/decomposition delegable) → plan = DAG + per-task criteria + DoD + recorded rationale; planning + re-planning checklists in the sketch. (3e) PLAN CORRECTION (operator 2026-07-22): critic detecting all-tasks-successful-but-goal-unmet must PROMPT the plan owner to replan; (3f) EXECUTE SEMANTICS: ▶ Execute = a task whose worker is the OWNER — starts/attaches the owner agent's session (like a task run); the owner's turn triggers the deterministic engine; same session receives critic feedback + re-planning prompts; Stop cancels it like any session. (3g) CORRECTION FORM (lead recommendation, operator question answered): APPEND-ONLY extension of the SAME plan — after first Execute existing members immutable (done=evidence, failed=history), correction = tail members blocked_by established outcomes + a revision entry (falsified assumption + what the tail adds); NOT a follow-up plan (splits the goal, DoD never judged whole, rounds reset), NOT in-place mutation (destroys evidence under a running engine); scope-change diagnosis → genuinely new plan = user decision. (3h) PARALLEL STREAMS (researched 2026-07-22, documented in sketch §3c): planner CoT = dependency analysis → write-set contention → mergeability class per artifact (text/code=git-mergeable; structured data=shard CSV/JSON + deterministic assemble; opaque binary xlsx/pptx=single-writer OR generate-from-data at join; overlapping write-sets=serialize) → isolation per stream → ALWAYS author the join as a first-class merge/assemble member with own criteria. Infra (layered): per-stream subdirs under work/ = default (zero infra); git worktree per code stream = opt-in (reuse pkg/agent/runner/worktree.go pattern but rooted work/.streams/<task>/, days of work); staging+assemble = default join, only option for non-code; overlayfs REJECTED (privileged). Contention real today: all members share workspaces/<id>/work/ with concurrent dispatch and zero partitioning. (3i) EMBEDDED GO-GIT DECISION (operator 2026-07-22, sketch §3d): pure-Go go-git in the binary = workspace versioning layer — work/ auto-init hidden repo, auto-commit at task/attempt boundaries (task·attempt·agent) → undo/revert-task, per-attempt diff evidence for Judge (closes #532 mechanism), audit trail, branch-per-stream isolation; system git = opt-in acceleration for full 3-way merge/worktrees, else shard/assemble fallback; planner must reason: commit boundaries=undo points, join only what the runtime can execute, criteria name expected diff paths; caveats: gc/retention for binary bloat, nested-repo detection (user repo wins). (3j) RELATED REQUIREMENT (operator 2026-07-22): /home/dev/subagent-session-control-visibility-requirements.md (draft v0.3, authored by Jim in-product) — parent orchestrator mid-flight control of subagent sessions: steer/cancel without progress loss, live visibility (status/blockers/artifacts, no raw monologue), BIDIRECTIONAL typed session-scoped messaging (child→parent questions = the waiting-on-owner pause mechanism!), human attach/peek/reply parity; gaps today: no mid-run inject/pause, no child→parent inbox, chain follow-up = cold restart. MUST be folded into the goal-loop ADR amendment scope — same primitives as owner loop steering + pause. DOCUMENTED as sketch §4 (2026-07-22): ONE typed SessionMessage envelope over MessageBus (contract-first) → 4 consumers (child→parent inbox incl. question(wait=true)=the pause; parent→child steer/respond/cancel-soft at tool boundary; events→goal pill/status; human Agent-View parity). KEY: injection machinery EXISTS (pkg/agent/steering.go per-scope queues at tool boundaries + async_notifier.go terminal-wake precedent) — generalize, don't build new. 7 delegation changes incl. DelegateTaskState→durable session record with needs_input state; depth-3/trust unchanged; steer never increments depth. (3k) UNIFYING THESIS (operator-confirmed 2026-07-22, = amendment's opening statement): ONE goal core, THREE bindings — shared: goal shape, claim discipline, claim-or-idle triggers, question→pause, critic/ladder, steering, typed messaging, cancel cascade, bounds, git evidence; ONLY legitimate differences = (1) the deterministic engine (plan-only composition layer) + (2) UI/datamodel binding (session meta / task record / plan record). Implied unifications: ONE budget concept (rounds=attempts=JudgeRounds), ONE embedded goal-store shape, ONE steering/messaging implementation. Build the core once, bind three times. (3l) GRILL VERDICT on sketch (2026-07-22): FAIL — full report /home/dev/goals-target-review.md; 2 CRITICAL (no crash/restart reconciliation of persisted session lifecycle → boot sweep L-7 needed; git evidence wrong under parallel topology + tamper-able .git in work/ → write-set-scoped serialized commits + integrity check), key MAJORs: L-3 vs warm-resume contradiction, frozen-member reset scope, go-git has NO worktrees (branch-per-stream infeasible embedded-only), needs_input unbounded (TTL+escalation), waiting_on_user needs explicit GOAL_STATUS marker, claimless idle-judging unspecified, human-as-owner missing from spawn-edge model, plan-keyed (not session-keyed) member inbox, 3p children can't speak protocol, inbox DoS, UI injection via child text, LLM-compiled machine checks = command exec (policy+echo-confirm), no acceptance criteria for §1-3d, 12 tunables no config keys. All fixes = normative paragraphs, not redesign. (3m) ARCHITECT VERDICT (2026-07-22): NOT READY as ONE amendment — TWO FALSE CODE BASELINES in sketch: (F1) "Execute=owner session" reverses shipped ADR-049 D4 headless hybrid (Plan has NO SessionID; wakeOwner=one-shot async-notify at 3 decision points) — persistent owner session = biggest NEW build, must explicitly re-open D4 (or alt: correction on the wake seam, owner calls create_task); (F2) engine does NOT auto-reset failed members on unmet DoD — AND LIVE BUG: all-terminal+unmet re-judges UNCHANGED state every ~30s tick burning JudgeRounds until exhausted while owner deliberates → needs "awaiting owner correction" gate (fix regardless!). F4: no GOAL_STATUS marker/gate exists for chat (net-new or idle-only). F5: idle vs FR-045 no-signal-burns-attempt precedence undefined; verifier must count as activity (double-verdict race). F6: session-messaging = substantial new subsystem (childID-keyed queues, shared transcript FR-6a, silent-drop MaxQueueSize=10, in-memory state) — SessionMessage oneOf+discriminator MUST be inline-hosted (ADR-034). F7: go-git → own spike-gated ADR, OUT of amendment; §3c taxonomy = planner guidance doc not engine scope. SPLIT PLAN (architect Q6): ADR-052a trigger fix FIRST (claim-or-idle+pause, small); ADR-052b chat-goal shared model; ADR session-control-plane (companion doc, P0 only); ADR owner-replanning (needs plane + F2 gate + D4 re-open); ADR git layer (spike). Reviews: /home/dev/goals-target-review.md + architect report in task output a-fe2cd0b. (3n) OPERATOR DECISION 2026-07-22: prior-ADR contradictions are FINE — this becomes a NEW ADR (D4 reversal accepted, persistent owner session IS the direction). (3o) THIRD-PASS RESULT (15 NOVEL findings, full report in task output a5b222b11aa7c1848 tasks dir): N-1 BLOCKER uncapped spend — owner loop runs on core agent Jim which is EXEMPT from SEC-26 daily cost cap (IsPrivilegedAgent=core-only) + PlanBounds has NO money fields (NFR-1) → OPERATOR DECISION 2026-07-22: NO money caps ANYWHERE (cost cannot be reliably measured); a per-plan/per-goal TOKEN budget is the discussable form — tokens counted from provider usage, debited by owner-loop + member + verifier turns regardless of core exemption, brake failed(budget_exhausted) in TOKENS; N-2 BLOCKER owner can author evidence-fabricating tail members (self-attesting prose) → machine/behavior rungs outweigh prose, flag post-unmet-authored artifacts, red-team acceptance case; N-3/N-4/N-11 judging cardinality: define GOAL-BEARING sessions (delegate children NOT goal-bearing, one verdict per goal-bearing scope); per-goal-id idle timers + pills (multi-goal per session); owner-as-task excluded from task idle trigger (else double DoD adjudication); N-5 compile-time FEASIBILITY gate (behavior tool must be policy-reachable, command must resolve) else immutable+fail-closed=guaranteed round exhaustion; N-6 /goal re-statement = diffed+confirmed amendment, never silent recompile; N-7 Play needs new owner-session GENERATION via resumed_from + cancelled→approved edge (retire literal one-session-forever); N-8 tail append must be TRANSACTIONAL (all-or-nothing, Stop/crash mid-append = pre-append DAG); N-9 human pause: no auto-bind next message (answer-vs-new-instruction, addressable per-question UI, one open question surfaced at a time); N-10 content-egress policy for messages crossing agent/channel boundaries (S-2 secrets≠confidential content); N-12 /goal clear must also cancel in-flight COMPILATION + claim trigger gated on goal-active; N-13 fix incentive gradient (idle path cheaper than claiming → gate atrophies; idle-no-evidence = free teaching steer); N-14 pill state for re-planning; N-15 live-install upgrade rule (quiesce/re-baseline in-flight goals at trigger-semantics change, fold into boot sweep). Risk register R1-R5 in report. ALL THREE REVIEWS DONE. (3p) SKETCH REVISION 2 COMPLETE (2026-07-22, operator /goal): /home/dev/goals-target.html now 12 sections — ALL ~46 findings folded as normative text (operator overrides applied: N-2 = low-likelihood note only; TOKEN budget not money; owner session = proud NEW build superseding D4); new §5 boot/upgrade reconciliation, §6 bounds/token-budget/incentive-gradient, §7 config keys+kill switch, §8 acceptance table G-1..G-16, closing best-in-class comparison (honest: Temporal leads durability, LangGraph generality, Devin hosted sandbox — Omnipus claim = the COMBINATION in one sovereign pure-Go binary); t0/t3/g6 diagrams re-rendered; page verified (8 svgs, 12 sections balanced). (3q) UNIFIED GIT MODEL (operator 2026-07-22): collapse software/non-software split — ONE git-based model for ALL parallel streams: worktree/branch per stream (value = full read view + isolated writes); KEY FACT: git conflicts only on both-sides-same-file edits → disjoint write-sets make merges trivial for ANY file type incl. binary; write-set disjointness = enforced invariant (plan-lint; merge conflict = contention-analysis failure → plan-correction event, never silent); shard+assemble = exception PATTERN for genuinely-shared artifacts (not a second model); fallback ladder: system-git worktrees → go-git local clones → subdirs (degraded); sketch §3c/§3d being rewritten accordingly. (3r) BILL OF MATERIALS (operator 2026-07-22, to add as sketch section after §3c rewrite lands — ONE writer at a time on the html): every behavior → carrier table. Carrier rule: enforced invariant→engine Go; identity/disposition→SOUL.md; on-demand procedure→SKILL.md (PLANNER BEHAVIOR = planning skill, reusable by any create_plan-granted agent, NO Planner-agent fork); turn contract→engine-injected prompt fragment CO-LOCATED with its parser (evidence-gate lesson); affordance→tool schema (create_plan demands criteria/write-sets/rationale); permission→seeds; wire→contracts. Layered: soul disposes, skill teaches, schema demands, engine enforces. Anti-parallel rules: every BOM row names existing mechanism extended or justified-new; one concept one implementation; skills+grants over new agents; BOM = review gate (mechanism not in BOM = flagged parallel infra). (3s) SKETCH FINAL (2026-07-22): §3c/§3d unified git model landed (lint-gated write-set disjointness, isolation ladder, shard+assemble pattern, conflict=plan-correction; G-16 + comparison row fixed) AND §9 BOM landed (15 code-grounded rows; KEY finds: embedded plan skill ALREADY EXISTS pkg/skills/embedded/plan/SKILL.md — extend not fork; SMART compiler = engine-invoked schema-validated turn NOT skill; co-location rule grounded in Gate-2 B2; token budget must ignore IsPrivilegedAgent exemption; 4 anti-parallel rules = review gate). Page: 13 sections, 8 svgs, verified. DOCUMENT COMPLETE, committed to repo as docs/internal/design/unified-goal-plan-subagent-target-design-v2.1.html (cf22d60f, pushed; v2.1 header: Design-complete pre-ADR, supersedes-in-part ADR-049 D4 + ADR-052 triggers). ASSESSMENT committed 5abd7615 (docs/.../v2.1-assessment.html) — 5 blockers/22 majors/9 minors, 5 viewpoints + SOTA.
(3z) V2.1 INTERVIEW DECISIONS (operator, 2026-07-22 — fold into v2.2): D1 child sessions = ISOLATED-but-LINKED (own durable SessionID; typed SessionMessage channel = reliable bridge; curated context snapshot at spawn, no shared transcript — FR-6a refuted, dropped). D2 questions = child asks PARENT, parent decides answer-or-escalate; only direct session asks human (channel-portable, in-chat). D3 git layer SHIPS in-scope, spike-gated (go-git; real per-attempt diffs → resolves idle-evidence + plan-Judge-evidence blockers). D4 correction verbs = add SUPERSEDE (tail marks done-member outcome ignored-by-Judge, record immutable) + TARGETED RETRY (transient/frozen member retried without full Stop/Play). D5 3P workers = honest fire-and-collect; respond = corrective re-dispatch (new session, orig prompt + answer folded in); no needs_input/question advertised to 3P. D6 deep chains = one-hop-to-parent CORRECT by design; escalation = parent decision; depth configurable (my D-1 finding was misframed, not a gap). D7 plan-member tasks have NO individual start/cancel/resume (plan owns lifecycle); standalone tasks keep them; adjust member = stop plan→change→continue. D8 idle = EVERY idle consumes a round (G-2/G-3 stand; N-13 free-first removed); bare-claim first bounce stays free → claiming cheaper than idle (right incentive). D9 unverifiable criteria = rely on COMPILE-TIME feasibility gate only (no criterion_unverifiable verdict); gate must vet semantic judgeability not just reachability. D10 exploratory work = run parallel EACH IN OWN git worktree (worktree=isolatch; merge at join). D11 plan DoD + chat goal = CONVERSATIONAL confirmation (LLM asks in-chat, no form) for both. D12 budget = NO per-plan/per-goal/Judge budgets; convert SEC-26 from app-level USD → app-level overall TOKEN budget (Usage screen accounting+setting), covers ALL workloads no exemptions; bounds = rounds + attempts + one app-level token budget. D13 Play resumes cancelled/failed members from LAST GIT COMMIT (not zero). D14 pill states = add judge_unavailable + queued. D15 message ceiling = PER-CHILD not per-parent. D16 ad-hoc delegate inboxes keyed to durable chat/plan id. D17 .git deny by OPERATION (allow log/blame/show/diff; deny commit/amend/rebase/rm) not path. → NEXT: draft the NEW ADR from it (split: 052a trigger fix ships first), then /grill-spec the ADR, then implementation waves. deterministic engine wrapped in an OWNER AGENT LOOP; Judge = critic feeding that loop; failed members → auto reset+re-dispatch with reasons; all-done-but-DoD-unmet → owner APPENDS an additional task plan to the END of the DAG (blocked_by established outcomes) to close the gap — done work untouched as evidence; DoD immutable (owner extends toward it, never edits it; unachievable → honest fail or ask user); JudgeRounds bounds all cycles. Sketch: /home/dev/goals-target.html. Sketches: /home/dev/goals-target.html (target) + /home/dev/goals-explainer.html (as-is), both self-contained inline-SVG. (4) unmet steer restates persistent goal + per-criterion judge reasons (mirror task-retry pattern); (5) UI: goal display = pill below chat on the RIGHT (like subagent pill) with blue goal icon — NOT the current full-width box above chat; (6) 3 transcript bugs: SVG send_file re-ingested as vision input → xAI 400 kills turn (loop.go:7614); goal recovery re-dispatch starves on own worker pool limit 6 (task_executor.go:1630); blocked-by-safety-guard machine check must read "unable to verify" not fail-closed unmet. Also noted: goal set on ray's session but mia executed (loop drives workspace lead — design question).
