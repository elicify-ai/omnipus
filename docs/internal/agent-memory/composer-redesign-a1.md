---
name: composer-redesign-a1
description: "Chat composer A1 redesign — approved plan location, session-recovery state, and what remains (branch bugfixes2)"
metadata: 
  node_type: memory
  type: project
  originSessionId: 45eb163f-c6f7-4e88-8de3-bcc8667cefc7
---

The chat composer redesign (variant A1: attach+agent+model+tokens context row above input row, ActivityBar folded into the card) was approved by the user on 2026-07-13 and is being implemented on branch `bugfixes2`.

**Why:** the original session (`19b99654-b6a3-4c30-a472-9060be5db8a9.jsonl`) died mid-implementation on a provider usage-limit 429 and cannot be resumed (transcript contains Z.ai `server_tool_use` blocks whose IDs fail Anthropic's `^srvtoolu_` pattern → 400 on resume).

**How to apply:** the full approved plan is at `/home/dev/.claude/plans/clever-toasting-sonnet.md`; design mockups in `.preview-doc/composer-redesign/` (A1-real-styles.png is the vision-verified reference). SHIPPED 2026-07-13: commit `1da62408` (feature) + `6d59df84` (vite proxy) + `78d5cb65` (settings) + `f9274258` (header-QA docs), pushed to `origin/bugfixes2`. FOLLOW-UP SHIPPED 2026-07-14 (`806fb6a4`): operator-directed visual evolution — context row bare above the card, activity pills bare below, disclaimer removed, `--color-surface-2` darkened to `#141416` centrally, surface-1-resting hover tints repointed to surface-3 (else ~1.03:1, imperceptible). The three-zone layout is contracted in `ChatScreen.composer-card.test.tsx`; evolution recorded in `.preview-doc/composer-redesign/README.md`. Follow-ups not yet filed as issues: attach gold-glow on files-attached (needs `useComposer` mock coordination across ~13 test files); `agent_removed` lost across the workspace redirect; `setActiveSession` contract JSDoc; ModelPicker providers-error swallow; team-scoping fail-open; dead reconnect-queue send UI; slash-highlight clamp; ActivityPanel abrupt unmount. The dev install's z-ai provider key is dead (401 "User not found") — streaming states can't be live-tested until it's replaced. Implementation history: ChatControls stripped, all tests migrated/green (280 files/4458 tests), 7-reviewer wave + fix wave applied (notable fixes: `@container` on the composer card — the `@2xl:` token-counter gate is a CONTAINER query that lost its ancestor when leaving the top-bar; `block` on the composer textarea — textareas are inline-block and the descender line-box gap was the residual ~6.4px misalignment; AgentPicker auto-select now preserves activeSessionId to avoid detaching deep-linked sessions), live Playwright verified (0.00px alignment). 14-reviewer final sign-off + commit were the last steps in flight. Known deviations (accepted): overflow `⋯` control omitted; h-chrome-header sizing superseded by the single-flex-container fix. Deferred pre-existing findings (candidates for tracked issues): ModelPicker swallows providers-query errors; workspace team-scoping fails open; dead reconnect-queue send UI + stale aria-label; draft-status message misdiagnosis; typed test fixtures / PENDING_SESSION_ID constant. The dev install's z-ai provider key returns 401 (dead) — live streaming states could not be exercised.

**2026-07-16 PIPELINE COMPLETE** — full chain shipped on bugfixes2: reviewer-gate rounds 1+2 (`1d7799de`, `bc469a5f`) → operator-driven tab-order overhaul: explicit-tabIndex convention repo-wide (codemod 256 sites + primitives + AST tripwire `src/lib/tabindex-convention.test.ts`, `2e74388f`) → always-docked browser panel (Sheet+pin retired, ADR-040 amended, `da2b7626`) → 6-screen reassessment → final 8-agent fix wave (`eb4de2a2`: Esc exits browser driving [was WCAG 2.1.2 trap], Link/NavLink stamping + tripwire extension, keyboard-hijack guards, overlay-sidebar focus trap, env-editor stable ids, [configured] sentinel → write-only secrets, footer flex-col-reverse ban + tripwire). Deferred items for operator: see reassess-findings.md ledger (tablist→nav semantics, card children-presentational refactor, calendar day-cell parity, dirty-form confirms, sheet Close-X DOM position). Suite at completion: 287 files / 4591 tests. KEY CONVENTIONS to preserve: every interactive JSX element carries explicit tabIndex (WebKit); composer ring skip=1/input=2..browser=7; central 1px gold focus ring; no flex-col-reverse in footer primitives.

**2026-07-16 a11y-audit pipeline state (superseded by the COMPLETE entry above):** design → 5 fix agents → commit `e792cb63` → 7-reviewer ROUND 1 (60+ findings; CRITICAL: always-mounted ChannelConfigPanel leaked dirty form values incl. secrets across channels) → 8-agent fix wave → commit `1d7799de` (95 files, +2774/−2933; SessionPanel/SessionItem zombies deleted, skip-link tabIndex=1 + composer ring renumbered 2–7, /clear alias interception restored, SmartSelect ariaLabel required) → pushed → **ROUND 2 of 7 reviewers in flight** over `c6776dd1^..HEAD`. Consolidated round-1 findings: scratchpad `round1-findings.md` (session 45eb163f). Remaining after round 2: fix round-2 findings → rebuild SPA + sync `pkg/gateway/spa/` + go build + redeploy to :8080 (`OMNIPUS_HOME=/tmp/omnipus-hybrid-fresh`, preview https://pod-omnipus.fly.dev) → multi-agent a11y reassessment (one agent per main screen).
