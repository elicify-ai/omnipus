# Wave 1 (B1+C+F+E backend slices) — comment-analyzer review (round 1)

**Scope:** `fba0acbf` vs parent `d0e7374a` on `sendfile-fix`  
**Review focus:** accuracy, rot, public-API completeness, and maintenance burden of comments, package docs, schema descriptions, and the added research artifact.  
**Context:** ADR-051 Rev 4 delivery plan and Wave 0 round-1 comment report.

## Findings

| ID | Severity | File:line | One-line | Fix |
|---|---|---|---|---|
| CA-W1-1 | MAJOR | `pkg/providers/capabilities/catalog.go:16-24`, `pkg/providers/capabilities/catalog.go:40-44`, `pkg/providers/capabilities/embed.go:8-11`, `docs/internal/research/provider-media-format-support-2026-07.md:74-75,105` | The package/docs describe a live gateway service that pulls on startup and every seven days, uses the gateway client, and boots from the embedded seed, but this stack has no non-test caller of `NewCatalog`, `NewGHReleasePuller`, `Refresh`, or `embeddedSeed`; the delivery plan assigns integration to Wave 3. | Rewrite these as future/integration contracts (“when wired by Wave 3”), or defer the operational claims until the gateway actually constructs and schedules the catalog. |
| CA-W1-2 | MAJOR | `pkg/providers/capabilities/puller.go:27-29`, `pkg/providers/capabilities/puller.go:99-110`, `pkg/providers/capabilities/puller.go:227-270` | “Every successful Pull verifies” and “on any failure … falls back” are false: missing/unreachable/non-200/unreadable/empty sidecars all return success without verification, while a release checksum mismatch returns immediately and does not try raw fallback. | Document the exact best-effort checksum policy and the failures that trigger fallback, or change the implementation to match the stronger stated contract. Remove the unsupported “GitHub Release signing”/raw “transport-layer guarantee” rationale. |
| CA-W1-3 | MAJOR | `pkg/providers/capabilities/catalog.go:97-117`, `pkg/providers/capabilities/catalog.go:135-140`, `pkg/providers/capabilities/catalog.go:285-287`, `pkg/providers/capabilities/catalog.go:406-445` | Multiple comments promise that `Catalog.Refresh` serializes Puller/Store calls and takes a write lock, but `Refresh` has no operation-level lock: concurrent refreshes can pull/apply/write in different orders. The same doc says successful refresh is applied **and persisted**, although a Store write failure is swallowed and `Refresh` returns nil. | State the actual field-level locking and best-effort persistence semantics, or add operation serialization/transactional ordering before retaining these guarantees. |
| CA-W1-4 | MAJOR | `pkg/providers/capabilities/catalog.go:74-78`, `pkg/providers/capabilities/catalog.go:312-340`, `pkg/providers/capabilities/catalog.go:397-403` | Public return-value docs are wrong: a known `Model` is not “safe to mutate” because `InputModalities` and an explicit `ResizeBudget` remain shared; an unknown model does not carry the catalog default budget (`OptimisticModel` returns `ResizeBudget:nil`); and embedded boot does not make `Source()` return `"embedded"`—it returns the seed’s research-artifact string. | Deep-copy returned models or document the shared members; describe the nil-budget fallback accurately (or apply the catalog default); document `Source` as the seed-provided value. |
| CA-W1-5 | MAJOR | `pkg/providers/capabilities/catalog.go:84-94`, `pkg/providers/capabilities/catalog.go:162-200` | `ParseSeed` documentation simultaneously says unknown modalities cause schema-validation failure and are accepted, logged through diagnostics, and never persisted. The code accepts any non-empty modality without recognizing or logging it, and successful refresh persists the original bytes. `Notes` is also not diagnostic-only because it is returned by `Resolve`/`Models`. | Specify the behavior actually implemented, or implement the promised known-modality diagnostics/non-persistence policy; remove contradictory “validation failure” and “diagnostic only” claims. |
| CA-W1-6 | MAJOR | `pkg/agent/media_downgrade.go:1-8`, `pkg/agent/media_downgrade.go:19-26`, `pkg/agent/media_downgrade.go:71-75`, `pkg/agent/turn.go:244-259` | Retry-guard comments conflict with each other and the code: several still promise at most one media downgrade/retry for the whole turn and that all subsequent calls return false, while the new implementation intentionally allows one PDF retry **and** one image retry on separate calls. | Replace the obsolete whole-turn invariant with one concise per-class invariant in both files; avoid duplicating the full state machine in comments. |
| CA-W1-7 | MAJOR | `pkg/media/resize/resize.go:10-14`, `pkg/media/resize/resize.go:70-81`, `pkg/agent/loop_media.go:402-418`, `pkg/agent/loop_media.go:491-494` | Comments repeatedly say ladder-floor/unsupported formats are offloaded to `workspace work/`, but the current caller returns `""` and `resolveMediaRefs` emits `[attachment unavailable …]`; the delivery plan assigns step-5 offload to Wave 2 Slice D and orchestration to Wave 3. | Describe the current empty-result/marker behavior and identify offload as pending integration, or land the offload caller before claiming it occurs. |
| CA-W1-8 | MAJOR | `pkg/media/library/library.go:1-404`, `pkg/workspace/media_delete.go:9` | The new library’s public API is effectively undocumented: there is no package comment and 16 exported types/functions/methods plus exported constants/errors lack Go doc, including security- and persistence-sensitive semantics (`Upload`, `Read`, `ResolveWithWorkspace`, refcounts, GC, `Store`/`Load`). `WorkspaceDeleteHook` is also undocumented and currently test-only. | Add concise package documentation and doc comments covering ownership, durability, mutation/copy behavior, limits, error contracts, GC semantics, and workspace isolation; document the hook as a B1 stub pending B2 wiring. |
| CA-W1-9 | MAJOR | `pkg/agent/translate_error.go:56-69`, `pkg/agent/translate_error.go:320-336`, `pkg/agent/translate_error.go:352-399` | The classifier precedence comment says body substrings override an explicit status only for two narrow media cases, but generic 4xx also maps policy, context, tool-args, and schema bodies. It also says 413 maps to provider-rejected, then the implementation maps a context-looking 413 to `CodeContextTooLong`. | Rewrite the precedence table directly from the switch order, including every 4xx body override and the 413 context exception; keep one authoritative description rather than competing per-constant prose. |
| CA-W1-10 | MAJOR | `docs/internal/research/provider-media-format-support-2026-07.md:9-16`, `docs/internal/research/provider-media-format-support-2026-07.md:43-60`, `docs/internal/research/provider-media-format-support-2026-07.md:101-105` | The “freeze-gate re-validation” prose overstates its evidence: it says each provider was re-checked live and the seed is frozen, while also admitting inaccessible pages reused the prior snapshot and release-checklist sign-off is still pending. It further promises the unwired seven-day pull will pick up corrections. | Mark the artifact as provisional/pending sign-off, distinguish live-observed rows from inherited claims row-by-row, and remove runtime-refresh promises until wiring exists. |
| CA-W1-11 | MINOR | `pkg/providers/capabilities/embed.go:18-20`, `pkg/providers/capabilities/puller.go:220-223` | `embeddedSeed` and `rawURL` are described as “Exposed,” but both are unexported package-private helpers; only tests/package internals can call them. | Say “available within the package” or export them if external diagnostic tooling is genuinely intended. |

## Maintenance-burden note

The E/F/C slices add long comments that restate wave numbers, plan steps, FR identifiers, tests, future files, and implementation order. Several findings above are already rot caused by that duplication. Keep durable invariants and caller-visible contracts near the code; leave delivery sequencing and future-wave behavior in the plan/spec.

## Hypothesis disposition

- **Confirmed — inaccurate comments:** pull verification/fallback, refresh serialization/persistence, model-copy/default/source behavior, classifier precedence, per-class retry count, and step-5 offload claims disagree with the implementation.
- **Confirmed — incomplete public API docs:** the new `pkg/media/library` surface exposes 16 undocumented declarations/methods, and `WorkspaceDeleteHook` has no contract comment.
- **Confirmed — high rot burden:** Wave/step/FR narratives are duplicated across package docs, function comments, tests, and the research artifact; current Wave 1 comments already describe Wave 2/3 behavior as present.
- **Rejected — catalog/offload behavior exists elsewhere in the stack:** repository search found catalog constructors/refresh only in capability tests, `WorkspaceDeleteHook` only in tests, and no current step-5 offload call from `resolveMediaRefs`.
- **Rejected — generated schema descriptions remain affected by the Wave 0 UUID issue:** `MediaLibraryEntry.id` now has `format: uuid`; the new library writes UUIDs. UTC timestamp wording also matches the library’s explicit `.UTC()` writes.

## Verdict

**REVISE — 0 CRIT / 10 MAJOR / 1 MINOR / 0 OBS.**
