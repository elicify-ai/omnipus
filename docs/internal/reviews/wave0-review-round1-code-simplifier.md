# Wave 0 Review — Round 1 — Code Simplifier

**Scope:** `f6eccbcd..08690ff9` (`sendfile-fix`, Slice A / FR-031)

## Findings

| ID | Severity | Section | One-line | Fix |
|---|---|---|---|---|
| CS-01 | MAJOR | `contracts/openapi.yaml` — operations | Slice A adds three speculative REST operations (`listWorkspaceMedia`, `getWorkspaceMedia`, `createWorkspaceMediaAttachment`) even though FR-031 and the Wave 0 plan require only the two component schemas and their generated types; this prematurely commits unimplemented endpoint semantics and produces roughly 300 lines of unnecessary generated surface. | Keep the two schemas registered under `components.schemas`, which is sufficient because oapi-codegen has `skip-prune: true`; defer concrete media routes to their owning backend/frontend slice after request, response, delete, and handler behavior are specified together. |
| CS-02 | MAJOR | `MediaAttachmentRequest.yaml` | `content_injection_override` and `position` are speculative controls with no FR, BDD, or ADR behavior behind them; the override also creates an unnecessary alternate presentation path that bypasses the automatic layer promised by the feature. | Reduce the request to the required `media_id`; add ordering or content-override fields only when a governing requirement and implementation need them. |
| CS-03 | MINOR | `MediaLibraryEntry.yaml` | `last_refcount_seen_at` is not part of FR-003's canonical manifest shape, and exposing it plus `refcount` turns internal GC bookkeeping into public API without a stated consumer. | Remove `last_refcount_seen_at`; keep manifest refcount internal unless a later slice identifies and specifies a wire consumer, then add only the required public field. |
| CS-04 | OBS | `scripts/gen-asyncapi-go/main.go` | The generator repair is proportionate: one naming-convention match plus one structural comparator replaces hand-edited generated payload pointers without adding a second generation path or per-type special cases. | None. |

## Rejected candidates

- The copies under `pkg/gateway/inboundschemas/` are generated by the existing canonical schema-sync step, not hand-maintained duplicate logic.
- The `src/lib/llm-error.ts` change collapses duplicated handwritten wire shapes into generated aliases; it is simplifying rather than gold-plating.
- The generated Go/TypeScript artifacts are expected mechanical output; their size is not itself a finding once the unnecessary operations are removed at the source schema.

## Verification observed

- `git diff --check f6eccbcd..08690ff9` — clean (no output).
- `CGO_ENABLED=0 go test -count=1 ./scripts/gen-asyncapi-go` — PASS.

## Verdict

**REVISE — 0 CRIT / 2 MAJOR.**
