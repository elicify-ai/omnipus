# ADR-091 — Shell permission modes: Ask / Auto / God Mode; drop the block list; one rule format

- **Status:** Proposed (founder-approved decisions incl. UI, 2026-09-23)
- **Date:** 2026-09-23
- **Deciders:** Daniel Piatkowski (founder, ratifying decision); architect (design, drafting)
- **Supersedes-in-part:** [ADR-036 — Consolidate shell and subagent tools](./ADR-036-consolidate-shell-and-subagent-tools.md) §3.1's deny-pattern axis ("on by default... layered with the operator-extensible custom-pattern mechanism") and its "one approval flow" decision (still valid; this ADR adds the mode selector in front of it, does not reopen it).
- **Extends:** [ADR-077 — Two-layer tool-policy model](./ADR-077-two-layer-tool-policy-remove-fail-closed-backfill.md) — the three modes select which value the `bash` tool policy resolves to at Layer 1; the two-layer ceiling/override model itself is untouched.
- **Evidence baseline:** `release/v0.1.1` @ `838d8092c` (worktree `wt-release-session`), read-only. Findings cited below trace to three lane reports written against this same commit: the as-is call-chain trace, an industry comparison against Claude Code and Codex CLI, and a re-verification of issue #83 and its "Additional" claim.

## Context

### The problem: six mechanisms, no single story

A `bash` call today passes through, in fixed order: (1) a hardcoded ~38-regex deny list (`defaultDenyPatterns`, `pkg/tools/shell.go`) that cannot be disabled and is unconditional even under God Mode; (2) a structural command-substitution guard (`pkg/tools/shell_subst_guard.go::substitutionGuard`); (3) operator-configured deny patterns, global (`config.SandboxConfig.ShellDenyPatterns`) and per-agent (`config.AgentShellPolicy.CustomDenyPatterns`/`EnableDenyPatterns`), both opt-in and off by default; (4) a command-text path-containment scan with an ADR-068 read/write carve-out (`pkg/tools/shell_path_guard.go::guardCommand`); (5) a per-binary exec allowlist that is a no-op unless an operator configures `security.policy.exec.allowed_binaries` (`pkg/policy/evaluator.go::EvaluateExec`); and, earlier in the agent loop, (6) the ADR-077 tool-policy verdict for the tool name `"bash"` itself (global ceiling × per-agent override, strictest wins — default `allow`, so by default nothing above even fires an approval dialog).

None of these six is redundant by design; they accumulated independently across several ADRs (ADR-036, ADR-068, the v0.2 #155 hardening pass) and nothing coordinates between them or explains, to an operator or a founder, what actually stops a destructive command in each mode. Mechanisms 1 and 3 share the same fundamental weakness: they are regexes over lowercased raw command text, and the code's own comment states it plainly — "a list of phrasings, not a judgement about the act." They are defeated by mid-token quoting (`r'm' -rf /`), `$IFS`-based whitespace substitution (`rm${IFS}-rf`), or simple variable indirection (`X=rm; $X -rf /`) — none of which changes what the shell executes, all of which changes what the regex sees. Where a kernel sandbox is present and enforcing (Linux 5.13+, Landlock), it is the real boundary for *where* files can be touched; the deny-pattern layer is the only thing standing between an agent and a destructive *phrasing* against files the kernel would otherwise allow it to touch (`rm -rf .` inside its own already-permitted workspace) — and that is the bypassable layer.

### GitHub issue #83, and its correction

Issue #83's headline claim — "Always Allow" on one command silently grants every future invocation of that binary with any arguments — traces in the codebase to `pkg/security/execapproval.go::ExecApprovalManager` (`CheckApproval`, `PersistPattern`, `matchAllowlistPattern`). Re-verification against `838d8092c` confirms the code still behaves exactly as the issue describes *in that file* — but also confirms `ExecApprovalManager` has **zero production callers anywhere in the repository**; it is referenced only by its own package's tests. Its answering frontend (`ExecApprovalBlock`/`ExecApprovalTool`) and its gating WebSocket frame were deleted by ADR-036 §3.4, which relocated grant consultation to `pkg/security/approvalgrants.go::ApprovalGrantStore` instead. The issue's headline finding is real code with the behavior described, but the code is dead — no chat session reaches it. The **live** "Always Allow" mechanism (`ApprovalGrantStore.Record`) does not have this defect: it fingerprints the entire args object (`command`, `cwd`, `run_in_background`, …) per `(session, agent, tool)`, in memory only, so a differently-worded command does not silently match an old grant.

Issue #83's *second*, "Additional" claim is a different code path and is confirmed **still live**: the operator's opt-in exec allowlist (`pkg/policy/evaluator.go::EvaluateExec`) matches the raw command string against configured glob patterns with no call to `exec.LookPath`, `filepath.Abs`, or symlink resolution anywhere in the match path — an approved pattern like `"git *"` says yes to `git evil-script` without ever checking which file named `git` is about to run. Given an attacker who can place a file earlier on `PATH` than the trusted binary directories, or symlink over an allowed name, the allowlist provides no defense. Decision D3 below closes this gap directly.

### Why simplify now

Six independently-evolved mechanisms with no shared vocabulary is not a security posture, it is an audit liability: neither an operator configuring the system nor a founder answering "what actually stops this" can give a one-sentence answer. Anthropic's own Claude Code and OpenAI's Codex CLI — surveyed for this ADR — independently reached the same conclusion: neither ships a curated "dangerous command" deny list, and both say so explicitly. Claude Code's docs state outright that "Bash permission patterns that try to constrain command arguments are fragile" and point to the OS-level sandbox as the actual boundary. Codex ships no default `execpolicy` rules at all and instead intercepts `execve(2)` at the syscall level. Both treat their text-level rule engines as UX friction-reducers, not security boundaries, and both give deny an unconditional trump card over allow regardless of rule specificity. This ADR adopts that posture for Omnipus.

## Decisions

### D1 — Three modes: Ask, Auto, God Mode

Every bash-capable context resolves to exactly one of three modes (scope: global, per-agent, per-chat — see the three-level merge below and UI presentation):

- **Ask** — every shell command shows the approval dialog. No auto-run.
- **Auto** (the default for fresh installs) — commands run without asking while a kernel sandbox confines them; anything needing more than the sandbox allows asks (D7). Where no kernel sandbox is active, **Auto behaves like Ask** — the app-layer guards are then "the only thing standing between the agent and the filesystem/network" (the as-is behaviour matrix's own finding), and Auto must not silently trust them the way it trusts a real kernel boundary.

- **God Mode** — no approvals, no kernel sandbox, no network egress filter. This is the existing `sandbox.GodMode` field (`pkg/config/sandbox.go`), gated as today behind availability (`--allow-god-mode` or the persisted `GodModeAllowed` grant) and a build tag. This ADR does not add a new bypass switch; it renames the operating posture already implemented and clarifies exactly what it turns off (D6).

**"Kernel sandbox active" — per-platform predicate** (drives the Auto/Ask fallback and the badge's status field; both read exactly this):

| Platform | "Kernel sandbox active" | Auto behaviour |
|---|---|---|
| Linux | Landlock available **and** applied — `sandbox.mode = enforce`, not degraded to the app-level fallback | Auto |
| macOS | Seatbelt backend active | Auto |
| Windows | Never — no kernel sandbox backend | Ask |

macOS **counts as sandboxed**: for a `bash` call the confined thing is the child process, and Seatbelt does confine the child (`pkg/sandbox/backend_darwin_seatbelt.go`). The earlier "Linux/macOS app-level fallback" wording was wrong to treat macOS as *always* fallback: macOS is sandboxed → Auto when the Seatbelt backend is active, and falls to the app-level fallback → Ask only when Seatbelt is off or unavailable — the same way Linux does when Landlock is off. Windows alone is "never." The badge's "kernel sandbox active" bit is a contract-defined status field (UI presentation, below) — it must equal this predicate, not be re-derived client-side.

**Mode applies at three levels, tighten-only below the global default** (founder UI decision, 2026-09-23), mirroring ADR-077's own ceiling/tighten philosophy:

- **Global default** — Settings → Security (`SecuritySection.tsx`). Sets the operating mode for the whole install. God Mode is **global-only** — it does not appear as a per-agent or per-chat choice — and keeps its existing password step-up (`sandbox.GodModeAllowed`, unchanged).
- **Per-agent** — the agent's Tools & Permissions screen (`ToolsAndPermissions.tsx`) may set a mode **only stricter** than the global default (e.g. global Auto → this agent Ask). It cannot loosen below the global default (an agent cannot set Auto or God Mode when the global default is Ask); God Mode is not offered here at all.
- **Per-chat** — a quick switch in the chat composer ("`[Auto ▾]`") may tighten **further, for that chat only** (e.g. global Auto, agent Auto, this one chat Ask). Same rule: it can only tighten, never loosen, and never offers God Mode.

**Loosening below the global default is not possible at any lower level — that is the rule, stated once here and not repeated at each level below.** It is enforced **server-side at the write endpoint**: the REST handler re-reads the global default at write time and returns a 4xx on any loosening write (per-agent or per-chat). The UI hiding the looser option is defence-in-depth, not the enforcement — any API client, a stale SPA tab, or an agent itself reaching a mode-write API is still bounded by the server check, so an agent can only ever tighten its own mode. Architecturally, the per-chat quick switch is **not** a third entry in ADR-077's two-layer tool-policy maps (Hard Constraint #6: "two layers, no third" is non-negotiable) — it is a session-scoped resolution modifier, applied *after* the existing global-ceiling × per-agent-override merge resolves the `bash` policy value, in the same structural sense that `ApprovalGrantStore` already sits outside the two-layer model as a session-scoped store consulted only once policy resolves to `"ask"`. Implementers must not add a `chat_id` key to `cfg.Sandbox.ToolPolicies` or `AgentConfig.Tools.Builtin.Policies` to carry this; it is new, separate, session-keyed state.

**Delegation (decision): the session modifier inherits to delegated subagent sessions exactly as approval grants do** (`pkg/security/approvalgrants.go::InheritFrom`, ADR-057). A subagent spawned mid-chat runs under the **tightest of (the parent's session modifier, the delegate's own per-agent override)** — both on top of the global default. Without this, a chat tightened to Ask is escaped by delegating to an agent with no override (which resolves global Auto and runs without prompts) while the user's own grants *do* flow down — consent moving in opposite directions for two session-scoped mechanisms. **Terminology:** the modifier is *session*-scoped (dies with the session, like grants); the composer labels it "per-chat" because a chat is normally served by one session. On gateway restart the modifier clears with the session and the chat resumes at its global/agent default — the two lifetimes diverge only if a chat outlives a session, and a resumed chat is then a fresh consent context, which is acceptable. **Mid-turn changes:** a command is resolved against the mode in force when it is *resolved*; a tighten that lands while a command is already pre-flight-evaluated does not retroactively change that command's verdict, only subsequent ones.

**In all three modes**, the audit log, the prompt-injection guard, and rate limiting stay on. Verified against the as-is trace: God Mode's own field comment already states these three are NOT disabled ("those defend against external threats, not agent freedom") and the call-chain evidence confirms `emitAuditOrDeny`, the guard chain, and rate limiting are not conditioned on `godMode` anywhere in `executeRun`. Nothing in this ADR changes that.

**Operator-authored `deny` rules stay in force in God Mode — this is NEW behaviour, not a restatement.** Today God Mode's floor (`pkg/tools/compositor.go::resolveEffectivePolicyWith`, `if cfg.GodMode { g = allow }`) replaces the *global* ceiling, so a global `bash: deny` does **not** survive God Mode; only an agent's own per-agent `deny` does (strictest-wins). D3's operator rules therefore surviving God Mode is a change, not today's behaviour. The mechanism that makes it implementable: **D3 rules are enforced inside the shell tool, downstream of policy resolution** — `executeRun` evaluates them *after* the compositor has already floored the ceiling to `allow`, so the floor cannot erase them. This matches Claude Code's managed-deny precedent (a managed `deny` is the one thing no mode, including `bypassPermissions`, can override). D6's "no behavioural change beyond D1–D3" holds — this *is* one of the D1–D3 changes.

### D2 — Drop the built-in shell block list entirely

`defaultDenyPatterns` (`pkg/tools/shell.go`, ~38 regexes — this variable already includes `secretGuardPatterns`, appended at package-init time: `append([]*regexp.Regexp{...38 patterns...}, secretGuardPatterns...)`, so dropping `defaultDenyPatterns` drops both in one motion) and the per-agent `AgentShellPolicy.CustomDenyPatterns`/`EnableDenyPatterns` machinery are removed outright — not disabled, not left dormant behind a flag.

**Rationale:** text patterns over a real shell are bypassable by construction (mid-token quoting, `$IFS` substitution — see Context); Claude Code's own docs call this class of rule "fragile"; neither Claude Code nor Codex ships one; the kernel sandbox is the real boundary where one exists. The regex secrets backstop (`secretGuardPatterns`) is, by its own in-code comment, "a backstop over a boundary the kernel sandbox already enforces" — the secret files it names (`master.key`, `credentials.json`, `config.json`, `cli.token`, `auth.json`, backups) live under `~/.omnipus/`, outside any agent workspace, so where a kernel sandbox is active it already denies filesystem access to them independent of command text.

**Architectural resolution, not a separate founder instruction:** the global `sandbox.shell_deny_patterns` field (`config.SandboxConfig.ShellDenyPatterns`) is not named in the founder's list above, but leaving it in place as a second, parallel regex mechanism alongside D3's new unified rule format would recreate exactly the "six mechanisms, no shared vocabulary" problem this ADR exists to close. Reading D2 together with D3's "one rule format," this ADR retires `ShellDenyPatterns` as well: an operator who wants to block a command family expresses it as a `deny` rule in the new format (D3), not a regex. `[INFERRED — flagging this bridge explicitly since the founder's list named the per-agent fields, not this one, by name.]`

**Accepted residual risk, stated plainly:** on Windows (no kernel sandbox exists at all today) and inside an agent's own workspace (where the sandbox, when present, permits deletion by design), nothing in this ADR blocks a destructive command in God Mode, or in Auto running without a kernel sandbox. Auto's fallback to Ask (D1) is the compensating control for the second case; God Mode's inherent posture — no approvals, no sandbox — is the accepted trade of the first, exactly as it is today (God Mode is opt-in, gated, and already documented as removing containment). Nothing about this ADR makes God Mode more dangerous than it already is; it removes a bypassable regex layer that, on the evidence above, was providing a false sense of coverage rather than real containment.

**What survives, decided per guard from the as-is evidence:**

| Guard | Mechanism | Fate |
|---|---|---|
| `defaultDenyPatterns` (~38 regexes + `secretGuardPatterns`) | Regex over lowercased raw text | **Dropped** |
| Per-agent `custom_deny_patterns`/`enable_deny_patterns` | Regex, opt-in | **Dropped** |
| Global `sandbox.shell_deny_patterns` | Regex, opt-in | **Retired**, folded into D3's unified rule format (see bridge above) |
| Structural command-substitution guard (`pkg/tools/shell_subst_guard.go::substitutionGuard`) | Tokenizes `$(...)`/backticks, judges command position, not blanket-refusing | **Survives unchanged** — it parses structure, not text-matches phrasing; the founder's task brief names it explicitly as a survivor |
| Path-containment / secret-exclusion guard (`pkg/tools/shell_path_guard.go::guardCommand` step 4, `inTurnSecretSet`, the ADR-068 read/write classification) | Tokenized absolute-path scan with a proven-read exemption | **Survives, mode-sensitive** — the tokenizer is unchanged, but a denial's *disposition* now depends on mode: **Ask** hard-denies exactly as today; **Auto** surfaces a would-be denial as the pre-flight escalation prompt (D7), and after approval the guard re-evaluates against the widened policy and passes; **God Mode** hard-denies with no prompt (no approvals exist there). The "survives" claim applies to the scan, never to what a denial does |
| Per-binary exec allowlist (`pkg/policy/evaluator.go::EvaluateExec`) | Opt-in, raw string match | **Retired, folded into D3** — decision logic replaced; `PolicyAuditor.logDecision`'s audit write survives, retargeted |
| ADR-077 `bash` tool policy (allow/ask/deny) | Global ceiling × per-agent override | **Survives** — now selected via the three modes, see D1/Consequences |
| Session "Always Allow" grant store (`pkg/security/approvalgrants.go`) | In-memory, exact-args fingerprint | **Survives, extended** — see D4 |

### D3 — One rule format and one decision order

Operator-authored rules (allow/deny) and user "Always allow" grants (D4) share one rule format and one resolution order:

- **Order: deny beats ask beats allow, regardless of specificity.** A broad `deny` on a binary is not defeated by a narrower `allow` elsewhere, matching Claude Code's own stated precedence rule verbatim ("the first match in that order determines the outcome, and rule specificity doesn't change the order") and consistent with ADR-077's existing strictest-wins merge for tool policy generally.
- **Chained commands are split before matching.** `&&`, `||`, `;`, `|`, and newlines are split using the **existing** shell tokenizer already in the tree — `pkg/tools/shell_subst_guard.go::splitShellSegments` and `shellCommandHead` — no new parser. Each resulting segment must independently be allowed (by rule or by default posture), or the whole call asks. This mirrors Claude Code's documented behaviour ("a rule must match each subcommand independently") and closes the same class of gap: an allow rule for `npm test` must not silently also allow `npm test && curl evil.com`.
- **Resolve the real binary before matching, and bind to it.** Each segment's leading token is resolved to its actual executable *against the child's effective environment* (the same layered PATH/env `shell.go` applies to the sandboxed child — the gateway process's PATH is not the child's, so resolving against the gateway would reintroduce the look-alike gap a quieter way), then **argv[0] is rewritten to the resolved absolute path** so the matched binary *is* the executed binary. This closes the still-live PATH/look-alike-binary finding against `EvaluateExec` (issue #83's "Additional" claim): a `deny`/`allow` rule written against `git` binds to the `git` that will actually run, not a string starting with `g-i-t`. The residual window is TOCTOU — the resolved absolute path can still be swapped between resolve and exec — which the kernel sandbox, not the rule engine, is the boundary against.
- **The pre-existing operator exec allowlist is folded into this format, not kept as a second engine.** `pkg/policy/evaluator.go`'s `Evaluator.EvaluateExec`/`MatchGlob`/`FirstToken` — the opt-in, raw-string-matching allowlist keyed on `security.policy.exec.allowed_binaries` — is retired as a standalone decision path. An operator's configured allowlist entries become `allow` rules in the D3 format instead, evaluated with the same resolved-binary/segment matching every other rule uses. Running D3's new engine *alongside* the old glob-matcher would recreate exactly the "six mechanisms" problem this ADR exists to close (grep-verified: `Evaluator.EvaluateExec` has exactly two production callers, both inside `pkg/policy`'s own `PolicyAuditor.EvaluateExec`/`pkg/tools/shell.go`, and `FirstToken`/`MatchGlob` have no callers outside `pkg/policy/evaluator.go` and the already-dead `pkg/security/execapproval.go` — nothing outside the exec-allowlist path depends on this file, so folding it in is safe to do completely). `PolicyAuditor.logDecision`'s audit-log write is preserved, retargeted to log the D3 engine's decision instead of `Evaluator`'s — the audit trail does not go away, only the decision logic it logs.

This is new engine work, not a config flag: the new rule matcher needs the resolved-path step and the segment splitter as a new caller (the splitter's existing callers are the substitution guard, not this).

**Decision order — mode → rules → grants → outcome, per segment.** (1) The **mode verdict** (D1) resolves the effective `bash` posture: Ask = `ask`, Auto = `allow` + pre-flight, God Mode = `allow`. (2) **D3 operator rules**, deny-first: a matching `deny` → refuse (every mode, including God Mode — D1); a matching `ask` → prompt (every mode). (3) **Session grants** (D4) and operator **`allow` rules** auto-approve only in Auto and God Mode; in **Ask** mode they do **not** suppress the prompt — Ask's floor is "always ask," and the only things that move the verdict there are `deny` (refuse) and `ask` (no-op above the floor). (4) **Outcome**: run (Auto/God), prompt (Ask, any `ask` rule, or Auto's pre-flight escalation), or refuse (`deny` rule). **Operator rules are config-file-only** — a new `config.SandboxConfig.CommandRules` list (json `command_rules`), each entry `{action: allow|ask|deny, binary, arg_prefix?}` — and do **not** cross the gateway/SPA boundary in this ADR, so they need no contract schema (Hard Constraint #8 applies only to wire formats). If the SPA ever displays or edits rules, that is future work and a schema comes first.

**Honest gap:** D3 rules bind to a segment's head and do **not** see through interpreters or wrappers — `sh -c 'rm -rf X'`, `xargs rm`, `find … -exec rm`, `awk 'system(…)'` all escape a `deny` on `rm`. They are a friction layer, not a containment boundary; the kernel sandbox is the boundary — the same posture quoted from Claude Code in Context.

### D4 — Approval dialog: three buttons, a scope choice, chained commands shown per part

The approval dialog (`src/components/agents/ToolApprovalModal.tsx` — verified path; the coordinator's shorthand named a `chat/` location that does not exist in the tree) is **[Deny] [Allow once] [Allow]** (founder UI decision, 2026-09-23) — replacing today's four-button `Approve`/`Deny`/`Always Allow`/`Cancel`:

- **Deny** — refuses this call. No grant recorded.
- **Allow once** — approves this one call. No grant recorded — functionally today's `Approve`, renamed to make the one-shot nature explicit against the new `Allow` option below.
- **Allow** — approves this call **and** records a session-scoped grant (`ApprovalGrantStore`, in memory only, wiped at session close — unchanged from today's mechanism), with a scope choice presented **above** the three buttons:
  - **"(•) exactly this command"** (default, pre-selected) — **keeps today's scope exactly: command text + working folder (`cwd`)** (founder decision, 2026-09-23, resolving former Open Question 3) — today's `ApprovalGrantStore` full-args fingerprint, unchanged.
  - **"( ) anything starting with `<suggested prefix>`"** — the new, looser prefix grant, a `{binary, arg_prefix}` rule in D3's unified shape, scoped to the session instead of globally or per-agent. A prefix grant **ignores `cwd`**: it covers the command family in every folder (deliberate, stated — the dialog still shows the command's `cwd` so the user sees the context, but the grant does not bind to it). **`run_in_background` stays a match dimension for both scopes**: a grant made against a foreground command does not auto-approve a detached background run of the same family — the user approves the background variant separately.

**Chained commands grant one rule per part** (founder decision, 2026-09-23, resolving former Open Question 4): choosing "Allow" (either scope) on a compound command records one grant per segment, mirroring the dialog's own per-part display below and Claude Code's documented behaviour (one rule per subcommand of an approved compound). A later call matching some but not all of a previously-granted compound's segments still prompts for the unmatched segment(s) only, per D3's per-segment evaluation.

**Suggested-prefix derivation algorithm** (founder decision, 2026-09-23, resolving former Open Question 5): the suggested prefix is the resolved binary plus its leading sub-command words, stopping at the first token that is a flag (starts with `-`), a path-shaped argument, or a URL. Examples: `npm run test -- --watch` → `npm run test`; `git diff src/` → `git diff`; `curl https://x.com` → the very next token is a URL, so there is no useful prefix — offer the exact tier only, no prefix radio (a future per-website option is noted above, not built here). A leading environment-variable assignment (`FOO=1 cmd …`) is stripped before deriving the prefix and the grant matches under any assignment value, not the one observed — mirroring Claude Code's "known-safe leading assignment" handling. A known wrapper (`sudo`, `env`, `timeout`, `xargs`, `sh -c`) is **not** stripped — the prefix is derived starting from the wrapper itself (`sudo apt install …` → `sudo apt install`), never from what the wrapper executes, so a grant on `sudo apt install` cannot silently cover `sudo rm -rf /`. This is the opposite of Claude Code's own wrapper-stripping (which strips wrappers to widen an *existing* rule's match); here a *new* rule is being derived from one observed command, and deriving it from the wrapper, not through it, is the safer of the two directions.

The dialog shows the command and its working folder (`cwd`) — matching today's `BashApprovalPreview.tsx` behaviour — and, for a chained command (D3's segment split), **lists each part separately** rather than showing the raw compound string as one block, so the user can see what each segment resolves to before choosing a scope.

**There is no `Cancel` button** in the new three-button design; Escape/overlay-click/X resolve to `Deny` as they do today (default-deny-on-dismiss is unchanged — Deny remains the default keyboard focus target).

Network tools getting a per-website "always allow this domain" option is **future work, not decided here** — flagging explicitly per the founder's own note, not inferred.

**Session grants are not shown anywhere** (founder UI decision, 2026-09-23) — no list, no chip, no "manage grants" surface. A recorded grant is invisible after the moment it is created; it simply stops future matching calls from prompting for the rest of that chat session, and ends with the chat exactly as `ApprovalGrantStore.ClearSession` already does today. **Explicit consequence:** a user who grants "Allow" and changes their mind mid-chat cannot revoke it except by starting a new chat — there is no in-session undo. This is recorded as a deliberate trade (see Consequences → UX), not an oversight.

### D5 — Delete the dead code

`pkg/security/execapproval.go` (`ExecApprovalManager`, `PersistPattern`, `matchAllowlistPattern`, `NewExecApprovalManager`, `WithAllowlistFile`) and its dedicated test file (`pkg/security/execapproval_test.go`) are deleted outright — definition and all, no deprecation shim, no retirement comment left in its place beyond what CI guard scripts require elsewhere in this repo (house precedent: ADR-037, ADR-061, ADR-077 D3/D6, ADR-088 D9). It has zero production callers today (confirmed by repo-wide grep) and answers a UI flow (`ExecApprovalBlock`/`ExecApprovalTool`) already deleted by ADR-036. Leaving fully-tested, fully-implemented dead code with the exact defect issue #83 describes sitting in the tree is itself a liability — a future reader could re-wire it "to fix #83" and reintroduce a reachable version of the bug this ADR is explicitly not shipping.

### D6 — Fix God Mode's code comment

`pkg/config/sandbox.go`, the `GodMode` field doc comment currently states God Mode forces "shell guard / deny-patterns off." This is stale against actual behaviour: the as-is trace confirms `guardCommand` (guards 1–4, pre-D2) runs **unconditionally in `executeRun` regardless of `godMode`**, and only the **per-agent** custom deny-pattern list is explicitly dropped under God Mode (`agentShellPolicy = nil // drop per-agent deny patterns under god mode`, `pkg/agent/loop_wire.go`) — the global list was never gated on God Mode. Post-D2, with the block list gone entirely, the comment is rewritten to describe exactly what remains true: God Mode floors every agent's effective tool policy at `allow` (operator/per-agent `deny` rules under D3 still apply, D1), forces the kernel sandbox off, forces network egress open, and does **not** disable the audit log, the prompt-injection guard, or rate limiting. No behavioural change beyond D1–D3; this is a documentation-accuracy fix on a field whose actual runtime behaviour is otherwise unchanged by this ADR.

### D7 — Auto's sandbox-denial detection: pre-flight evaluation against a single source of truth

**Founder decision, 2026-09-23, final** (supersedes an earlier runtime-catch answer — see Alternatives considered): before a command runs, Auto evaluates it against the same rules the kernel sandbox will enforce, and asks when that evaluation says the command needs more than the sandbox allows. The command is never started and then interrupted; the prompt, if any, comes first.

**The hard condition: one policy value, not two implementations.** A pre-flight check that computes its own, separate answer to "what is allowed" is exactly the drift that makes such a check lie — approving what the kernel then blocks, or asking about what the kernel would have allowed anyway. This is not new infrastructure to build: ADR-063 already put exactly this single-source architecture in place, for exactly this reason, and D7's pre-flight evaluator is a third consumer of it, not a fourth opinion:

- **The single source:** `pkg/tools/resolvepath.go::ResolveTurnFSPolicy` produces the turn's one authored `fspolicy.FSPolicy` value.
- **Consumer 1 (app layer, existing):** `pkg/tools/shell_path_guard.go::guardCommand`'s `checkPathSegment` step calls `ResolveTurnFSPolicy` directly and matches candidate paths against it.
- **Consumer 2 (kernel, existing):** `pkg/tools/shell.go::turnKernelPolicy` calls the *same* `ResolveTurnFSPolicy`, then `pkg/sandbox/turn_policy.go::KernelPolicyForTurn(authored)` → `pkg/sandbox/derive_from_fspolicy.go::DeriveKernelPolicy` — named in its own doc comment as "THE single function that turns an authored per-turn policy into a kernel policy" (ADR-063 D1/spec FR-1.3), built specifically because "the app layer and the kernel layer each computed their own answer... and they had drifted in both directions" before it existed.
- **Consumer 3 (D7, new):** Auto's pre-flight evaluator resolves the same `fspolicy.FSPolicy` for the turn and asks the identical question `DeriveKernelPolicy` answers — would the kernel grant this operation — rather than re-deriving a parallel verdict from config.

**What an Auto approval does (founder decision, 2026-09-23, resolving C-1).** When the pre-flight evaluation flags an out-of-containment path, the escalation prompt names the path and the operation(s) it needs. Approving it **widens the turn's filesystem policy for that specific path, for the current chat session only**, and the command then runs. The grant is `{path, operation}` where `operation ∈ {read, write, read+write}` is exactly the access class the pre-flight verdict found the command needs on that path and the current policy denies — nothing broader. The widened value is re-resolved as the single `fspolicy.FSPolicy`, and **both** the app-layer guard (consumer 1) and the kernel ruleset (consumer 2, `DeriveKernelPolicy`) are rebuilt from it *before* the command spawns, so the two stay in step — the single-source-of-truth rule applies to the widened value, not just the pre-widening one. The widening is recorded as a session-scoped grant: a new record kind in the existing `ApprovalGrantStore` (`{path, operation}` alongside the command grant), reusing its session keying, `InheritFrom` on delegation, and clear-on-close lifetime. **Consequence:** the next identical command in the same session resolves the widened policy directly and does **not** re-prompt — approving an escalation is a session-scoped widening, not a one-shot.

**Anti-drift lock test, proposed:** a generated matrix of `{path, operation}` pairs evaluated against a fixed `fspolicy.FSPolicy`; for each pair, assert the pre-flight verdict and the rights `DeriveKernelPolicy` actually encodes into the resulting `SandboxPolicy` agree — any divergence fails the test. This is pure Go-level comparison against `DeriveKernelPolicy`'s output (no live enforcement needed), so it runs on every OS in CI, alongside `derive_from_fspolicy_test.go`'s existing `TestDeriveKernelPolicy_IsTotal` (spec FR-1.4), which already asserts every authored field reaches the kernel rendering. A second, narrower test — actually spawning a Landlock-confined child and confirming its real syscalls succeed/fail exactly as the derived `SandboxPolicy` predicts — is genuinely Linux-only and needs a Linux CI leg; this repo cannot run it locally (CLAUDE.md's OOM/CI-authority rule).

**Honest gaps, stated plainly.** A pre-flight text/path evaluation cannot see everything the kernel sees at syscall time: symlink resolution at the moment of the actual access (the existing path guard already documents this same lexical-vs-realpath gap), a path constructed at runtime inside the command rather than present in its literal text, or a program that re-executes another program under a different identity. **The pre-flight check therefore drives the prompt only — the kernel remains the boundary.** A kernel denial the pre-flight missed (approved as "no prompt needed," then actually denied when the command runs) still fails the command with a clear error; it is never silently allowed and never silently retried unconstrained. This can still mean partial execution in exactly these gap cases — a command that gets partway through before hitting a denial pre-flight did not predict fails there, the same way an unconfined program hitting a real permission error would — but this is now the rare missed-edge-case path, not the normal detection mechanism.

**Where there is no kernel sandbox** (Windows, the app-level fallback, or a Landlock-degraded Linux host) — **Auto falls back to Ask**, per D1's existing rule. `fspolicy.FSPolicy`/`ResolveTurnFSPolicy` is platform-independent (it is the *authored* policy, not a kernel rendering of it), so the same single-source value still exists and still feeds the app-layer guard on these platforms — but with no kernel policy derived from it to check a pre-flight verdict against, there is nothing for Auto's "would the kernel deny this" question to answer, which is exactly why it defers to Ask instead of guessing.

**Network egress is out of Auto's pre-flight scope.** The pre-flight evaluation is over filesystem policy only — the thing the kernel sandbox confines. A `curl evil.com` that is filesystem-contained runs silently in Auto, exactly as today; egress filtering is a separate layer (the proxy) and its posture is unchanged by this ADR. Auto does not relabel or alter egress; it selects filesystem-confinement behaviour and approval posture only.

## Removal inventory (mandatory, same change set)

**Founder requirement, 2026-09-23:** every mechanism this ADR supersedes is deleted completely in the same change set — no dead code, no deprecation shims, no compatibility aliases, no "kept for reference" comments. The codebase must come out of this ADR simpler than it went in, not merely re-labeled. Every item below was checked by grep against `838d8092c` for callers before being listed; where a caller survives outside the removed mechanism, that is stated and the item is scoped or moved to Open Questions instead of deleted outright.

### 1. The built-in block list (D2)

- **Backend, deleted:** `pkg/tools/shell.go::defaultDenyPatterns` (the ~38-regex slice, including `secretGuardPatterns` folded into it) and `buildSecretGuardPatterns`. `pkg/tools/shell_guard.go::applyDenyPatterns`, `compileDenyPatterns`, `denyPatternMessage` — grep-verified: `applyDenyPatterns` has exactly two non-test callers, both in `pkg/tools/shell_path_guard.go` (the hardcoded-list call and the operator-list call), both removed by this ADR; `compileDenyPatterns` has exactly two non-test callers, both in `pkg/tools/shell.go::NewExecToolWithConfig`'s deny-pattern wiring, also removed; `denyPatternMessage` is called only from `applyDenyPatterns`. All three are callerless once D2 lands and are deleted with it. **`lowerASCII` (same file) is NOT deleted** — it has three other callers in `pkg/tools/shell_subst_guard.go` (the surviving substitution guard); move it there (or leave `shell_guard.go` reduced to just `lowerASCII`, implementer's call) rather than deleting a function still in use.
- **Config, deleted:** `config.SandboxConfig.ShellDenyPatterns` (`pkg/config/sandbox.go`, json key `shell_deny_patterns`), `config.AgentShellPolicy.CustomDenyPatterns`/`EnableDenyPatterns` (`pkg/config/config.go`, json keys `custom_deny_patterns`/`enable_deny_patterns`) — and, if `AgentShellPolicy` carries no other field once these two are gone (confirmed: its only properties are `enable_deny_patterns` and `custom_deny_patterns` — see the contract schema below), the `AgentShellPolicy` type itself.
- **Gateway validation, deleted:** `pkg/gateway/sandbox_config_validation.go::validateShellDenyPatterns` and its caller in `pkg/gateway/rest_sandbox_config.go` (the global `shell_deny_patterns` REST-write validation) — the write path for the field, not just the field itself.
- **Wiring, deleted:** `pkg/tools/shell.go::ExecToolDeps.GlobalShellDenyPatterns`/`AgentShellPolicy` fields and their use in `NewExecToolWithConfig`; `pkg/agent/loop_wire.go`'s `agentShellPolicy`/`globalShellDenyPatterns` construction, including the `agentShellPolicy = nil // drop per-agent deny patterns under god mode` carve-out (moot once the mechanism it carves out of is gone).
- **Contracts, deleted (schema-first, Hard Constraint #8):** `contracts/components/schemas/AgentShellPolicy.yaml` in full — confirmed dedicated entirely to `enable_deny_patterns`/`custom_deny_patterns`, no other properties. `contracts/components/schemas/SandboxConfig.yaml`'s `shell_deny_patterns` property (the file itself survives — it carries other, unrelated sandbox settings). `contracts/components/schemas/SandboxConfigUpdate.yaml` and `AgentUpdateRequest.yaml`'s references to these fields. Regenerate `pkg/api/generated/` and `src/lib/api/generated/` after the schema edit (`scripts/gen-contracts.sh`) rather than hand-editing the generated diff.
- **SPA, deleted:** `src/components/agents/ShellDenyPatternsEditor.tsx` and `ShellDenyPatternsEditor.test.tsx` in full (confirmed: a dedicated textarea component for editing deny-pattern regexes, no other purpose). References to it and the two fields removed from `src/components/agents/CreateAgentWizard.tsx`, `src/components/agents/wizard/Advanced.tsx`, `src/components/agents/wizard/types.ts`, `src/components/agents/agentDraft.ts` (+ `.test.ts`), `src/components/agents/AgentProfile.tsx` (+ `.test.tsx`), `src/components/agents/CreateAgentModal.test.tsx`. The `globalDenyPatterns`/`extractShellDenyPatterns` state and the `ShellDenyPatternsEditor` import in `src/components/settings/SandboxSection.tsx` (+ `.test.tsx`) — the deny-pattern section of that screen, not the whole file, which carries other sandbox settings. The `enableDenyPatterns` state and `enable_deny_patterns` read/write in `src/components/settings/SecuritySection.tsx` (+ `.test.tsx`) — again the deny-pattern portion only; this file also gains the new mode selector (UI presentation, above) in the same change set. `src/lib/api/agents.ts`, `src/lib/api/config.ts`, `src/lib/api/security.ts` lose their references to the removed fields once the generated types (regenerated from the contract change) no longer carry them — this should surface as compile errors, which is the correct signal, not something to work around.
- **Tests, deleted:** the dedicated `pkg/tools/shell_secret_guard_test.go` (tests only `secretGuardPatterns`' coverage of `fspolicy.SecretEntriesAlways`, which no longer exists as a mechanism) — **not** `shell_readwrite_guard_test.go`/`shell_mount_guard_test.go`, which the as-is evidence confirms exercise the surviving path-containment guard (`guardCommand` step 4), a different mechanism entirely despite the similar file-naming convention.

### 2. The dead "always allow" manager behind issue #83 (D5)

`pkg/security/execapproval.go` in full — `ExecApprovalManager`, `NewExecApprovalManager`, `CheckApproval`, `PersistPattern`, `matchAllowlistPattern`, `MatchExecAllowlist`, `WithAllowlistFile` — and `pkg/security/execapproval_test.go` in full. Grep-confirmed zero production callers repo-wide for every one of these symbols; `MatchExecAllowlist` specifically confirmed referenced only from within `execapproval.go` and its own test file. Any allowlist file this manager reads or writes at runtime (`WithAllowlistFile`'s target) goes with it — there is no persisted-file format to preserve, since nothing else reads it.

### 3. The separate operator exec allowlist, folded into D3

`pkg/policy/evaluator.go` in full — `NewEvaluator`, `Evaluator.EvaluateExec`, `MatchGlob`, `FirstToken` — grep-confirmed: `FirstToken`/`MatchGlob` have no callers anywhere in the tree outside this file and the already-dead `execapproval.go` (item 2); `NewEvaluator` has exactly one production caller (`pkg/agent/loop_construct.go`, constructing the `Evaluator` fed to `PolicyAuditor`); `Evaluator.EvaluateExec` has exactly one caller, `pkg/policy/auditor.go::PolicyAuditor.EvaluateExec`, which itself has exactly one caller, `pkg/tools/shell.go`. The whole chain is replaced by D3's unified matcher; `PolicyAuditor.logDecision` (a distinct method, the audit-log write) survives, retargeted to log the new engine's verdict. Config field `config.ExecConfig.AllowedBinaries` (`pkg/config/config.go`, json key `allowed_binaries`, env `OMNIPUS_TOOLS_EXEC_ALLOWED_BINARIES`) and its dedicated contract schema `contracts/components/schemas/ExecAllowlist.yaml` are retired, greenfield, no migration — consistent with item 1's treatment of the other removed config surfaces. An operator's existing allowlist entries are not auto-converted to D3 rules; the operator re-authors them, same as the deny-pattern config in item 1. Its **REST and SPA surfaces** go with it, not just the field: `pkg/gateway/rest_exec.go::HandleExecAllowlist` + `sanitiseAllowlist`, the route registration in `pkg/gateway/rest.go` (`/api/v1/security/exec-allowlist`), the `/security/exec-allowlist` path in `contracts/openapi.yaml`, `src/components/settings/ExecAllowlistSection.tsx` (+ `.test.tsx`), its render in `src/components/settings/SecuritySection.tsx`, and `fetchExecAllowlist`/`updateExecAllowlist` in `src/lib/api/security.ts` — all removed, since D3 rules are config-file-only (D3) with no wire format.

### 4. Newly discovered adjacent surface — flagged, not silently removed or silently kept

While verifying item 3, `config.ExecConfig.Approval` (`pkg/config/config.go`, json key `tools.exec.approval`, values `"ask"`/`"off"`, doc-commented "US-7: Interactive approval before exec commands") was found still wired into `SecurityConfig` construction (`pkg/agent/loop_construct.go`, feeding `policy.NewEvaluator`) and consumed separately by `pkg/gateway/rest_exec.go`. This reads as a pre-ADR-036 approval surface — the "US-7" tag and the shape (`"ask"`/`"off"`, not the current allow/ask/deny tool-policy vocabulary) match the era before ADR-036 unified the three shell tools and their approval flows into `bash` + the generic `ToolApprovalModal`. Whether it is still reachable and load-bearing today, or a second piece of dead code adjacent to item 2 that the as-is investigation's tool did not happen to trace, **was not resolved in this pass** — it is out of the evidence gathered for D1–D3 and is listed under Open Questions below rather than assumed dead or silently left in place.

### 5. Stale comments and docs

- `pkg/config/sandbox.go`'s `GodMode` field comment — corrected by D6 above (already in scope, not a new item).
- **Explicitly out of scope:** `pkg/gateway/middleware/csrf.go`'s stale "separate health-server mux" comment on `defaultOperationalExemptPaths` describes the unauthenticated `/reload` endpoint (tracked separately as issue #276, unrelated to shell command guards) — this ADR does not touch it; flagging here only to record that it was considered and ruled out, not overlooked.
- Docs referencing the retired mechanisms by name — confirmed by `grep -rli "shell_deny_patterns\|custom_deny_patterns\|enable_deny_patterns\|execapproval" docs/`: `docs/internal/specs/bash-tool-spec-review.md`, `docs/internal/specs/policy-change-approval-spec.md`, `docs/internal/specs/agent-config-matrix-spec.md`, `docs/internal/specs/tool-consolidation-spec.md`, `docs/internal/specs/agent-form-requirements.md`, `docs/internal/specs/browser-agent-capability-spec.md`, `docs/internal/notes/settings-security-ux-analysis.md`, `docs/internal/notes/session-transcript-readable.md`, `docs/internal/notes/security-ux-pages/page-11.txt`, `docs/internal/_archive/plan/v0.1-ship-readiness-plan.md`, `docs/internal/v01-fix-backlog.md`, `docs/internal/v01-uat-plan.md`, and ADR-025's review doc. Each needs a pass at implementation time to either mark the reference historical (if it is describing what the code used to do) or update it (if it is live guidance an operator would still read) — this ADR does not resolve which is which for every doc; that triage is an implementation-outline task (below), not an architecture decision.

### CLAUDE.md "Retired surfaces" addition

Add to CLAUDE.md's "Retired surfaces — do NOT reintroduce" list, matching the existing entries' format: the hardcoded shell deny-pattern block list (`defaultDenyPatterns`/`secretGuardPatterns`) and the operator/per-agent deny-pattern config surfaces (`shell_deny_patterns`, `custom_deny_patterns`, `enable_deny_patterns`) — guard: `scripts/check-no-shell-deny-patterns.sh`; and the dead exec-approval manager (`pkg/security/execapproval.go`, `ExecApprovalManager`) — guard: `scripts/check-no-exec-approval-manager.sh`. Both wired into `scripts/guards.sh` per the existing pattern (`make lint-guards`).

## Consequences

### Security

- Removes a text-pattern layer that was bypassable by construction and, per its own in-code comment, largely redundant with the kernel sandbox — while making the one thing that actually held (the kernel boundary, where present) the explicit story, not an implicit one buried under five other checks.
- Closes issue #83's live "Additional" claim (PATH/look-alike-binary evasion against `EvaluateExec`) via D3's resolve-and-bind step.
- Makes issue #83's headline claim explicitly moot going forward by deleting the dead code it was filed against (D5), removing the risk of a future "fix" reintroducing it.
- Accepted, stated risk: on Windows and in agent-workspace-local destruction under God Mode/sandbox-less Auto, no text-pattern layer remains to slow a destructive command down. This is the same trade Claude Code and Codex made and documented; Omnipus now documents it the same way instead of relying on a bypassable regex to appear to cover it.

### Audit

Four new audit events, written by the existing `emitAudit`/`logDecision` path (`logDecision` retargeted per D3):

- **Mode change** — the new mode, the level it changed at (global / per-agent / per-chat), and the actor.
- **Pre-flight escalation** — that Auto routed a denial to the approval prompt, naming the rule/path that tripped and the operation requested.
- **Grant recorded** — scope (exact / prefix / path-widening), lifetime (session), and the resolved binary.
- **Approval decision** — allow-once / allow-with-grant / deny, per call.

### UX

- One mode selector (Ask/Auto/God) replaces reasoning about six independent, uncoordinated mechanisms with different defaults and different opt-in postures.
- Fewer prompts for the common case (Auto, the new fresh-install default) while keeping every prompt that does fire meaningful — it fires because the sandbox genuinely could not confine the action, not because a regex matched an incidental phrase.
- The prefix "always allow" option (D4) closes a real friction gap: today, an "Always Allow" grant on `npm run test -- --watch` does not cover `npm run test -- --coverage`, because the grant fingerprints the entire args object. A prefix grant, evaluated per D3's segment/binary-resolved shape, covers the family the user actually meant.
- **Accepted trade, stated explicitly (founder UI decision):** session grants are not shown or listable anywhere in the UI (D4). A user cannot review what they have silently approved for the rest of the current chat, and cannot revoke a specific grant mid-chat — the only way to undo an "Allow" is to end the chat and start a new one, which also clears every other grant made in that session, not just the one the user wanted to revoke. This is a real usability and auditability gap relative to Claude Code's `/permissions` (which lists every saved rule and its source file); it is accepted here because Omnipus's grants are session-only and already self-expire at chat end, unlike Claude Code's durable per-repository rules.
- **Headless turns are unchanged, and operators should plan for them.** A turn with no human attached (cron, heartbeats, a channel-originated chat with no composer) that resolves to Ask stalls to the existing `ToolApprovalTimeout` exactly as today's `ask` policy does. This ADR adds no headless-specific handling; since per-agent mode can only tighten (never loosen below a global Ask), an install whose global default is Ask has no way to exempt its headless agents, and the operator should set the global default accordingly (or accept the stall) rather than expect a prompt that has no human to answer.

### Migration

**Greenfield, no upgrade path** (founder ruling, established elsewhere in this repo — matching ADR-088's identical "Migration: NONE — greenfield (operator directive)" precedent). `sandbox.shell_deny_patterns`, `AgentShellPolicy.CustomDenyPatterns`, and `AgentShellPolicy.EnableDenyPatterns` are removed from the config schema, not deprecated and not migrated forward into D3's new rule format automatically. An operator who configured these on `release/v0.1.1` gets a clean config on next load with those keys silently dropped (unknown-field tolerance, matching the existing config-load behaviour for retired keys) — not an error, not a silent behavioural promise that the old patterns still apply under a new name. Operators who want equivalent coverage re-author it as D3 rules after upgrade. This is a real, stated behaviour change on upgrade, not a bug: it is the explicit trade of removing a bypassable mechanism rather than translating it forward. This tolerance is the config-*file* half only. The **agent REST write path is strict**: `pkg/gateway/rest_agents_create.go` decodes with `DisallowUnknownFields`, so a stale client still PUTting `shell_policy` after `AgentShellPolicy.yaml` is deleted gets a hard 400, not silent tolerance. Both halves are intentional — file config forgives unknown keys so an old config file still loads; the API rejects unknown fields so a stale client fails loudly.

### Interaction with ADR-077

ADR-077's two-layer model (global ceiling × per-agent override, strictest-wins, `bash` defaults `allow` at the ceiling) is the resolution mechanism this ADR sits on top of, not a mechanism it replaces. The three modes (D1) are the **selector** for what the `bash` ceiling resolves to:

- **Ask** sets the effective `bash` policy to `"ask"` — the existing ADR-077 verdict, unchanged mechanically; `CheckGrantOrRequestApproval` fires exactly as it does today.
- **Auto** sets the effective `bash` policy to `"allow"` (today's shipped ceiling default, unchanged), **plus new pre-flight behaviour**: before the command runs, it is evaluated against the same single-source `fspolicy.FSPolicy` the kernel policy is derived from (D7); when that evaluation says the command needs more than the derived kernel policy grants, it routes to the same approval flow `"ask"` uses instead of running unconfined or hard-failing. This is genuinely new engine work — not a config flip — comparable in scope to ADR-088 D3's own "this is a BUILD, not a flip" framing for an analogous prior change.
- **God Mode** floors the global ceiling at `allow` for every tool — this is exactly today's already-implemented behaviour (`GodMode` field), unchanged by this ADR except for the comment fix (D6) and the explicit statement that operator/per-agent `deny` rules under D3 still apply on top of that floor (D1), consistent with the as-is finding that per-agent overrides already survive God Mode today.

Per-agent tightening (an agent with an explicit `bash: deny` or `bash: ask`, e.g. Judge, verifier agents) continues to resolve under ADR-077's existing strictest-wins merge regardless of mode — a mode is a ceiling-level default selector, not a bypass of per-agent tightening. The mode's own scope question is now resolved (D1, founder UI decision): global default (Settings → Security) sets the ADR-077 ceiling-equivalent; a per-agent mode override (Tools & Permissions) may only tighten it, using the identical strictest-wins semantics ADR-077 already defines for per-agent tool-policy overrides generally; a per-chat quick switch may tighten further still, for that one chat, as a session-scoped modifier applied after the two-layer resolution rather than as a third policy-map layer (see D1's explicit note on Hard Constraint #6).

## UI presentation (founder-decided, 2026-09-23)

- **Mode choice at three levels, tighten-only below the global default** (D1): global default in Settings → Security (`SecuritySection.tsx`, `GodModeControl.tsx` — God Mode is global-only and keeps its existing password step-up); a per-agent setting in Tools & Permissions (`ToolsAndPermissions.tsx`) that can only tighten (e.g. Auto → Ask); a per-chat quick switch in the chat composer ("`[Auto ▾]`") that can also only tighten, for that chat only. Loosening below the global default is not offered at any lower level — the control itself should not present a looser option, not merely reject one on submit — and it is enforced server-side at the write endpoint (4xx on a loosening write); hiding the option is defence-in-depth (D1).
- **Approval dialog** (`ToolApprovalModal.tsx`): three buttons — **[Deny] [Allow once] [Allow]** — with a scope choice above the buttons, **always visible and enabled only when `Allow` is selected** (visibility never depends on keyboard/mouse focus, so both input modes see the same dialog): **"(•) exactly this command"** (default) or **"( ) anything starting with `<suggested prefix>`"**. Shows the command and its working folder; for a chained command, lists each part separately rather than one raw compound string (D4).
- **Session grants are not shown anywhere** — no list, no chip, no management surface. They end with the chat; there is no mid-chat revoke (D4, Consequences → UX). Do not build a grants list as a "nice to have" — it is an explicit non-feature, not an oversight to fill in later.
- **Status signals:**
  - **Chat header mode badge** — shows the effective mode for the current chat. When Auto has no kernel sandbox active, the badge reads **"Auto → Ask"** with an info tooltip: *"No kernel sandbox on this system — commands will ask."* The badge must reflect the resolved, effective mode (after the three-level tighten-only merge), not just whichever level the user last touched.
  - **God Mode banner** — a persistent, red, app-wide banner ("**God Mode is on — agents run without sandbox or approvals**" + **[Turn off]**) shown in `AppShell` for as long as God Mode is on, everywhere in the app, not scoped to one chat — consistent with God Mode being global-only (D1).
- **Contract requirement:** the badge's data (effective mode, whether a kernel sandbox is actually active) must come from a contract-defined status field, per Hard Constraint #8 — not inferred client-side from separately-fetched config and platform-detection logic. See Implementation outline step 1.
- **Design system:** every new/changed component above is subject to `.claude/skills/omnipus-design-system/SKILL.md` — load it before touching `ToolApprovalModal.tsx`, the composer, the chat header, or `AppShell`; the banner and mode badge in particular must use the design system's existing tokens for a persistent alert state rather than inventing new colors/spacing for God Mode's red banner.

## Implementation outline

Ordered, small steps; contract-first where a wire format changes (CLAUDE.md Hard Constraint #8 — mode selection and grant-scope changes that cross the gateway/SPA boundary need a schema in `contracts/` before code, per the existing `openapi.yaml`/`asyncapi.yaml`/`components/schemas/` process).

1. **Schema first:** add/update `contracts/components/schemas/` for (a) the mode-status field the chat header badge and God Mode banner read (effective mode, whether a kernel sandbox is actually active), (b) the mode-selector write shape at all three levels, (c) the redesigned three-button/scope-choice approval response, (d) the deletions in the Removal inventory (`AgentShellPolicy.yaml`, `ExecAllowlist.yaml` removed; `SandboxConfig.yaml`/`SandboxConfigUpdate.yaml`/`AgentUpdateRequest.yaml` edited). Reference from `openapi.yaml`/`asyncapi.yaml`, regenerate via `scripts/gen-contracts.sh`, commit the generated diff — before any handler or component code, per Hard Constraint #8's 5-step process.
2. Design and land the D3 unified rule shape (binary resolution + segment scope + exact/prefix) as a shared type consumed by the new operator-rule resolver, `ApprovalGrantStore`, and the folded-in exec-allowlist logic; wire `splitShellSegments`/`shellCommandHead` as the segment source for rule matching (new caller, existing function). Include the resolved-binary-before-match step here directly — it is the mechanism that both closes the live PATH/look-alike gap (issue #83's "Additional" claim) and replaces `Evaluator.EvaluateExec`, so building it once as part of D3 avoids a throwaway fix-in-place pass on the file this step deletes in step 7.
3. Wire the three modes (D1) into the `bash` ceiling resolution path (`pkg/agent/loop_policy.go`), including the three-level tighten-only merge (global → per-agent → per-chat, the last as a session-scoped modifier per D1's explicit note, not a third policy-map layer). Add Auto's pre-flight evaluator (D7) as a third consumer of `pkg/tools/resolvepath.go::ResolveTurnFSPolicy`'s already-resolved `fspolicy.FSPolicy`, called before `shell.go::executeRun` spawns the command, comparing its verdict against what `sandbox.DeriveKernelPolicy` would grant for the same value rather than re-deriving policy from config.
4. Land D7's anti-drift lock test (`{path, operation}` matrix, cross-platform Go-level comparison against `DeriveKernelPolicy`'s output) alongside the pre-flight evaluator, before it ships — the test is what makes the "one source of truth" claim enforced rather than aspirational.
5. Build the UI surfaces against the step-1 contract: `SecuritySection.tsx` (global mode selector, God Mode entry point unchanged via `GodModeControl.tsx`), `ToolsAndPermissions.tsx` (per-agent tighten-only override), the chat composer (`[Auto ▾]` quick switch) and chat header (mode badge, "Auto → Ask" + tooltip), `AppShell` (persistent red God Mode banner + Turn off), `ToolApprovalModal.tsx` + `BashApprovalPreview.tsx` (three-button redesign, scope radio, per-segment chained-command list). Design-system rules apply throughout (`.claude/skills/omnipus-design-system/SKILL.md`) — load it before touching any of the above.
6. Delete `defaultDenyPatterns`, `secretGuardPatterns`, `applyDenyPatterns`/`compileDenyPatterns`/`denyPatternMessage`, the per-agent and global deny-pattern config fields, `AgentShellPolicy`, and their wiring (D2, Removal inventory item 1) — after step 3 lands, so Auto's pre-flight path is the compensating control before the text-pattern layer that partially overlapped it disappears.
7. Delete `pkg/policy/evaluator.go` in full and retire `PolicyAuditor.EvaluateExec` (Removal inventory item 3) — after step 2 lands, since step 2 is what replaces its decision logic; retarget `PolicyAuditor.logDecision` to the D3 engine's verdict; delete `ExecConfig.AllowedBinaries` and `ExecAllowlist.yaml`.
8. Add the scope-choice (exact/prefix) grant option to `ApprovalGrantStore` and the redesigned approval-dialog wire contract (D4) — consumes the step-1 schema.
9. Delete `pkg/security/execapproval.go` and its test file (D5, Removal inventory item 2).
10. Fix the `GodMode` field comment in `pkg/config/sandbox.go` (D6).
11. Resolve Open Question 1 (`ExecConfig.Approval`/`rest_exec.go` reachability) and act on the finding — delete alongside step 9 if dead, or fold into step 3's mode resolution if live.
12. Config-schema cleanup: confirm unknown-field tolerance on load for a `release/v0.1.1`-shaped config carrying every removed key (no migration, per Consequences).
13. Triage the docs list in Removal inventory item 5 — mark each reference historical or update it.
14. Wire the two CI guards (`scripts/check-no-shell-deny-patterns.sh`, `scripts/check-no-exec-approval-manager.sh`, matching ADR-036/ADR-077/ADR-088 precedent) into `scripts/guards.sh`; add both to CLAUDE.md's "Retired surfaces — do NOT reintroduce" list (Removal inventory).

## Test plan

- Segment splitting and binary resolution (D3): compound commands, mid-segment substitution, look-alike-binary PATH evasion (regression test directly reproducing issue #83's "Additional" claim, asserting it now fails to auto-approve).
- Mode resolution (D1): Ask/Auto/God Mode → correct `bash` ceiling value; Auto with no kernel sandbox behaves identically to Ask; God Mode floors the ceiling but does not defeat an explicit per-agent or D3 operator `deny`.
- Auto pre-flight evaluation (D7): a command the derived kernel policy would deny routes to the approval flow *before* it runs, not after; a command the policy would grant runs with no prompt; on Windows/app-level-fallback/Landlock-degraded hosts (no kernel policy to check against), Auto behaves like Ask, matching the no-sandbox case.
- Auto escalation approval (D7/C-1): approving an escalation widens the turn's filesystem policy for the named `{path, operation}` only — read, write, or both, nothing broader; both the app-layer guard and the derived kernel policy reflect the widened value (the anti-drift lock test covers the widened value too); the next identical command in the session does not re-prompt; the widening does not leak past session close.
- Mode delegation (D1/M-3): the session modifier inherits to a delegated subagent session; the delegate resolves the tightest of (parent modifier, own override); a chat tightened to Ask stays Ask under a delegated agent with no override.
- Server-side tighten-only (D1/M-2): the PUT/PATCH handler rejects a loosening per-agent or per-chat write with 4xx — not silently clamped, not silently accepted; the check reads the global default at write time.
- God Mode + operator `deny` (D1/M-6): a global D3 `deny` rule + God Mode → the command is still refused, proving the D3 engine is enforced downstream of the policy floor.
- Audit events (M-7): mode change (level + actor), pre-flight escalation (the rule/path that tripped + operation), grant recorded (scope + lifetime + resolved binary), and approval decision each fire with their payload fields, in all three modes.
- D7's anti-drift lock test itself: a deliberately mismatched pre-flight/kernel-policy pair fails the test (proves the test can actually catch divergence, not just pass); the cross-platform half runs on every CI OS, the live-Landlock half runs only on the Linux CI leg.
- D7's honest-gap path: a synthetic case where pre-flight predicts "allowed" but the kernel denies at runtime (simulating a missed symlink/runtime-constructed-path case) fails the command with a clear error and does not silently proceed unconstrained.
- Suggested-prefix algorithm (D4): the worked examples (`npm run test -- --watch` → `npm run test`; `git diff src/` → `git diff`; `curl https://x.com` → exact-only, no prefix offered) plus a leading env-var assignment case and a wrapper case (`sudo apt install …` → prefix `sudo apt install`, not `apt install`).
- Always-Allow grants (D4): exact-command grant behaves as today; new prefix grant matches a family of commands and does not match outside the declared prefix; session scope and in-memory-only lifetime unchanged.
- Deletion regressions: `defaultDenyPatterns`/`secretGuardPatterns` removal does not regress the surviving guards (substitution guard, path/secret-exclusion guard) — each guard's own existing test suite continues to pass on the reduced guard chain.
- Audit/injection-guard/rate-limiting invariance: confirm these three fire identically across all three modes, including God Mode (regression against the as-is finding that they are not currently gated on `godMode`).
- Config load: a `release/v0.1.1`-shaped config with the retired deny-pattern and exec-allowlist fields loads cleanly with no error and no silent behavioural promise that the old patterns still apply.
- Fresh-install default: confirm Auto is the shipped default mode and resolves `bash` to `allow` at the ceiling, matching today's shipped default value.
- Three-level mode merge (D1): global default sets the floor; a per-agent override that attempts to *loosen* below the global default is rejected (not silently clamped, not silently accepted); a per-chat quick switch that attempts to loosen is rejected the same way; a per-chat tighten applies for that chat only and does not leak to a second chat with the same agent.
- Approval dialog (D4): `Allow once` records no grant (a second identical call re-prompts); `Allow` with "exactly this command" behaves as today's exact-fingerprint grant; `Allow` with the prefix option matches a family of commands sharing the prefix and does not match outside it; a chained command's dialog lists each part.
- Status signals: chat header badge reflects the resolved effective mode, not the last-touched level; the badge reads "Auto → Ask" with the info tooltip specifically when Auto is selected but no kernel sandbox is active (not merely whenever Auto is selected); the God Mode banner appears in `AppShell` app-wide (not scoped to one chat) the instant `GodMode` turns true and its `[Turn off]` control actually clears it.
- **Removal acceptance checks (grep-based CI guards, mandatory per the Removal inventory):**
  - `scripts/check-no-shell-deny-patterns.sh` fails the build if `defaultDenyPatterns`, `secretGuardPatterns`, `applyDenyPatterns`, `compileDenyPatterns`, `validateShellDenyPatterns`, `ShellDenyPatterns`, `CustomDenyPatterns`, `EnableDenyPatterns`, `AgentShellPolicy`, `shell_deny_patterns`, `custom_deny_patterns`, or `enable_deny_patterns` reappear as definitions or non-comment references anywhere in `pkg/`, `cmd/`, `src/`, `contracts/`.
  - `scripts/check-no-exec-approval-manager.sh` fails the build if `ExecApprovalManager`, `PersistPattern`, `matchAllowlistPattern`, or `MatchExecAllowlist` reappear as definitions or non-comment references in `pkg/`.
  - A third check (fold into either script or a new one) fails the build if `Evaluator.EvaluateExec`, `policy.MatchGlob`, `policy.FirstToken`, `AllowedBinaries`/`allowed_binaries`, `HandleExecAllowlist`, `sanitiseAllowlist`, `ExecAllowlistSection`, `fetchExecAllowlist`, `updateExecAllowlist`, or the literal path `exec-allowlist` reappear, confirming the exec-allowlist fold-in (Removal inventory item 3, including its REST and SPA surfaces) is not silently reintroduced.
  - All three wired into `.github/workflows/pr.yml`, the CI worker's `lint` gate, and `make lint-guards`, matching the existing ADR-036/ADR-077/ADR-088 guard pattern.
- `ExecApprovalManager` deletion specifically: confirm zero remaining references (the guard script above is the regression test; run it once by hand against `838d8092c`'s pre-deletion tree to confirm it actually fires, not just that it passes on the post-deletion tree — a guard that never fires on the thing it's meant to catch is not verified).

## Open questions

*(Resolved by founder decisions of 2026-09-23, no longer open: mode selector scope — three levels, tighten-only, D1; exec allowlist's relationship to D3 — folded in, Removal inventory item 3; Auto's detection approach — pre-flight evaluation against the single-source `fspolicy.FSPolicy`, D7; "exact" grant scope — command + cwd, D4; per-segment prefix grants — one rule per part, D4; suggested-prefix algorithm — D4.)*

1. **`config.ExecConfig.Approval` (`tools.exec.approval`) and the untyped `SecuritySection` keys** — the review's grep refined this. `Tools.Exec.Approval` has exactly three references: the dead `execapproval.go` prompt, the REST echo (`rest_exec.go`'s GET), and a copy into `policy.ExecPolicy.Approval` (`loop_construct.go`) that no consumer reads (`evaluator.go` never touches `.Approval`). It is reachable-but-unenforced — operator-settable, echoed back, changes nothing — which supports deletion alongside item 3 (and resolves the handler/route question in M-1). Separately, `SecuritySection.tsx` also writes `config.security.policy_mode` and `config.security.exec_approval` (a dead `exec_approval: auto|ask|deny` dropdown on the very screen this ADR redesigns) — keys with **no typed Go field** (`SecurityConfig` in `pkg/config/platform_auth.go` carries only `PlatformAuth`). The new Ask/Auto/God vocabulary collides with that dropdown. Implementation must confirm both the `tools.exec.approval` field and the untyped `policy_mode`/`exec_approval` keys are dead before deleting them alongside item 3, or fold them into D1's mode resolution if live.
2. **Single-value threading within one turn (D7).** Today, `guardCommand` and `turnKernelPolicy` each call `ResolveTurnFSPolicy` independently rather than sharing one resolved value; D7 adds a third independent call site for the pre-flight evaluator. Under normal conditions all three resolve identically, but nothing today structurally guarantees the same `fspolicy.FSPolicy` instance backs the guard, the kernel policy, and the pre-flight verdict for one turn if mount/policy state could change between calls within it. Whether this needs a per-turn cached resolution (one call, reused by all three) or is provably safe as three independent calls is an implementation-design question D7 does not resolve.

## Affected components

- **Backend:** `pkg/tools/shell.go` (delete `defaultDenyPatterns`/`secretGuardPatterns`, `ExecToolDeps.GlobalShellDenyPatterns`/`AgentShellPolicy`), `pkg/tools/shell_guard.go` (delete `applyDenyPatterns`/`compileDenyPatterns`/`denyPatternMessage`; keep `lowerASCII`), `pkg/tools/shell_path_guard.go` (survives; denial disposition made mode-sensitive, D2), `pkg/tools/shell_subst_guard.go` (new caller for `splitShellSegments`/`shellCommandHead` from the D3 rule matcher; guard logic itself unchanged), `pkg/policy/evaluator.go` (deleted in full — folded into D3), `pkg/policy/auditor.go` (`PolicyAuditor.EvaluateExec` retired, `logDecision` retargeted), `pkg/security/approvalgrants.go` (prefix-grant + path-widening record kinds), `pkg/security/execapproval.go` + `execapproval_test.go` (deleted), `pkg/config/sandbox.go` (`GodMode` comment fix, `ShellDenyPatterns` field removed), `pkg/config/config.go` (`AgentShellPolicy` type removed, `ExecConfig.AllowedBinaries` removed, new `CommandRules`), `pkg/gateway/sandbox_config_validation.go` + `pkg/gateway/rest_sandbox_config.go` (delete `validateShellDenyPatterns` + caller), `pkg/gateway/rest_exec.go` + `pkg/gateway/rest.go` (delete `HandleExecAllowlist`/`sanitiseAllowlist` + route), `pkg/agent/loop_wire.go` (drop the per-agent deny-pattern god-mode carve-out, now moot), `pkg/agent/loop_construct.go` (drop the `policy.NewEvaluator` wiring), `pkg/agent/loop_policy.go` (three-level mode resolution, Auto escalation wiring).
- **Frontend:** `src/components/agents/ToolApprovalModal.tsx` and `src/components/agents/approvalPreviews/BashApprovalPreview.tsx` (three-button/scope-choice redesign, per-segment display), `src/components/agents/ShellDenyPatternsEditor.tsx` + test (deleted), `src/components/settings/ExecAllowlistSection.tsx` + test (deleted), `src/components/settings/SecuritySection.tsx` (global mode selector, deny-pattern UI removed, allowlist section removed), `src/lib/api/security.ts` (drop `fetchExecAllowlist`/`updateExecAllowlist`), `src/components/settings/GodModeControl.tsx` (unchanged mechanism, banner is new/separate in `AppShell`), `src/components/agents/ToolsAndPermissions.tsx` (per-agent tighten-only mode override), the chat composer (per-chat quick switch, mode badge) and chat header (mode badge, sandbox-status tooltip), `AppShell` (persistent God Mode banner) — see Implementation outline for the full component list.
- **Contracts:** schema deletions (`AgentShellPolicy.yaml`, `ExecAllowlist.yaml`) and edits (`SandboxConfig.yaml`, `SandboxConfigUpdate.yaml`, `AgentUpdateRequest.yaml`) for the removed fields, plus the `/security/exec-allowlist` path removal in `openapi.yaml` (Removal inventory); new schema additions for the mode-status field the chat header badge reads and the mode-selector/grant-scope fields the three UI levels write — step 1 of the implementation outline.
- **Variants:** identical decision logic across Open Source / Desktop / SaaS — this is agent-loop and config logic, platform-independent; the *degradation* behaviour (Auto = Ask where the D1 per-platform predicate says no kernel sandbox is active) already differs by platform today — Windows: never sandboxed → Ask; macOS: Seatbelt → Auto; Linux: Landlock → Auto, degraded/fallback → Ask — and that existing asymmetry is unchanged by this ADR.

## Alternatives considered

**Keep the block list as a lower-priority, always-off-by-default layer.** Rejected — a disabled-by-default mechanism that operators can still turn on reintroduces the same false sense of coverage this ADR removes, and every vendor surveyed rejected this shape outright rather than defaulting it off.

**Migrate `ShellDenyPatterns`/`CustomDenyPatterns` forward into D3's rule format automatically.** Rejected under the greenfield/no-upgrade-path ruling — an automatic translation of a bypassable regex into a "real" rule would misrepresent the new rule's actual coverage (a regex pattern and a resolved-binary/segment rule are not equivalent, so a 1:1 translation is not honest).

**Catch the kernel sandbox's actual denial at runtime and convert it into an approval prompt (Codex's `Escalate` design), instead of a pre-flight check.** Considered first, then rejected by the founder in favor of pre-flight evaluation (D7). Reasons: on Linux, a Landlock denial is not a structured event the gateway can observe directly (it lands inside the child as a bare `EACCES`/`EPERM`), so reliable detection would need new infrastructure (an audit-log reader, seccomp user-notify, or an exec wrapper) with no shipped default of that kind in the codebase today; a command can already have performed real side effects before hitting the denied action, making "approve and retry" unsafe in general; and macOS Seatbelt and Windows have no comparable structured denial signal at all, so the runtime-catch design would degrade to the same Ask fallback pre-flight already uses on those platforms, without pre-flight's benefit of never starting a command that was always going to be denied.
