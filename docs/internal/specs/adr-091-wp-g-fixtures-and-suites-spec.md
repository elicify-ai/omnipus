# ADR-091 WP-G — Shared fixtures, test classification, cross-package suites

- **Decision record:** [ADR-091](../architecture/ADR-091-steered-sessions-replace-subagents.md) §10, §2.4
- **Landing order:** [adr-091-landing-order.md](adr-091-landing-order.md) — publishes I-7 first; runs alongside all packages
- **Owner files:** landing order §3, row G
- **Status:** Draft rev 2 (consolidated after three grills; supersedes every earlier sentence of rev 1)
- **Founder decision (round 4):** implementers write their own unit tests with the `test-driven-development` skill. This package owns only what is shared.
- **Founder decision (round 7):** the classification table is this package's first deliverable at CP-0, not a pre-grill task.

## Summary

Every package needs the same two things to prove its acceptance criteria: a real, store-backed delegation tree three levels deep, and a way to record everything that tries to reach a user so a test can assert "nothing did" — and prove the boundary was actually exercised. This package builds both first, decides what happens to the 49 existing test files that pin today's sub-agent behaviour, and writes the suites that only make sense across packages — the end-to-end reachability check above all.

## Existing codebase context

*Source inspection at `364290cb5`.*

| Item | Today | Change |
|---|---|---|
| Test helpers | `pkg/agent/testutil/` (scenario provider, `gateway_harness.go::RegisterGatewayRunner` injection pattern) | gains `DelegationTree`, `RecordingOutbound` |
| Existing mechanism tests | 47 files in the glob `pkg/agent/subturn*_test.go`, `pkg/tools/delegate*_test.go`, `pkg/tools/message_parent*_test.go`, plus the two containment tests below = **49** | classified retain / update / delete |
| Containment tests | `pkg/agent/system_turn_tool_output_test.go` (#766), `pkg/agent/async_child_publication_test.go` (#783) | assertions and controls **retained unchanged**; the second's setup constructs the deleted ring (`spawnPublicationTestChild` → `&ephemeralSessionStore{}`) and is moved to the persisted-store fixture by WP-A |
| In-package tools tests | `pkg/tools/delegate*_test.go` are `package tools` | those that need I-7 become **external-package tests** (`package tools_test`), because a `package tools` test importing `pkg/agent/testutil` recreates the import cycle |
| Cross-package suites | `tests/` | gains the ADR-091 E2E suite |
| CI vitest groups | `.github/workflows/pr.yml` | new SPA tests must match an existing group pattern (`scripts/check-vitest-coverage.mjs` is the tripwire) |

## Deliverables

### G-1 — `DelegationTree(t, deps steer.Deps, depth int) Tree` (I-7)

Builds R → A → B (→ C) with real sessions **through the injected I-2 launcher** (until WP-A publishes I-2 at CP-0, through the store directly with the same record shape), real lifecycle records with edges — including R's own `ordinary_root` record (round 9) — each session under a distinct agent profile, distinct workspaces available on request. The fixture holds only `pkg/steer` interfaces (`steer.Deps{Launcher, Canceller, Deliverer, Classifier, LifecycleStore, SessionStore, BootHook}`); every hook maps to a published operation: `Reenter(B)` → `Deliverer.Deliver` of a `handback` from B's child; `QueueWake(B, gen)` → `Deliver` with B's turn held; `Stop(node)` → `Canceller.CancelSubtree`; `Revive(node)` → `Canceller.Revive`; `CorruptRecord(node, invariant)` / `DeleteRecord(node)` / `DeleteEdge(node)` → the lifecycle store; `Crash()` → close every store without flushing; `Reboot()` → reopen and run `BootHook` (the same function `gateway_boot.go` runs).

### G-2 — `RecordingOutbound(t)` (I-7)

Implements `steer.BoundaryObserver` — every boundary calls `Observe(boundary, session, audience)` before acting on the audience decision, so "exercised and blocked" is distinguishable from "never exercised" — and wraps every sink in the boundary inventory (landing order §6): the bus (`PublishOutbound`, `PublishOutboundMedia`), `SendMedia`, the external-channel stream adapters' `Write`, the WS `send`, the `message` tool's send, the task result notification and the question-card broadcast. Provides `AssertNothingTo(address)`, `AssertReceived(session, kind)` (the control) and `AssertBoundaryInvoked(name)`.

### G-3 — Classification of existing tests

Every one of the 49 files gets a row: **retain** (protects behaviour ADR-091 keeps — target identity, ancestor authority, parked/respond transitions, cancellation of live descendants), **update** (asserts an identity source that changes — inherited address → edge; `ParentDurableKey` → edge; ring setup → real store), or **delete** (asserts wait-inline, the ring, per-site booleans, the sibling notifier). The row names the replacing test where one exists. The classification is the appendix of this spec, filled in by this package at CP-0 before any other package deletes a test.

### G-4 — Cross-package suites

| Suite | Asserts |
|---|---|
| `TestE2E_ThreeLevelDelegation_NoLeak` | AC-3 across all twelve boundaries with A, B and C, first run and re-entry; every boundary invoked; controls present |
| `TestE2E_StopReachesReenteredChild` | AC-8 through the real Stop surface |
| `TestE2E_StopSurvivesRestart` | AC-8, Q17: Stop, `Reboot()`, nothing runs; a newer instruction revives |
| `TestE2E_CompletionWakesPerChild` | D3 through the real notifier; `handback` per child |
| `TestE2E_Restart_ParentToldOnce` | §7: crash between persist and wake → one turn |
| `TestE2E_TaskChildInSidePanel` | a `create_task` child appears in the parent's transcript events and, via replay, in the row |
| Playwright `steered-session-reachability.spec.ts` | AC-12: side panel row with status → open → live stream → typed steer → parent chat gains nothing beyond the `delegate` line → reload → row restored |

## Acceptance criteria

1. **Given** any package's test, **When** it needs a tree or a recorder, **Then** I-7 provides it without that package building its own, and a `pkg/tools` test uses it from `package tools_test`.
2. **Given** the 49 files, **When** WP-A/B/C delete or update tests, **Then** each change cites a classification row.
3. **Given** the E2E suite, **When** run against the merged branch, **Then** AC-3, AC-8, AC-12 and §7 pass through real surfaces, not mocks.
4. **Given** a mocked provider that returns a verdict without tool calls, **When** the leak suite runs, **Then** the control assertion fails — proving the suite cannot pass vacuously.
5. **Given** the leak suite, **When** any one boundary is not exercised, **Then** `AssertBoundaryInvoked` fails for it.

## Machine-verifiable constraints

| Check | Expected |
|---|---|
| Fixture realism | `DelegationTree` sessions exist on disk under the test data dir with valid edges, and R has an `ordinary_root` record |
| Recorder coverage | every sink in the boundary inventory passes through the recorder in test builds; `AssertBoundaryInvoked` has one name per boundary |
| No import cycle | `go vet ./pkg/tools/...` and `go vet ./pkg/agent/testutil/...` pass with the fixture present |
| Classification complete | 49 rows, each retain/update/delete with a reason |
| Vacuity guard | the control assertion fails when the child made 0 tool calls; `AssertBoundaryInvoked` fails for a skipped boundary |
| CI grouping | every new vitest file matches a `pr.yml` group |

## BDD scenarios

```gherkin
Feature: Shared fixtures and cross-package suites

  # Happy Path — Traces to: AC 1
  Scenario: A tools test uses the tree from an external package
    Given a test in package tools_test
    When it builds DelegationTree(t, launcher, 3)
    Then R, A, B and C exist on disk with valid edges and R has an ordinary_root record

  # Error Path — Traces to: AC 4, 5
  Scenario: The leak suite cannot pass vacuously
    Given a provider stub that returns a verdict without tool calls
    When the leak suite runs
    Then the control assertion fails
    And AssertBoundaryInvoked fails for every boundary the child never reached

  # Happy Path — Traces to: AC 3
  Scenario: Stop survives a restart end to end
    Given a three-level tree and Stop pressed on R
    When Reboot() runs
    Then no session under R starts
    When a newer instruction is written into B
    Then B runs as a new generation

  # Error Path — Traces to: FR-G-004
  Scenario: A new SPA test outside every CI group is caught
    Given a new vitest file whose path matches no group in pr.yml
    When scripts/check-vitest-coverage.mjs runs
    Then it fails naming the file
```

## TDD plan

| Order | Test | Level | Traces to |
|---|---|---|---|
| 1 | `TestDelegationTree_BuildsValidEdges_AndRootRecord` | Unit | AC 1 |
| 2 | `TestDelegationTree_UsableFromToolsTest` (in `package tools_test`) | Unit | AC 1 |
| 3 | `TestRecordingOutbound_CapturesAllSinks` | Unit | AC 1 |
| 4 | `TestRecordingOutbound_ControlFailsWhenChildIdle` | Unit | AC 4 |
| 5 | `TestRecordingOutbound_BoundaryInvokedFailsWhenSkipped` | Unit | AC 5 |
| 6 | the six cross-package Go suites | E2E | AC 3 |
| 7 | Playwright reachability | E2E | AC 3 |

## Functional requirements

| ID | Requirement |
|---|---|
| FR-G-001 | I-7 MUST be published before any package asserts AC-3 or AC-8, and MUST be usable from `package tools_test` without an import cycle. |
| FR-G-002 | Every one of the 49 existing mechanism test files MUST be classified before any is deleted or updated. |
| FR-G-003 | Cross-package suites MUST exercise real surfaces, MUST include a vacuity control, and MUST assert every boundary was invoked. |
| FR-G-004 | New SPA tests MUST land in an existing CI vitest group. |

## Success criteria

| ID | Criterion |
|---|---|
| SC-G-1 | 49 / 49 rows classified. |
| SC-G-2 | E2E suites green on the merged branch; Playwright reachability recorded with a screenshot. |

## Traceability

| Requirement | Acceptance | Scenarios | Tests |
|---|---|---|---|
| FR-G-001 | AC 1 | tools test uses the tree | 1, 2, 3 |
| FR-G-002 | AC 2 | — (a table, checked by review at CP-0) | appendix |
| FR-G-003 | AC 3, 4, 5 | cannot pass vacuously; Stop survives restart | 4, 5, 6, 7 |
| FR-G-004 | *as a maintainer, I want every new SPA test to run in CI* | new test outside every group is caught | the `check-vitest-coverage.mjs` tripwire, exercised by scenario |

## Appendix — classification table (completed at CP-0)

| File | Class | Reason | Replaced by |
|---|---|---|---|
| `pkg/agent/subturn_target_identity_test.go` | **update (setup only)** | All 9 functions (`TestSpawnSubTurn_TargetIdentity_DispatchesExternalCLIFromTargetConfig`, `_PropagatesFullTargetConfig`, `TestSpawnSubTurn_NativeDispatch_AdoptsFullTargetIdentityIncludingModel`, `_AdoptsTargetToolPolicy`, `_AdoptsTargetWorkspaceForFileTools`, `TestSpawnSubTurn_ProviderPoolCopiedFromParent`, `_ConcurrentModelSwitchRace`, `_UnresolvedTargetAborts`, `_SelfDelegationNoFallbackWarning`) construct the deleted ring (`session: &ephemeralSessionStore{}`, D1) and call `spawnSubTurn` via the deleted synchronous convenience path (`Async: false` on every literal, D4/D10) purely to get an immediately-inspectable result. The ADR-032 assertions themselves (target's workspace/model/ID/tool-policy/MaxIterations/CLIPath/EnvOverrides win, parent's Model/Candidates/Provider quad kept) are explicitly unchanged by ADR-091 ("What is not changing") | the I-2 launcher (`Launch`+`Dispatch`) replacing `createChildSession`; setup must wait for async completion instead of relying on a blocking call |
| `pkg/agent/system_turn_tool_output_test.go` | retain, unmodified | Both functions (`TestProcessSystemMessage_SuppressesToolOutputButDeliversNarration`, `_SuppressesAsyncToolOutputButDeliversNarration`) drive `al.processSystemMessage` directly and assert only that raw tool output is suppressed while narration survives on a system-triggered wake — no ring, no `ParentDurableKey`, no `SubTurnConfig.Async` (the one `Async: true` hit is `tools.ToolResult.Async`, an unrelated field on a callback-style test tool, confirmed by full read). Confirmed as the #766 containment test the WP-G spec's own body text already names | — |
| `pkg/agent/async_child_publication_test.go` | split: **update (setup only)** for `TestDelegatedChild_GenericAsyncForUserDoesNotPublishOnParentRoute`, `TestDelegatedChild_ToolPreviewDoesNotPublishTopLevelOnExternalChannel` (both route through `spawnPublicationTestChild`, which constructs `&ephemeralSessionStore{}` and calls `spawnSubTurn` with Async omitted — D1/D10, moves to the persisted-store fixture); retain for `TestInteractiveRoot_GenericAsyncForUserStillPublishes`, `TestInteractiveRoot_ToolPreviewStillPublishesOnExternalChannel`, `TestInternalSessions_AsyncForUserDoesNotPublishTopLevel` (these call `al.ProcessDirectWithChannel`/`al.processTaskDirect`/`al.dispatchVerifierTurn` directly — no ring, no delegation mechanism at all) | — |
| `pkg/agent/subturn_adr057_test.go` | **update** | All 3 functions (`TestU7_ParentChildEdge_ChildCountBecomesNonZero`, `_OwnerCopiedVerbatim`, `TestU7_RoutingSessionID_ChildInheritsParentVerbatim`) call `spawnSubTurn` via `createChildSession` (deleted, replaced by the I-2 launcher) with `Async` omitted (implicit sync convenience, D4/D10). The routing test's own assertion — "routingSessionID is the ONE field that stays inherited verbatim from the parent" — is the exact sentence D2 corrects: post-ADR-091 `routingSessionID` is **derived from the edge's verified cascade root** (D2/D8), not copied from `parentTS.routingSessionID` by direct assignment; at this test's depth-1 nesting the two coincide, but the derivation mechanism changes | I-2 launcher; D2 edge-derived root replaces the direct-copy assignment |
| `pkg/agent/subturn_ask_deny_test.go` | **update (setup only)** | Both functions share `askDenyFixture`, which the file's own comment says is "the same entry point `DelegateTool.executeSync` reaches through `SubTurnSpawner`" and sets `Async: false` explicitly. The behavior under test (`AutoDenyAsk` inherited by a headless delegated child vs. an interactive one) is orthogonal to delivery mode and remains real under ADR-091 (scheduled/headless delegation still exists); the setup must move off the deleted synchronous convenience call to the launcher + wait for completion | I-2 launcher |
| `pkg/agent/subturn_awaited_cancel_test.go` | **delete** | `TestSpawnSubTurn_AwaitedSyncCancel_CascadesAndRecordsDescendants`'s own doc comment: "drives the REAL await (async=false) delegation shape... with the exact `SubTurnConfig{Async: false}`... `DelegateTool.executeSync` passes." This tests wait-inline cancellation AS A FEATURE, not merely uses it as scaffolding. D4 deletes `executeSync`/`async:false` wholesale | its async sibling coverage (`subturn_delegate_nesting_test.go`'s async-mode tests, `TestSpawnSubTurn_ParentHardAbort_RecordsInterruptedStatus`) |
| `pkg/agent/subturn_cancel_browser_test.go` | retain | Both functions (`TestCancel_CascadesWithoutClosingBrowser`, `TestCancel_ChildWithItsOwnRoutingID_IsNotReachedFromTheChat`) hand-construct `turnState` literals directly (no `ephemeralSessionStore`, no `SubTurnConfig`, no `ParentDurableKey`) and test that a chat-wide Stop reaches a child via `routingSessionID` without closing the shared workspace browser — the routingSessionID/browser-ownership properties are unaffected by D1–D10 | — |
| `pkg/agent/subturn_cancel_status_test.go` | **update (setup only)** | Both functions (`TestSpawnSubTurn_ParentHardAbort_RecordsInterruptedStatus`, `_ExplicitCancelViaRequestCancel_RecordsCancelledAndReason`) construct `session: newEphemeralSession(nil)` on the parent turnState (D1 ring, deleted) and dispatch the child with `Async: true, Critical: true` (kept path, but explicit `Async:` field removed per D10). Assertions (hard-abort/RequestCancel record "interrupted"/"parent_cancelled" correctly) are preserved | real store replaces `newEphemeralSession(nil)` |
| `pkg/agent/subturn_delegate_nesting_test.go` | split: **delete** for `TestNestedDelegate_Await` (its entire reason for existing, per its own doc comment, is covering the `executeSync`/await dispatch path in parallel with the async one — D4 deletes that path, making it redundant with `_Background`); **update (setup only)** for `TestNestedDelegate_Background` (its own OUTER `spawnSubTurn` call uses `Async: false` as a convenience even though it's testing the *inner* nested-delegate-registry-exclusion fix, not delivery mode) and `TestNestedDelegate_TrustGraphDenialDoesNotPoisonSubsequentCalls` (same outer convenience call, **and** its scripted LLM provider constructs the inner nested `delegate` tool-call arguments with `"async": false` for BOTH the denied and the authorized-retry calls — an argument value D4/AC-4 makes a validation error, so the scripted args must change to the sole remaining mode) | D4 |
| `pkg/agent/subturn_external_cancel_test.go` | split: **delete** for `TestExternalCLISubTurn_CancelPropagates_Sync` (its own doc comment: "modeling `delegate(async=false)` SYNCHRONOUS dispatch... the parent's own runTurn goroutine is blocked directly" — the exact wait-inline cancellation scenario D4 removes); **retain** for `TestExternalCLISubTurn_CancelPropagates_Async`, `_CancelDuringWorkspaceLockWait`, and `TestExternalCLISubTurn_RequestCancelEndToEnd_ExternalCLIOnlySession` (a 4th function the Haiku draft omitted entirely — models a genuine async/Critical delegate via the real `RequestCancel` entrypoint; no sync mode, no ring, no deleted field) | — |
| `pkg/agent/subturn_followup_resume_test.go` | **update (setup only)** | All 3 functions construct `session: &ephemeralSessionStore{}` (D1) and call `spawnSubTurn`/`createChildSession` directly (replaced by the I-2 launcher). The FR-096 collision-guard and warm-resume (`IsResume`) behavior they pin remains real under ADR-091 — I-2 explicitly keeps "a follow-up on a terminal session... bumps the generation first" and `Dispatch` on a terminal record returning `ErrTerminal` | I-2 launcher |
| `pkg/agent/subturn_identity_test.go` | retain | All 10 functions test `resolveRequestedSkillForChild`/`resolveDelegateSoul`/`composeDelegateInput` via `newTestAgentLoop` — no ring, no `SubTurnConfig`, no `ParentDurableKey`; skill/soul resolution is untouched by ADR-091 | — |
| `pkg/agent/subturn_key_reuse_race_test.go` | **update** | Both functions (`TestSubTurnKeyReuseRace_CancelStillReachesSurvivingGeneration`, `_SpawnSubTurnSideCompareAndDelete`) hand-drive `al.registerActiveTurn`'s plain, unconditional-overwrite `Store` and assert on that exact overwrite-then-compare-and-delete race ("generation 2 must have overwritten the stale generation-1 re-Store"). D10 names this function directly, not just the launcher's own registration: "turn.go::registerActiveTurn's plain store, replaced by the compare-and-set registration (WP-A)." If `registerActiveTurn` itself becomes compare-and-set (not merely supplemented by a separate `registerTurnIfAbsent` used only by `Dispatch`), the overwrite this test relies on to reproduce the race no longer happens the same way. The underlying property — a live/surviving generation's cancel-reachability must not be erased by a stale older generation's deferred cleanup — is retained as a concern; the fixture and its race construction need re-verification against WP-A's actual `registerTurnIfAbsent` implementation | I-3's compare-and-set registration |
| `pkg/agent/subturn_liveness_test.go` | split: retain for 6 functions (`_LiveMatch_ReturnsTrue`, `_FinishedAndPersisted_ReturnsFalse`, `_FinishedButNotYetPersisted_ReturnsTrue`, `_NoMatch_ReturnsFalse`, `_EmptyID_ReturnsFalse`, `_MultipleTurns_OnlyMatchingOneCounts` — all hand-construct bare `turnState{}` literals, no ring, no Async); **update (setup only)** for `TestIsSubTurnActiveForSpawnCall_RealRetryWindow_StaysActiveUntilPersisted` (constructs `session: &ephemeralSessionStore{}` and `SubTurnConfig{..., Async: true}` — the explicit field literal is removed per D10 even though `true` is the kept value) | — |
| `pkg/agent/subturn_rc5b_legacy_store_test.go` | **update (setup only)** | Constructs `session: &ephemeralSessionStore{}` (D1) and calls `spawnSubTurn` with Async omitted (D4 convenience). The RC-5b fix it pins (child's delegated task text lands in the child's own durable transcript, never only the ephemeral ring) is the D1 *destination* state itself — still correct, just needs the parent-field construction updated | I-2 launcher |
| `pkg/agent/subturn_rc5b_transcript_task_test.go` | **update (setup only)** | Same pattern as `subturn_rc5b_legacy_store_test.go` — `&ephemeralSessionStore{}` construction + omitted-Async convenience call | I-2 launcher |
| `pkg/agent/subturn_rc8_dead_override_test.go` | **update (setup only)** | Constructs `session: &ephemeralSessionStore{}` and calls `spawnSubTurn` with `Async: false` explicit. Assertion (native persona resolution via `ContextBuilder`, independent of a since-deleted `SystemPromptOverride` field) is unrelated to delivery mode | I-2 launcher |
| `pkg/agent/subturn_recall_limitation_test.go` | **update (setup only)** | `TestSubTurn_RecallReadsParentStore_KnownLimitation` constructs `session: &ephemeralSessionStore{}` and calls `spawnSubTurn` with `Async: false` explicit purely to obtain a synchronous, directly-inspectable second-Chat-call capture. The recall_conversation-reads-wrong-store known limitation is orthogonal to delivery mode; the capture technique (provider stub records the 2nd Chat call's content) can be re-driven by dispatching async and polling the stub instead of relying on a blocking return | I-2 launcher; poll the capturing provider instead of blocking on the return value |
| `pkg/agent/subturn_requested_skill_test.go` | **update (setup only)** | All 5 functions share `rsSpawn`, which constructs `session: &ephemeralSessionStore{}` and calls `spawnSubTurn` with `Async: false` explicit. The ADR-072 D9 skill-grant/deny/unresolvable-outcome assertions (resolved against the *child's* own ContextBuilder, never the parent's) are unaffected by ADR-091 | I-2 launcher |
| `pkg/agent/subturn_result_test.go` | split: retain for `TestUpdateToolCallStatusWithRetry_AsyncWaitsForDelayedPlaceholder` (exercises the kept async=true retry path), `TestDeliverSubTurnResultNoDeadlock`, `TestDeliverSubTurnResult_RaceWithFinish` (pure concurrency tests of `deliverSubTurnResult`, no ring/Async/ParentDurableKey — the Haiku draft omitted both of these functions from its row entirely); **update (setup only)** for `TestUpdateToolCallStatusWithRetry_FoundOnFirstAttemptSkipsRetry` (its `for _, async := range []bool{true, false}` loop must drop the `false` case — a caller can never again call `updateToolCallStatusWithRetry` with async=false once D10 removes that production call site's `false` branch; the `true`-path assertion is unchanged); **delete** for `TestUpdateToolCallStatusWithRetry_SyncDoesNotWaitForDelayedRecord` (its whole point is pinning the retry-skip behavior *specifically for synchronous delegation*, i.e. the production `false` branch D10 deletes, not merely using async=false as test scaffolding) | D4/D10 |
| `pkg/agent/subturn_soul_test.go` | retain | Both functions inspect `resolveDelegateSoul`/`composeDelegateInput`/`runExternalCLISubTurn` directly — no `SubTurnConfig`, no ring, no `ParentDurableKey`; the external-CLI runner is explicitly kept (D10) | — |
| `pkg/agent/subturn_sources_test.go` | retain | No `func Test...` at all — a shared helper (`readSubturnSourcesForTest`) that globs `subturn*.go` non-test sources for other guard tests. Glob-based, unaffected by the rewrite of `subturn.go`'s contents | — |
| `pkg/agent/subturn_test.go` | split (31 functions total): **retain** — `TestInjectFollowUp`, `TestAPIAliases` (pure steering-queue API tests, no ring/Async touched at all). **delete** — `TestSpawnSubTurn_EphemeralSessionIsolation` (its whole point is proving the child gets an isolated **ring** so it "never pollutes" the parent's ephemeral session — the exact premise D1 says ADR-057 D1 already removed, and the ring is deleted outright); `TestSpawnSubTurn_ResultDeliverySync` and `TestSyncSubTurn_NoChannelDelivery` (both pin "Async=false delivers via return value, not channel" as a feature — D4); `TestHardAbortSessionRollback` and `TestHardAbortOrderOfOperations` (both assert HardAbort **rolls back the ephemeral session's in-memory history** to an `initialHistoryLength` snapshot — a ring-specific recovery mechanism; the real store is append-only and `windowTrim` "deletes nothing on disk" per `pkg/agent/CLAUDE.md`, so there is no real-store equivalent to roll back to); `TestEphemeralSession_AutoTruncate` (directly tests `newEphemeralSession`/`maxEphemeralHistorySize` — the ring's own auto-truncation mechanism, named for deletion verbatim in D1); `TestConcurrencySemaphore_Timeout` (asserts `ErrConcurrencyTimeout`/a launch blocking on a full concurrency slot — D9 explicitly deletes `SubTurn.ConcurrencyTimeoutSec`/`defaultConcurrencyTimeout`: "a launch at the cap never blocks and is never refused"). **update (setup only)** — the remaining 22: `TestSpawnSubTurn`, `TestSpawnSubTurn_ResultDelivery`, `_OrphanResultRouting`, `TestSubTurnResultChannelRegistration`, `TestDequeuePendingSubTurnResults`, `TestSubTurnConcurrencySemaphore`, `TestHardAbortCascading` (cascade walks `childTurnIDs` in-memory — D8 moves the authoritative cascade to a lifecycle-store walk of the steered-by edge; `HardAbort`'s low-level context-cancel role may survive but the enumeration source changes), `TestNestedSubTurnHierarchy`, `TestFinalPollCapturesLateResults`, `TestSpawnSubTurn_PanicRecovery`, `TestGetActiveTurn`, `_WithChildren`, `TestTurnStateInfo_ThreadSafety`, `TestInterruptHard_Alias`, `TestContextWrapping_SingleLayer`, `TestAsyncSubTurn_ChannelDelivery`, `TestGrandchildAbort_CascadingCancellation` (same D8 cascade-source note as `TestHardAbortCascading`), `TestSpawnDuringAbort_RaceCondition`, `TestAsyncSubTurn_ParentFinishesEarly`, `_ParentWaitsForChild`, `TestSubTurn_IndependentContext`, `TestSpawnSubTurn_TranscriptSessionIDIsChildsOwn_RoutingSessionIDInheritsFromParent` (asserts `routingSessionID` is "inherited verbatim from the parent" — D2 changes the derivation to the edge's walked, verified cascade root; same coincidence-at-shallow-depth caveat as `subturn_adr057_test.go`'s routing test) — all construct `&ephemeralSessionStore{}`/`newEphemeralSession(nil)` and/or an omitted/explicit `Async` field on `SubTurnConfig{}` | I-2 launcher; D2 edge-derived routing; D8 lifecycle-store cascade |
| `pkg/agent/subturn_timeout_stops_child_test.go` | split: retain for `TestDelegatedTaskNoticeIdentityBoundsAndFlattensLabel` (pure string-formatting unit test of `delegatedTaskNoticeIdentity`, no turnState/spawn/Async at all); **update** for the other 5 (`TestSubTurnTimedOutResult_PublishesIdentifiedNoticeWhenChildAlreadyTimedOut`, `TestSpawnSubTurn_SettledTimeoutPublicationChoosesSingleOwner`, `TestDelegatedTaskLimitNotice_PersistsOnlyToNestedRootTranscript`, `TestSubTurn_TimedOutChild_StartsNoFurtherToolCalls`, `TestSubTurn_MaxToolIterationsPublishesIdentifiedOperatorNotice`) — every one asserts the delegate-lifecycle notice (`emitSubTurnIterationLimitNotice`/`subTurnTimedOutResult`/`emitDelegatedTaskLimitNotice`) lands by a **direct write to the root/parent session's transcript** (`store.ReadTranscript(parentSessionID)` carrying `ErrorCode == CodeDelegatedTaskLimit`), which is precisely boundary 11 in D3's table: "persists an identified max-iterations notice to the root transcript — child-origin content reaching the operator... Covered: delivered as an attributed lifecycle notice through the upward operation... never as a bare bubble in the root chat." Two of the five (`TestSubTurn_TimedOutChild_StartsNoFurtherToolCalls`, `TestSubTurn_MaxToolIterationsPublishesIdentifiedOperatorNotice`) also call `NewSubTurnSpawner(al).SpawnSubTurn(..., tools.SubTurnConfig{..., Async: false, ...})` — the `SubTurnSpawner`/`tools.SubTurnConfig` interface I-2 replaces with the launcher, plus the deleted `Async` field | D3 boundary 11 (inbox + wake replaces the direct root-transcript write); I-2 launcher |
| `pkg/agent/subturn_toolcall_hardabort_status_test.go` | **update (setup only)** | `TestSpawnSubTurn_ToolCallTimeHardAbort_RecordsCancelledNotSuccess` constructs `session: newEphemeralSession(nil)` (D1) and dispatches with `Async: true, Critical: true` (kept mode, field removed per D10). Assertion (hard-abort mid-tool-call records `SubTurnStatusCancelled`, not success) is preserved | real store replaces the ring |
| `pkg/agent/subturn_transcript_nesting_test.go` | **delete** | `TestSpawnSubTurn_MultiStepChild_StampsParentSpawnCallIDOnOwnNarration`'s own doc comment describes proving that `pkg/gateway/replay.go` **skips** an entry carrying `ParentSpawnCallID`, "matching live's silent suppression — instead of flattening it into a top-level bubble." This is precisely `replay.go::emitNestedToolCalls` / the pre-ADR-091 nested-child-step suppression mechanism D10 names for deletion ("nesting of child steps into parent transcripts... become dead and are deleted"), superseded by the frame-based side panel + twelve-boundary model (D7) | D7's `subagent_start`/`subagent_end`/`subagent_message`/`subagent_state` frame mechanism, keyed by `Origin.CallID` |
| `pkg/tools/delegate_adr053_test.go` | **update (setup only)** | All 25 functions share `newADR053TestTool`/hand-built `LifecycleRecord{..., ParentDurableKey: ...}` literals (D2 — field removed, moves to the `SteeredBy` edge) and `tool.SetSpawner(&mockDelegateSpawner{})` (the `SubTurnSpawner` interface I-2 generalizes into `steer.SessionLauncher`). The steer/respond/cancel/inbox/inbox_ack/peek/follow_up authorization, rate-cap and fail-closed-security assertions they cover remain real, explicitly-preserved concerns (D5's "any ancestor may act... siblings and unrelated roots rejected"; the fail-closed-on-Load-error posture) | D2 edge; I-2 launcher |
| `pkg/tools/delegate_adr057_fix_test.go` | split: retain for `TestKillChildBackgroundShells_ReturnsRealOutcome` (no `ParentDurableKey`/Async — tests `killChildBackgroundShells`'s real kill-outcome reporting directly against a `SessionManager`); **update** for `TestDelegateCancel_SurfacesBackgroundShellKillFailure` (`ParentDurableKey`, D2), `TestEvictStaleTasksLocked_CapEnforced_IndependentOfTTL` and `TestListTaskCopies_DoesNotMutateStoredLastStatusRead` (both manipulate `t.tasks`/`evictStaleTasksLocked`, the in-memory task map D5 supersedes as `status`'s data source — the underlying "don't leak memory unboundedly" / "don't mutate stored state on a read" concerns likely survive against whatever replaces `t.tasks`, but the mechanism needs re-verifying), `TestVerifyCallerOwnsSession_LogsIOErrorDistinctFromNotFound` (`ParentDurableKey`, and `verifyCallerOwnsSession` is explicitly named in D2 as a `tools/delegate.go` reader moving to the edge) | D2 edge; D5 record-based status |
| `pkg/tools/delegate_adr057_test.go` | split: **update** for `TestDelegate_RefusedWithoutLifecycleStore` (2 of 3 subtests use `"async": false`; drop the sync subtest), `TestOwnershipWalk_SiblingRejectedAncestorAllowed`, `_DepthBounded`, `_AllSixGatedActions` (all via `u14SeedChild`'s `ParentDurableKey` — this is D5's "any ancestor may act... siblings rejected" made explicit for all six gated actions, D2 for the record shape), `TestDelegateStatus_SyncAndAsyncSnapshotsNonEmpty` (its "sync" subtest registers via `executeSync`/`t.sessionIndex`, deleted by D4; drop it, keep "async"), `TestDelegateTaskMaps_BoundedAfterNCompletions`, `_RetainWithinTTL` (all-`async:false` dispatch loops feeding `t.tasks` bounded-eviction — D4 convenience-call fix plus D5's status-source shift); **delete** for `TestRecentActivityLines_LogsEmptyPath` — this IS `tool.recentActivityLines`, the exact live-transcript-activity-snapshot mechanism D5 explicitly removes from `status` ("never from streaming progress") | D2 edge; D4; D5 |
| `pkg/tools/delegate_adr057_unix_test.go` | **update** | `TestDelegateCancel_KillsThatChildsShells` constructs `LifecycleRecord{..., ParentDurableKey: ...}` (D2). The real-shell-kill assertion (delegate `action=cancel` terminates the child's real background-shell PID) remains a live D8 concern | D2 edge |
| `pkg/tools/delegate_completion_identity_test.go` | split: **update** for `TestDelegateAsyncCompletion_MaxToolIterationsNamesTask` (uses the `SubTurnSpawner` interface I-2 replaces; the max-tool-iterations notice content is delivered through the async callback — boundary 2 of D3's twelve — worth re-verifying against the new audience-resolver gate, but not itself deleted); **delete** for `TestDelegateSync_ThreadsGeneratedTaskIDToSpawner` (`tool.Execute(..., map[string]any{..., "async": false})` — the whole point is proving `TaskID` threads through the **synchronous** dispatch path, D4) | D4; I-2 |
| `pkg/tools/delegate_detached_timeout_test.go` | split: retain for `TestErrDelegationDetachedImpliesTimedOut` (pure `errors.Is` sentinel-relationship check, no tool/mode involved); **update (setup only)** for `TestDelegateSync_DetachedTimeoutReportsStillUnwinding` (`"async": false` used as a convenience to get a directly-inspectable synchronous result; the truthful-"still unwinding"-vs-false-"fully stopped" reporting property for a cancellation-ignoring detached child remains real and must be re-proven via the async/background path) | D4 |
| `pkg/tools/delegate_fixwave_test.go` | split: **update** for 12 of 13 (`TestDelegateTool_Run_AgentID_Validation` [drop 5 async:false subtests' sync-only framing, D4/I-2], `TestDelegateTool_Run_TimeoutSeconds_ThreadsIntoSubTurnConfig` [D9 — `timeout_seconds`'s destination may move from `SubTurnConfig.Timeout` to the edge's `SteeredBy.Limits.TimeoutSeconds`; drop the redundant sync-labeled subtest], `TestDelegateTool_FollowUp_UsesTextField`, `_TaskAliasStillWorks`, `_TextWinsOverTask`, `_EmptyInstruction_Rejected` [`ParentDurableKey`, D2], `TestDelegateTool_Steer_TerminalCheck_RoutesThroughMutate` [`ParentDurableKey`; its own comment explicitly discusses "climbs the ParentDurableKey chain via `t.lifecycle.Load(ancestor)`" — D2], `_TerminalSession_Rejected` [`ParentDurableKey`], `TestDelegateTool_ResolvableSessionIDs` [`SubTurnSpawner` interface, I-2], `TestDelegateTool_Cancel_AlreadyTerminal_IsIdempotentNoOp`, `_NonTerminal_StillSucceeds`, `_DescendantsMiss_ReturnsTerminalMessage` [all `ParentDurableKey`, D2]); retain for `TestDelegateTool_ResolvableSessionIDs_EmptyIndex` (zero-value check, touches nothing ADR-091-specific) | D2 edge; D4; D9; I-2 |
| `pkg/tools/delegate_followup_resume_test.go` | **update** | All 3 functions (`TestDelegateTool_FollowUp_NativeSetsIsResume`, `_3PDoesNotSetIsResume`, `_SpawnFailure_LoggedUnconditionally`) construct `LifecycleRecord{..., ParentDurableKey: ...}` (D2) and use `tool.SetSpawner(spawner)`/`spawnerFunc` (I-2). The `IsResume`/native-vs-3P follow-up distinction and unconditional-failure-logging behavior they pin remain real | D2 edge; I-2 launcher |
| `pkg/tools/delegate_grandchild_test.go` | retain | `TestDelegateCannotSpawnGrandchild` tests the `CloneExcept(ExcludedDelegate, ExcludedSwitchAgent)` registry primitive in isolation — its own header comment already says production stopped calling it with `ExcludedDelegate` (FR-H-006 reversal) and this test is not evidence of current grandchild-blocking behavior. `switch_agent` exclusion is explicitly kept "as a property of the edge" (D2); nothing here touches a deleted symbol | — |
| `pkg/tools/delegate_inbox_ack_ancestor_test.go` | **update** | All 3 functions (`TestDelegateInboxAck_AncestorAck_KeyedByTargetParent_NoRedelivery`, `_DirectParentStillWorks`, `_UnrelatedCallerDenied`) construct `LifecycleRecord{..., ParentDurableKey: ...}` via `seedChainRecord` (D2). The ancestor-ack-must-key-by-the-target's-own-parent / unrelated-caller-denied properties remain real under D5's ancestor-authority model | D2 edge |
| `pkg/tools/delegate_m1_m3_wiring_test.go` | split: **update** for `TestDelegateInboxAck_TruthfulCountAndUnknownIDs`, `_AllUnknown_ZeroAcknowledged` (`LifecycleRecord{..., ParentDurableKey: ...}`, D2 — the truthful-Acknowledged-count/unknown-id-surfacing behavior is unaffected in substance); **update** for `TestDelegateTool_ResolvableLabels_ImplementsJobLabelResolver` (manipulates `tool.tasks`/`tool.sessionIndex` directly — D10 states list_jobs's label now comes "from its title" on the lifecycle record, not this in-memory `JobLabelResolver`/`DelegateTaskState.Label` mechanism; needs re-targeting) | D2 edge; D10 (label from record `Title`) |
| `pkg/tools/delegate_parent_agent_test.go` | **update** | `TestDelegateMint_StampsParentAgentID` references `rec.ParentDurableKey` directly (D2); `TestDelegateMint_FailsClosedOnEmptyParentAgentID` never reads the record (it checks the tool result and the lifecycle directory listing), but its fail-closed trigger moves because I-1 derives `ParentAgentID` from the steering session at launch instead of `ToolAgentID(ctx)` — the latter also has a `for _, async := range []bool{true, false}` "run async"/"run sync" parameterization that must drop the `false` case (D4). `ParentAgentID` stamping/fail-closed-attribution itself is explicitly kept ("the launcher now stamps that field for both fronts", D2/D10) | D2 edge; D4 |
| `pkg/tools/delegate_park_lifecycle_test.go` | split: **update** for `TestDelegateTool_ExecuteAsync_ParksTurn_LeavesNeedsInputRecord`, `_NonParkedResult_StillTransitionsToCompleted` (the kept `executeAsync` path; `SubTurnSpawner` interface, I-2); **delete** for `TestDelegateTool_ExecuteSync_ParksTurn_LeavesNeedsInputRecord`, `_NonParkedResult_StillTransitionsToCompleted` (both explicitly test "**separate code from executeAsync**... `executeSync`'s own `default:` branch" — D4 deletes `executeSync` outright, not just the `async:false` argument) | D4 (sync pair deleted); I-2 (async pair's spawner interface) |
| `pkg/tools/delegate_require_parent_test.go` | **update (setup only)** | All 5 functions call `tool.Execute(..., map[string]any{..., "async": false})` as the sole dispatch mode exercised throughout the file. The `require_parent_agent_id` kill-switch behavior (default fail-closed, live-read-not-eagerly-captured, real-parent-resolves-regardless-of-switch) is entirely orthogonal to delivery mode; success-path subtests need to move to `ExecuteAsync`+callback since `async:false` is rejected outright (D4) | D4 |
| `pkg/tools/delegate_signoff14_test.go` | split: **update** for `TestDelegateTool_Respond_NativeRedispatchesWithIsResume`, `_EnqueueFailure_LeavesSessionParkedNotWedged`, `TestDelegateTool_Inbox_AuthorizedAncestor_SeesMessagesUnderDirectParentKey`, `TestDelegateTool_Peek_AuthorizedAncestor_SeesSnapshotUnderDirectParentKey`, `TestDelegateTool_Cancel_NothingToCancel_StillSurfacesShellKillWarnings` (all construct `ParentDurableKey`/use `SubTurnSpawner`, D2/I-2); retain for `TestSteerRateWindows_EvictsExpiredOtherSessionKeys` (pure `steerRateWindows` map-hygiene test, no `ParentDurableKey`/Async at all) | D2 edge; I-2 launcher |
| `pkg/tools/delegate_status_reconnect_test.go` | **update** | All 5 functions (`TestDelegateStatus_SurvivesReconnect_GetByID`, `_ListAll`, `_DifferentDurableSession_StillIsolated`, `_NoDurableSessionOnEitherSide_FallsBackToChannelChatID`, `_CallerHasDurableSession_TaskDoesNot`) directly seed `tool.tasks[...] = &DelegateTaskState{..., OriginChannel, OriginChatID, SessionID}` and scope `status` reads by those in-memory fields. D5: "`status` answers from the record and the inbox... never from streaming progress" — the data source and scoping mechanism this file tests move from the in-memory task map to the `LifecycleRecord`+inbox and edge-based ancestor authority. The reconnect-survival / cross-conversation-isolation properties remain real concerns, just answered from a different source | D5 record+inbox; edge-based ancestor authority replaces `OriginChannel`/`OriginChatID` matching |
| `pkg/tools/delegate_status_snapshot_test.go` | **delete** | All 3 functions (`TestDelegateStatus_RunningNative_IncludesRecentActivity`, `_NoMatchingActivity_OmitsHeader`, `TestDelegateStatus_RunningExternalCLI_NoLiveSnapshot`) exist specifically to prove `action:"status"` surfaces **live transcript activity** (`recentActivityLines`) for a running task. D5 states plainly: `status` answers "never from streaming progress" — this is exactly the mechanism being reversed, not merely updated | D5 — `status` answers from the record + inbox only |
| `pkg/tools/delegate_status_test.go` | **delete** | All 4 functions test `formatToolCallProgressLine` directly — the rendering helper for the **live tool-call-argument progress line** `action:"status"` shows. Once D5 removes streaming-progress reporting from `status`, this helper's only caller (the G1 live-progress consumer `delegate_toolcall_progress_test.go` also names) disappears, making the formatting function itself dead code | D5 |
| `pkg/tools/delegate_test.go` | split (25 functions): retain for `TestDelegateTool_Name`, `_Description`, `TestDelegateTool_Execute_EmptyTask`, `_InvalidAction`, `TestDelegateStatus_Empty`, `_TaskID_NonString`, `TestDelegateTool_KillSwitchGatesSessionMessagingActionsOnly_ArchM2` (7 functions — none touch a deleted symbol); **update** for `TestDelegateTool_Parameters` (drop `async` from the required-properties check — the argument itself is removed from the schema per D4), `TestDelegateTool_Execute_NilSpawner` (drop its added "sync mode" secondary check), `TestDelegateTool_Execute_AsyncWrongType` (re-verify against the schema with no `async` parameter), `TestDelegate_DefaultIsAsync`, `TestDelegate_StatusReflectsRealState`, `TestDelegate_MainAgentCanDelegate` (all three use the `SubTurnSpawner` interface, I-2; the status test's data source also shifts per D5), `TestDelegateStatus_ListAll`, `_GetByID`, `_ResultTruncation`, `_ChannelFiltering_ListAll`, `_ChannelFiltering_GetByID`, `_ChannelFiltering_NoContext`, `_DifferentConversation_NotFound`, `_GetByID_NotFound` (all directly seed `tool.tasks[...]` to back `status` — D5's record+inbox source shift); **delete** for `TestDelegate_AsyncFalseBlocks` (its whole point, per its own doc comment: "async=false blocks the caller... matching the former run_subagent behavior exactly" — D4), `TestDelegate_AsyncFalse_NowRecordsStatusTask` (proves `executeSync` ALSO registers `t.tasks` — moot once `executeSync` no longer exists, D4), `TestDelegate_SyncInterrupted_PropagatesToResult`, `TestDelegate_SyncGenuineError_StillUsesGenericShortcut` (both name `executeSync`'s own return-value behavior explicitly as their subject, D4) | D4; D5; I-2 |
| `pkg/tools/delegate_timeout_force_cancel_unix_test.go` | split: **delete** for `TestDelegateSync_TimedOut_ReportsStoppedAndKillsChildShells` (labeled "async=false" in its own name/comment; its sibling below already covers the same timeout/shell-kill property via the sole remaining async path — D4); **update** for `TestDelegateAsync_TimedOutAfterParentTurnEnded_ReportsStoppedNotCanceled` (`SubTurnSpawner` interface, I-2; the timeout-truthfully-reported/shell-killed property is preserved) | D4; I-2 |
| `pkg/tools/delegate_toolcall_progress_test.go` | **delete** | All 3 functions (`TestDelegateStatus_RunningNative_ShowsLiveToolCallProgress`, `_NoProgressRecorded_OmitsProgressLine`, `TestDelegateStatus_NilProgressReader_DegradesGracefully`) are the G1 fix's dedicated proof that `action:"status"` reads a **live tool-call-argument progress snapshot** (`DelegateProgressReader`/`ProgressForSession`) mid-turn. This is the second half of the exact mechanism D5's "never from streaming progress" removes | D5 |
| `pkg/tools/delegation_deny_wiring_test.go` | split: **delete** for `TestDelegateTool_AwaitDelegationDenyChecker_Aborts`, both subtests of `TestSyncDelegateWait_Rejected`, and `TestDelegateTool_AwaitNilDenyChecker_FailsClosed` (all pin the retired await/synchronous delegation path or its dedicated deny-checker); **retain** for every other function (background delegation and task deny-checker wiring remain active security controls) | D4; `delegate_adr091_launcher_test.go::TestDelegate_RejectsRemovedArgs` proves `async`/`wait` are rejected before dispatch |
| `pkg/tools/message_parent_adr057_test.go` | **update** | Both functions (`TestMessageParent_DrainedByDirectParentAtDepth3`, `TestPerChildMessageCeiling_IsPerDirectParent`) construct `LifecycleRecord{..., ParentDurableKey: ...}` directly (D2). The ancestor-drain-by-target's-own-parent-key and per-direct-child ceiling-isolation properties remain real | D2 edge |
| `pkg/tools/message_parent_test.go` | split: **update** for 11 of 13 (`TestMessageParentTool_Progress_AppendsToInbox`, `_Question_Wait_ParksNeedsInput`, `_Question_OmittedAuthority_DefaultsOwnerRequired`, `_PerChildCeiling_FailsBackAsToolError`, `_3PChild_Rejected`, `_ContentEgressFilter_Applied`, `_Handback_Wakes`, `_InvalidKind_Rejected`, `_WakeFailureDoesNotFailToolCall`, `_EgressFilter_AppliedToPathFields`, `_KillSwitchDisabled_FailsClosed_ArchM2` — all route through `newMessageParentTestSetup`, which persists `LifecycleRecord{..., ParentDurableKey: "parent-1", ...}` (D2); 4 of these also use `fakeWaker`, a `MessageParentWaker` implementation I-5 states is **replaced** by `steer.UpwardDeliverer` (`Deliver(ctx, UpwardEvent) (Delivery, error)`), a different method shape); **retain** for `TestMessageParentTool_NoSessionContext_Rejected` and `_TaskRun_NoParentSession_RedirectsToGoalClaim` — neither uses `newMessageParentTestSetup`; both build their own bare `lc`/`inbox` with no `LifecycleRecord` persisted at all and no waker touched, testing only the missing-session-context and task-run-has-no-parent redirect cases. Progress/question/handback/wake-failure-tolerance behavior itself is preserved per D3's outcome table | D2 edge; I-5 `steer.UpwardDeliverer` |

Verified by reading every test function (cross-family review of the Haiku draft).

## Definition of done

1. *Code correct and tested:* fixtures, recorder and suites green; classification complete.
2. *Reachable:* the Playwright reachability spec is the product's AC-12 proof, executed and recorded.
