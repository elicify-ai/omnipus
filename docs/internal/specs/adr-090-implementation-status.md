## Current checkpoint — 2026-09-18, generic Bash installation

This checkpoint supersedes the historical outstanding-state descriptions below. The founder selected Bash with enhanced, bounded installation permissions: agent-supplied commands, existing global/per-agent Ask, workspace default, explicit shared scope, and existing background process controls. The additional detached-process supervisor was removed. Dependency choices remain in skills and task instructions.

**Code correct and tested:** installer, storage and runtime correction checks pass. Full registered-tool acceptance passes six scenarios (`run-2228212392`), and fresh-package acceptance passes three (`run-3415285064`), including real package installation and ordinary sandbox use. Corrected fixture routing and an explicit response-label assertion also pass for Mia (`run-1909144494`); the discrepancy was caused by the test selecting an unrelated CLI session. Final non-root Linux checks pass all three confinement scenarios (`20260918-134520`). These are separate from live-model planning evidence.

**Reachable by a user/agent:** browser denial, workspace approval, shared approval and cancellation pass. The new standalone Admin chat entry point and connection/replay corrections pass 47 focused UI tests and typechecking. Browser R3 (`20260918-141714`) passes on binary SHA-256 `727794948739a5556b0b4b3f677d975b282e0cb6e9cd2c0b8c25b286cd5ee698`: the approval displays the target workspace name, and the installed executable runs from that target's prefix. All twelve final document images across Mia, General Purpose and a custom agent are parent-inspected and acceptable, with exact decoded-pixel matching to images sent to the real model. A worker spreadsheet required a targeted correction after the first final image still clipped text; failed attempts remain recorded.

Terra fallback workers completed these corrections after the recorded GLM five-hour quota error. The parent remained an orchestrator and verifier. The generic installation implementation and required verification are complete: the [final verification record](adr-090-environment-setup-verification.md) closes the audit with actual custom/Mia/attended-worker install-to-document chains, multi-page/formula/Unicode cases, controlled limitations, deliberate-fault tests and exact outgoing worker-image pixel verification. No native Windows or Linux Office-rendering certification, commit, push or release is claimed. Deferred issue #731 remains outside this work. Earlier progress checkpoints below are historical.

**Separate deferred finding:** `pkg/agent/loop_session.go::rebuildChannelSessionIndex` rebuilds persisted channel keys from Channel/PeerID, while `resolveOrCreateChannelSession` uses InstanceID/ChatID. A multi-instance channel can therefore fail to resume its existing session after restart. This was found during fixture diagnosis and was not changed as part of installation delivery.

## Document workflow checkpoint — prepared runtime, scripted provider

A fresh macOS Seatbelt run completed document generation, conversion/rasterization and image reading for Mia, General Purpose and a custom agent across DOCX, XLSX, PPTX and PDF. All twelve image reads reached the scripted provider; the parent checked the twelve artifact hashes and visually inspected representative Word, spreadsheet and slide renders. All subjects used the sandbox with God mode off, and the run recorded no Admin actions or finalizer calls. A generic workspace executable was also found through normal PATH lookup.

This uses an existing prepared runtime and a scripted provider. It does not prove fresh installation, live visual defect detection/correction, Windows/Linux enforcement, or approval through the browser. Separate GLM lanes now own live-model visual acceptance and authenticated browser installation approval checks. Installer security findings remain open and prevent completion.

## Generic installation implementation checkpoint — 2026-09-18

The generic command/script implementation is now being integrated in parallel GLM 5.3 Flash lanes. Source exists for installation storage, background sessions, live registration, per-turn runtime paths, the approval preview and explicit Ask defaults. The rejected package-recipe engine files have been removed from the new installation package. This is implementation progress, not a completed or verified installer.

The completed frontend correction passed all 35 focused tests, all 604 tests in the agents UI group and the full frontend typecheck (exit 0). Current setup-default and affected prompt tests pass in `pkg/coreagent`. The storage test for retaining two published shared generations also passes; normal-agent execution of both tools remains an acceptance requirement. Independent review found that some extra request fields could be omitted from the summary; the GLM correction and its regression tests are now complete. Backend review has sent concrete storage-confinement, publication-error and completion-status findings to their owning workers. Production storage and proxy wiring is present; successful registered execution remains to be demonstrated.

Separate GLM lanes are preparing registered-tool approval/sandbox acceptance and real document workflow acceptance. Backend integration tests, current-source independent reviews, Office rendering/visual inspection and supported-platform evidence remain open. Existing document runtime fixtures may establish rendering behavior, but will not be counted as proof of a fresh generic installation. The parent coordinates and verifies; product coding and bug fixes remain delegated. No current GLM five-hour quota limit has been observed.

Earlier checkpoints below are historical. In particular, statements that no setup source exists predate this checkpoint; none of the earlier results establish completion of the current generic installer.

## Current correction — generic installation, 2026-09-18

The founder rejected hardcoded library/application recipes. Dependencies and installation knowledge belong in skills or the agent’s task plan. The generic tool retains existing Ask configuration, workspace/shared destinations and background execution. The founder selected option A: agent-supplied installation commands/scripts, shown through existing Ask approval. Affected development lanes were deliberately interrupted and are being reassigned to replace the superseded recipe catalogue. Their reusable session, approval and workspace work is preserved; no working generic installer is claimed. Policy configuration and portable guidance work can continue independently.

## Current design amendment — 2026-09-18 (approved environment setup)

The founder approved a general environment_setup tool: workspace-local installations by default, application-managed shared runtimes, the existing Ask tool permission as installation approval (no new approval workflow), and support for custom native agents without delegation edges. This supersedes Admin document-dependency handoffs and role-ID-specific runtime wiring. See [the setup specification](adr-090-environment-setup-spec.md). This checkpoint is a **documentation change, not delivered setup functionality**. The founder subsequently locked in Ask for Mia/General Purpose/Admin, Deny for Jim/Ava/Planner/Researcher, locked Deny for Judge/Supervisor, and subsequently clarified that the existing global policy and explicit per-agent policy must both be Ask for permitted built-ins and new custom agents. Execution-dependent defaults remain excluded. Admin targets workspaces using cross-workspace filesystem authority without membership. Installation is specified as a background session using the Bash-style poll/read/kill lifecycle; unattended Ask keeps existing automatic denial. This supersedes the earlier all-ordinary-roles Ask draft; implementation and acceptance remain pending. Earlier scope/acceptance entries below remain historical evidence; unrelated deferred defects are not reopened by this amendment.

**Code correct and tested:** the new setup tool, existing approval integration, installer integration and custom-agent runtime wiring are not implemented or tested. A separate bounded converter-registry candidate has local unit/mutation evidence but still needs independent review and runtime integration; it is not complete rendering acceptance.

**Reachable by a user/agent:** Ask-gated setup, installation, resume and Office rendering through the new route remain pending. Next delivery gates are ES-SC-01 through ES-SC-04, affected regression checks and live vision acceptance. No CI, push or release is claimed.

## Founder scope change — non-Ava defects deferred

The founder requested that further fixes for the listed non-Ava findings stop and be tracked together in [issue #731](https://github.com/elicify-ai/omnipus/issues/731). Preserve the unintegrated document-runtime candidates; do not continue implementing or integrating them in this work. The live Ava workflow remains active. Already completed corrections and evidence are retained. This supersedes earlier action lists below and does not claim the deferred functionality is complete or waive unrelated acceptance requirements.

## Ava checkpoint — confirmation and cancellation verified; permission preview pending

This supersedes the older Ava live-workflow entries below. In an isolated Sonnet 4.6 fixture, Jim relayed Ava's proposal, one test-user conversational confirmation authorized the exact reviewed proposal, and Ava created the teammate with the specified instructions, model, skill, connector exclusions, workspace and delegation edges. The new teammate successfully wrote the requested marker file. A separate cancellation run retained identical saved configuration and created no agent. Parent checks are recorded in the `parent-apply-acceptance.json` and `parent-cancel-acceptance.json` artifacts under the local Ava acceptance directory. These are test-user confirmations, not founder approval of production changes.

The proposal experiment exposed a missing read-only way to inspect a not-yet-created agent's starting permissions. `get_agent_tools` now accepts either an existing `id` or native `new_agent_type` (`Main`/`Subagent`). The preview uses the same shared defaults as creation, applies global restrictions, keeps ToolSearch available, and creates no saved agent or files. Focused tests and three deliberate fault checks passed. After clarifying the tool description, affected tests passed again (1.868s) and the local gateway rebuilt successfully. A first live run of the preview build did not call the preview and produced an incorrect effective-tool summary; the proposal was rejected with zero writes and zero confirmations. The agent-authoring skill now explicitly requires native creation preview, discovery of the creation schema, retained-default semantics, and verification of skill prerequisites. Independent code and Prometheus skill reviews completed with no blocking defects. The repeated unchanged live task discovered the preview but failed because Claude rejects top-level `oneOf` in a tool schema. A new regression test reproduced that rejection; the schema now relies on the existing exact-one runtime validation instead. The rebuilt live run successfully invoked `get_agent_tools(new_agent_type=Main)` before proposing changes. Parent inspection verified all 16 non-denied baseline policies were retained in the proposal, with only the requested write-file grant added, and no configuration writes or confirmations occurred. The test user requested two revisions for omitted saved instructions/identity, an unsupported inbox-access claim, and unwanted marker metadata. After one conversational test-user confirmation, Ava applied the final proposal and the new teammate wrote the exact marker bytes. Parent inspection verified the saved identity/model/skill/connector/workspace/delegation settings, preserved existing agents and graph edges, and confirmed the effective permission baseline plus write-file grant. Two bold section titles in the proposed instructions were saved as level-two headings; all instruction wording matched. This proves the reviewed revise-confirm-apply path, not reliably perfect first drafts. Cancellation on this latest build also passed: an awaited Ava proposal, zero confirmations, no successful mutation tools, identical saved configuration before/proposal/cancel, and no new teammate. Parent evidence is in `sc003-preview-20260918-03/parent-apply-acceptance.json` and `sc003-preview-cancel-20260918-01/parent-cancel-acceptance.json` under the local Ava acceptance artifacts directory. The verified binary SHA-256 is `85b069ea39b3360a1c9a0d8221df2e184031b5e5ee292af33589b6461764e0f7`.

Two independent API source reviews (Sonnet code review and Opus using the repository's Prometheus definition) completed. Their claimed type-specific-default defect is disproved by the actual shared creation caller; the follow-up review includes that caller and the written disposition. This is source review through an API, not execution by the Claude Code CLI. The follow-up Prometheus source review accepts the tool preview and wording. Additional literal default-policy and connector-catalog assertions pass. Live preview verification remains pending. The broader skills selection found an unrelated Define Goal display-name mismatch; this is deferred in #731. Affected role-skill, tool-reference and seeding checks pass separately; no full-suite-green claim is made. The existing saved-agent read-error classification finding is deferred in issue #731.

The final Ava supplement completed seven independent source-only API reviews (correctness, security, errors, tests, types, simplicity, comments), plus the repository Prometheus-definition review. Current source hashes match their reviewed snapshots. Confirmed existing read-error classification and an unverified ID-readback concern are tracked in #731; incorrect and stylistic findings have explicit evidence-based dispositions. No claim is made that these API reviewers executed CLI agents or tests. The affected local tests and current-build live checks were run separately by the parent. Broader repository review-record reconciliation and delivery remain distinct from this Ava checkpoint; CI has not run.

## Current acceptance checkpoint — 2026-09-18 (browser restart closure)

This checkpoint supersedes pending-state claims in historical entries below. The overall feature remains incomplete.

- **Code correct and tested:** contract-validator corrections pass 65 focused tests and generation is idempotent. The last full frontend run passed 1,153 tests; subsequent tool-settings changes passed 42 affected tests, lint/build and then the cleanup type check. The WebSocket error-frame regression now exercises the actual turn handler with a portable fixture; all four tests pass. Independent reviews approved the contract and tool-settings fixes.
- **Reachable by a user/agent:** real browser connector selection passed all/selected/none modes across reload and process restart. A separate browser run verified UI-written Judge/Plan Supervisor instructions, Mia read-file Ask permission and empty skill assignments across a real gateway restart. Ordinary identity/base instructions stayed protected; installed skill packages were retained. Evidence uses binary SHA-256 `da2c3830e5ca4364b32d4bd286a466490a0e421eb77914b40ee668f96459a12e`.
- **Document evidence:** eight basic diagnostic renders (Mia and General Purpose, four formats each) completed under the actual macOS sandbox and reached the next model request as images. Parent visual inspection found readable text without clipping. This used a diagnostic LibreOffice service-registry override; production integration, complex/non-ASCII/multipage fixtures and live visual correction remain open. The production candidate passes its existing focused tests, but independent review requested changes for publication safety, sibling-registry validation, XML namespace identity and missing execution-boundary tests. It is not integrated.
- **Live Ava workflow:** a fresh OpenRouter test reached model execution, but Jim returned a background-delegation acknowledgement rather than Ava's complete proposal. A reported pre-confirmation change was limited to derived runtime metadata; the comparison is corrected and tested. A later awaited-delegation run was rejected because the model repeatedly supplied an empty requested-skill value; a fresh run explicitly requests Ava's installed agent-authoring skill. No confirmation or application has occurred. Proposal/apply/cancel and subsequent teammate use remain unverified.
- **Ratified update rule:** preserve saved settings unless the user explicitly resets them; never automatically add new capability defaults to existing ordinary agents. Judge/Plan Supervisor capabilities remain fixed and edited instructions are preserved.
- **Remaining:** production document integration and independent review; complex and live document acceptance; remaining live role/skill, Admin, delegation and revoked-connector scenarios; platform evidence; final review-coverage reconciliation and local commits. CI has not run.

## Closure checkpoint — remaining defects (2026-09-18)

This checkpoint supersedes earlier pending-state entries below; it does not claim completion.

- Removed premature document-runtime readiness publication after a real failing regression; the documentruntime package passed afterward.
- Document Unix socket grant is limited to a writable current directory. A regression caught and corrected an overly broad parent-directory match. Actual macOS kernel experiment permits the authorized Unix socket and denies an outside socket and TCP bind. The full sandboxed document conversion failed: the shell passed its fixed agent-home directory to the socket grant while executing in a shared workspace. A targeted correction is in progress; no application-readiness claim.
- Corrected Seatbelt rule syntax after the native compiler rejected the first proposal. Independent review closes the syntax and containment finding; application-level document acceptance remains open.
- Active-revision fixtures initialize the persisted configuration through the real publish path; focused sysagent and gateway tests pass.
- Anthropic unsupported BMP/TIFF omission handling and all four supported image format regression tests pass.
- Existing empty-inventory runtime manifests are rebuilt. Both empty/null inventory regression cases failed before the fix; the complete documentruntime package passes afterward.
- Final combined verification, final review reconciliation, generated-document visual inspection, and local commits remain outstanding. CI has not run.

# ADR-090 implementation and verification ledger

## Closure checkpoint — 2026-09-18

Latest integrated corrections: mutation failure envelopes now omit unknown revisions and serialize empty changed-field lists as arrays. The new regressions failed before the fix and pass afterward. The team editor reproduced stale revision 2 after displaying revision 3; the fix passes all 18 team-editor tests and preserves dirty-draft revision checks. Anthropic/Bedrock media-omission regressions failed before the warning fix and pass afterward, with supported-image controls still passing. Independent delta review remains required for these latest changes.

The converter diagnostic now reaches a local socket inside the workspace but Seatbelt denies its bind operation; this is recorded in `libreoffice-kernel-socket-path.log`. A narrowly scoped document-runtime Unix-socket policy is under review. The management activation callback proposal is being checked against actual publication failure, since comparing the shared config alone may not prove which instance is active. Comment corrections and the simplification review are also in progress.

Further closing evidence: 136 affected frontend tests, source lint and production frontend build pass after correcting missing API mocks in the capability-test fixture. Delegation PUT now distinguishes corrupt existing storage (500) from missing workspace (404): both corruption regressions failed before the fix, then all classification cases passed. Runtime tests reproduced the missing child TMPDIR and missing second-worker cache and pass after correction; platform-aware executable validation is covered by a simulated Windows mode oracle, not native Windows execution.

Native `/review` completed with six P2 findings and explicitly incomplete whole-diff coverage. Runtime validation/cache findings are corrected; management inventory/activation reporting and team revision refresh remain under correction. The type review found invalid failure-envelope arrays/revisions; tests and corrections are being integrated. Independent silent-failure review found provider adapters silently omitting unsupported image evidence; a narrow warning fix is in progress. Main plus DOCX/PPTX supplemental Prometheus reviews pass their independent scopes.

The sandboxed converter still fails after adding TMPDIR. LibreOffice's pipe implementation uses `/tmp`, `/var/tmp`, then `OSL_SOCKET_PATH`; therefore TMPDIR was not the complete IPC diagnosis. An unchanged-sandbox diagnostic using `-env:OSL_SOCKET_PATH=.` is pending. No actual document workflow success is claimed.

Frontend loading-error closure: three new regressions reproduced the failures before the fix; the corrected component now passes all 13 focused tests. One test selector was adjusted to match the expected text inside its error region rather than requiring the entire paragraph to equal a substring. Main Prometheus review passed its independent 50-file scope; separate DOCX/PPTX review remains pending. The first full-turn document run failed because the negative-test canary was rejected by the command-text guard before document generation; the harness is being corrected, and no workflow success is claimed.

Latest verified additions: Judge and Plan Supervisor both passed real browser instruction editing, save, reload and restoration of the original text (`browser-hidden-instructions-result.json`). The oversized instruction-management test reproduced the real GET failure and passes after the strict management reader fix; the focused workspace/gateway instruction checks pass (`workspace-management-close-result.json`). Runtime prompt reads remain bounded as before. Updated role-skill/package checks, tool-schema checks, document-runtime tests and contract generation pass (`prompt-close-checks-result.json`).

The portable document handoff wording and Agent Skills naming metadata are sourced from Elicify skills repository commit `687242a67612744c88aa1c6b4ac0a56a5537cde3`; Omnipus's bundled files, provenance hashes, and runtime revision track that pushed commit. The new local runtime prefix was provisioned and Admin finalization/probes pass; full-turn image acceptance remains pending.

Independent frontend review found two additional loading-error issues (global restriction failure leaves individual controls active; connector-list failure renders as an empty list). Focused fixes and regression checks are in progress. Independent test-quality review closes its eight prior source findings, without claiming all current tests executed. Native full `/review` and the final Prometheus review are running; no final clean gate is claimed.

This checkpoint supersedes contradictory in-progress statements in older activity snapshots below. The goal remains active; no CI, push, or release has been run.

- Production frontend build and final local Go gateway build both pass. Source lint passes with `.local/**` excluded because that directory contains downloaded test runtimes and temporary artifacts; no source lint rule was disabled.
- Real browser acceptance on the rebuilt application passed Mia's `search_web` permission changes Ask → Deny → Ask. Each save returned HTTP 200; a full page reload and fresh backend read confirmed the requested value. The interface exposes editable overrides and explicitly keeps ToolSearch always available. Evidence: `/Users/danielpiatkowski/Documents/Agent-Workspace/omnipus/worktrees/release-20260917/.local/adr090/browser-final-save-reload-result.json`. This proves this permission workflow, not all settings workflows.
- Workspace review and configuration rollback checks each caught three deliberate semantic regressions, then passed with restored production source. The 20 workspace Settings tests and 71 affected Ava configuration checks pass.
- The final broad review is **REVISE**, not a clean seven-reviewer gate. Its claim that the document inventory is missing is incorrect: `adr-090-document-package-inventory.json` exists beside this ledger. Remaining findings require source/evidence verification; review-only workers did not execute tests.
- Grok CLI prompt/harness fixers terminated with provider HTTP 402 (usage balance exhausted). Work resumed with authorized native agents. No unfinished worker output is treated as delivered.
- Pending: final prompt/schema/handoff corrections and independent Prometheus re-review; oversized workspace-instruction handling and failure-contract review; remaining independent reviewer gates; actual document generation/render/image workflow acceptance; remaining settings and role-skill acceptance; consolidated final ledger and verified local commit. The scripted document harness requires correction before its results can be trusted. Supported-platform certification and live-model behavior are not proven by the existing direct-tool probe.


## Document execution readiness — observed local check

The registered bash tools passed six checks with real managed dependencies: Mia and General Purpose were refused runtime finalization; Admin finalized successfully; Admin, Mia and General Purpose each generated and independently validated DOCX, XLSX, PPTX and PDF through the runtime probe. Exit 0. This harness directly invoked registered tools with GodMode disabled, but did not install a full-turn policy or call a live model. Full-turn sandbox, visual-workflow and supported-platform acceptance remain unproven. Evidence: `/Users/danielpiatkowski/Documents/Agent-Workspace/omnipus/worktrees/release-20260917/.local/adr090/document-acceptance/artifacts/summary.json`.


## Current integration checks — latest local evidence

This section supersedes the older activity snapshots below. Committed parent: `9455f2a4a`. Implementation and final acceptance remain in progress; CI has not been run.

- Workspace mutation error classification, partial agent deletion, and fresh self-delegation depth are committed. Their focused checks passed, and the associated deliberate defects were detected before restoring the passing source.
- The affected frontend run passed **134 tests across seven files**. Remaining review fixes cover connector tool selection, loading/error handling, and revision-aware workspace instruction saves. Connector GET serialization must be integrated with the interface before browser acceptance.
- Discovery passes its focused production-path checks. Three deliberate regressions (snapshot Ask gating, stale agent registration, and live Deny gating) each caused assertion failures; restored tests pass. Target tool permissions remain enforced.
- Runtime document guidance reproduced the missing-guidance failure and passes after integration. It does not establish visual workflow acceptance.
- Configuration rollback tests reproduced two real failures: an omitted empty scalar survived a failed save, and a missing save handler did not roll back. The corrected rollback and affected Ava default/connector-clear tests pass. Final independent review and deliberate-defect checks remain pending.
- The re-auth regression fixture now submits the endpoint's complete effective policy map plus a sparse override list; the focused test passes.

Latest connector follow-up: 39 focused interface tests pass; three deliberate regressions (clearing an unknown connector list, enabling presets before loading policy limits, and treating no-tools as all-tools) were detected, then the restored tests passed. The backend connector test initially failed in fixture setup because the persisted agent was missing; that result is **not** readback-defect evidence. After correcting the fixture and a separate missing test import, all four cases failed on the old handler for the expected missing `config.mcp`, then passed with the serialization fix. The 140 API tests covering the corrected deletion fixtures also pass. Workspace outcome and caller patches are integrated. Contract regeneration and its TypeScript check pass; focused outcome tests are still running. Independent review found inconsistent workspace read snapshots, missing security headers on unload saves, and duplicate saves after revision acknowledgement; corrective work is active.

Additional integration evidence: Ava's nested parameter schema checks reproduced the missing shape definitions and pass after integration (three tests). The role-skill lint now distinguishes the supported `subagent_3p` type from the retired `subagent` operation; direct checks pass; three injected retired operations were caught and restored checks pass. The built-in roster classifier reproduced outdated-ID failures and passes with the current nine identities. Workspace handler assertions pass, but the package cleanup gate exposed existing media tests leaving console logging disabled; cleanup is corrected and the focused workspace instructions/deletion/media rerun passes, including the package cleanup gate.

Create-path integrity: focused tests reproduced invalid heartbeat values being ignored, incomplete agent-home initialization lacking a partial-save result, heartbeat write errors being swallowed, and non-private new heartbeat files. All pass after integration. Broader affected-agent regression checks and independent final review are still outstanding; this does not establish whole-feature completion.

Workspace review corrections now pass the focused backend checks and all 20 Settings tests. The instruction save token is separate from the dirty draft, unload saves include the required security header, GET/PUT responses use captured workspace state, and an unreadable post-write state does not advertise the old revision as current. The dirty-GET test mock was corrected to return saved content after acknowledging a successful save; reviewed-token assertions remain intact. Final independent review and deliberate-regression checks remain open.

Ava configuration regressions: 71 passing checks. The only failure among the broader built-in-agent checks was an obsolete knowledge matrix; the corrected independent ADR-090 matrix passes against the production compositor, including sparse inheritance and stricter global restrictions.

Evidence is under `/Users/danielpiatkowski/Documents/Agent-Workspace/omnipus/worktrees/release-20260917/.local/adr090/`: `frontend-followup-verification-result.json`, `discovery-final-verification-result.json`, `document-guidance-integration-result.json`, and `rollback-followup-result.json`. These are local verification records, not proof of the remaining full-review or live workflow gates.

## User clarification — discovery availability

Tool discovery is intentionally always available and cannot be denied. The proposed change to enforce a denial of ToolSearch was rejected by the user before integration. Permissions continue to govern which target tools are discoverable and executable. Tests and specification wording are being reconciled with this decision; discovery availability is not an outstanding defect.


Status: in progress. Baseline: `12d97e7e70a869b72a246c9385776e71e9bdaa22`.

The founder authorized the agreed parallel implementation on 2026-09-17, using Sol workers, full code review, Prometheus review of every changed prompt/skill, correction of all findings, and green local tests. CI is explicitly deferred: do not push implementation branches, open implementation PRs or dispatch workflows during this phase.

## Current verified state — Grok continuation, 2026-09-18

This checkpoint supersedes worker activity and open/closed statements in the historical entries below. The implementation is **in progress**, and final review, reachability and workflow gates remain incomplete. Current committed parent is `59a09e160`; its Admin backend/team-picker fix is committed, while the chat-selection follow-up is under test. `878f3d3eb` records the tested reader/carrier changes, and `420b8401a` records the tested prompt/skill guidance and runtime capability lints.

- **Parallel work:** at the founder's instruction, nine saved CLI sessions resumed through `claudeg` using `grok-4.6` after GLM's terminal quota errors. Seven implementation lanes cover Ava configuration completeness, mutation error reporting, self-delegation depth, image delivery, roster cleanup, Admin chat reachability and ToolSearch policy enforcement. Two independent reviewers cover the full frontend and all changed prompts/skills through Prometheus. Running sessions are not completed work.
- **Reader and visual transport:** expanded reader tests and removal of unused image-carrier metadata are integrated. The reader and agent inspection runs both pass. All eight reader-worker semantic mutations were restored before integration; the parent verified clean production source before copying the tests.
- **Prompts and skills:** complete browser-control/knowledge guidance, parent-relayed Ava confirmation and both catalog/capability lints are integrated. The lint resolves actual freshly seeded runtime permissions. Three injected instruction defects were caught; restored lints and core prompt checks pass. Internal labels impose no new contract on user-authored or external skills. Independent final Prometheus review remains pending.
- **Admin:** default-team and team-write exclusion, own-home turn admission and team-picker filtering are integrated. The parent fixed a further working-directory context injector that advertised/created a forged workspace. Actual outgoing instructions and file placement now pass. All focused gateway acceptance cases and system-tool membership cases pass; picker tests pass. Seed, reroot and diagnostic mutations were caught by assertion failures; restored checks pass. Initial compilation failures were corrected and never counted as caught defects. The Grok worker delivered the Admin chat-selection follow-up; initial parent hook/picker tests pass, and its mutation/regression checks are in progress.
- **Checks:** full generated-contract package previously passed all 516 tests, including portable-path controls. Current file/function budget checks pass. The earlier frontend build passed but predates the latest picker changes; fresh build and rebuilt-binary browser acceptance remain due.
- **Remaining gates:** finish and integrate the seven implementation lanes; correct every independent review finding; run affected local regressions and meaningful negative controls; complete independent final/delta reviews, real browser and agent/document workflow acceptance, and supported-platform evidence. Managed document runtime setup is now in progress in the isolated browser profile: portable Python, pinned document packages and Node are installed; LibreOffice signature/version verification remains pending. No worker readiness or rendered-document acceptance is inferred from installation. No CI, push, PR or release has run.

## Historical local checkpoint — 2026-09-18

- Local checkpoints: `168cd39fc` contains roster cleanup, approved knowledge/delegation defaults, removal of Ava session gates and actual configuration-write tests. `409cb5b80` contains the portable contract harness. All 516 generated-contract tests passed on the parent (8.946 seconds, no skips); third path-separator mutation caught. Worker Windows vet and test-binary cross-compilation passed; Windows execution remains unverified.
- Runtime manifest acceptance: all four agent tests passed (4.081 seconds), including nine-role contexts, deferred knowledge discovery and denied tools. Both gateway cases fail specifically on Admin membership, confirming the open operator fix. Additional source review found compressed manifests force-add ToolSearch after policy filtering removes it; a dedicated fixer now owns stricter-global/per-role Deny checks.
- A transient DNS failure terminated six GLM sessions. DNS recovered and the same sessions were resumed, preserving work. Reader checkout's temporary pixel-limit mutation was explicitly flagged for restoration before integration.
- Capability-lint follow-up is being tightened to resolve actual fresh runtime policies and keep internal lint markers optional for authored/external skills. Its intermediate claimed inventory-based policy check is not the final accepted gate.

- Parent found a concrete Admin integration gap: defaultWorkspaceTeam includes Admin through All(), while resolveTurnWorkDirOrRefuse rejects a nonmember Admin because only hidden System Agents have an exception. This contradicts the agreed standalone chat-capable operator role. A separate GLM fixer owns default/team-write filtering, safe standalone turn admission and dedicated regression tests; no ordinary-agent membership bypass is authorized. This remains open until integrated runtime tests prove it.
- All six knowledge/delegation mutations were caught by assertion failures (no compilation failures), and both restored core-agent and actual delegation suites passed. The three additional independent reviewers have now launched from the stable snapshot.

- Current frontend build (`npm run build`, TypeScript plus Vite) passed, exit 0; the bundle has been synchronized into the embedded SPA directory. This is build evidence, not rebuilt-binary/browser acceptance.
- Additional Ava agent/workspace worker delivered 29 persistence/guard cases. Parent review corrected the fresh-workspace delegation-store existence assumption and valid-team-member fixtures; integration is complete and all 29 cases passed (4.299 seconds); all three semantic mutations were caught and the restored tests passed.

- Prometheus bounded implementation completed with verified tool-name lint, complete registered browser-control guidance, four-tool Supervisor rubric and Judge grep guidance. Its dedicated Judge family rerun executed all 12 cases after catching a vacuous earlier test pattern. The Judge/Supervisor rubric changes are integrated; other prompt/skill changes await the capability-lint follow-up. A fresh GLM implementation session now owns the remaining invocation-vs-grant capability lint and latest knowledge/delegated-Ava guidance; this is not the independent final review.

- User authorized additional GLM 5.3 CLI workers beyond the built-in subagent concurrency limit. Two additional implementation workers are confirmed running with actual model `glm-5.3`: delegated Ava agent/workspace persistence tests, and actual normal/compressed tool-manifest coverage. Their ownership is limited to dedicated new test files; parent owns integration and test execution.
- Three additional read-only review sessions are scheduled from a stable snapshot: security/error handling, types/comments/simplification, and full specification/test/code audit through grill-code. Snapshot creation waits on the shared test lock to avoid capturing temporarily mutated production code; do not count these reviews as completed or clean.
- The knowledge/delegation combined focused run passed after Jim write Ask, Admin read Allow and Jim-to-Ava delegation changes. All six mutations produced the intended assertion failures, and the restored core and agent tests passed.
- Ava-specific session restrictions have been removed from all agent, workspace and skill mutation entry points. Actual skill create/edit/remove tests pass across attended, delegated, unattended and missing-session contexts (8.280 seconds), and actual delegated installation passes (12.816 seconds). All three reintroduced-restriction mutations were caught and the restored skill tests passed (2.515 seconds). Agent/workspace context persistence coverage is assigned above.
- Ava/Jim prompts and authoring skills now support actual user confirmation relayed through Jim and execution in Ava's current delegated run. Historical Opus M-3 documents explicitly mark their former owner-session-only resolution as superseded. Final Prometheus recheck must include these latest changes after integrating the active prompt-lint fixer.

## Active integration checkpoint — GLM parallel recovery

Current committed parent: `f0d040833`. This entry supersedes worker-activity and unresolved-fix statements in older checkpoints below; completion remains unproven.

- Latest founder decision supersedes the former Ava session restriction: remove the hardcoded attended/user-owned/nondelegated write gate. One combined conversational proposal and actual user confirmation remain prompt/skill requirements; Jim can relay the answer and Ava can apply in the delegated run. Normal tool permissions, protected fields and revision checks remain. Actual create/edit/remove tests reproduced all nine forbidden-context failures before removal; all twelve write contexts plus delegated installation now pass. Mutation and broader agent/team coverage remain in progress.
- The founder explicitly switched parallel work to `claudez` with `glm-5.3`. Three CLI workers are active in separate worktrees: image-reader boundary coverage/fixes, Windows contract-test portability, and an independent Prometheus review of changed prompts, skills and descriptions. Their initialization and assistant response events report `glm-5.3`. Sol quota exhaustion is no longer preventing parallel implementation, although the native Codex review gate still requires completion.
- Gateway revision fixtures and omission-preserve/workspace partial-state assertions were integrated in `b79704e05`. Valid MCP policy overrides were corrected in `37f56048a`. Read-only `updated_at` rejection and unconditional unsupported-field rejection were corrected in `0f9ef18b6` and `fa08814a0`; each production correction has three caught/restored semantic mutations and focused green evidence.
- The latest combined affected gateway run failed five top-level tests. Inspection identified obsolete/malformed positive fixtures (unconfigured MCP server, retired timeout/default-policy keys, an explicitly empty revision, and duplicate core-agent entity seeding). All five corrected top-level cases passed in the focused rerun (exit 0), committed as `1a527d20d`. The subsequent combined affected configuration run passed 172 top-level tests in 83.222 seconds (exit 0); this is not the full gateway package. Evidence: /Users/danielpiatkowski/Documents/Agent-Workspace/omnipus/worktrees/release-20260917/.local/adr090/gateway-integrated-current.log.
- Frontend budget refactoring is integrated in `4b8b3610e`: 203 affected tests, TypeScript compilation, targeted lint, and file/function budget checks passed. Backend budget refactoring is integrated in `7249f0025` with focused reader verification after integration.
- Image-reader JPEG/configured-byte/pagination coverage is integrated in `cf2ccd9a3`. Actual-turn private image coverage is integrated in `52a7c2e87`. Additional visual tests recovered from the stopped worker now exercise forced context-window trimming, durable session reload, interruption, unknown-model attempts, provider rejection and timeout fallback; three mutations were caught and the restored focused run passed in 6.485 seconds. These additional tests are integrated in `517917d8f`; independent re-review remains open.
- Roster constructor/dead-policy cleanup passed 75 focused core-agent tests and is integrated but not yet committed; agent caller regressions are queued. The founder clarified knowledge defaults: Jim also needs writes, and Admin needs reads. The ADR/spec now records read Allow for all seven ordinary built-ins, write Ask for Mia/Jim/General Purpose, and denied writes for the other roles; hidden agents remain denied. Production policy and runtime tests are being updated.
- TEST004 current-source correction: Codex and Azure already had HTTP capture tests, contrary to the original review's absence claim. Their identical-image and permissive-output assertions admitted image substitution, missing text, and wrong image detail. `4db4a3508` replaces them with distinct PNG fixtures and exact ordered request shapes, decoded dimensions/pixels, and byte identity. Both adapters pass after restoration; all three mutations fail the new tests and pass the old tests. Evidence: /Users/danielpiatkowski/Documents/Agent-Workspace/omnipus/worktrees/release-20260917/.local/adr090/responses-transport-mutations.json. Independent final test review remains required.
- Fresh GLM Prometheus review at `fa08814a0` confirms PROM-090-001 through 010 resolved. It reports further browser guidance, hidden-rubric consistency and FR013 catalog-lint gaps; a fresh GLM implementation session owns those fixes. Parent owns stale metadata/comment cleanup. The PDF wording finding requires reconsideration because its existing dependency paragraph already prohibits silent model switching. Review source: /Users/danielpiatkowski/Documents/Agent-Workspace/omnipus/worktrees/claudez-prometheus/.local/adr090/result.md. No final clean Prometheus verdict is claimed.
- Remaining gates include integrated regression checks, reader/provider transport gaps, roster and manifest verification, final independent reviewer lenses, final Prometheus findings/fixes, rebuilt browser acceptance, and actual agent/document workflows. No CI, push, PR or release has run.

## Latest checkpoint: integrated review fixes

Current code snapshot: `281b55712` (2026-09-17). This checkpoint supersedes outstanding-work statements in the historical entries below; the full feature remains in progress.

- Integrated canonical fallback configuration (`09b2b8b5f`), safe agent-save errors (`5b9eb0926`), authorized document finalizer reads (`0f5c12e81`), management skill inventory (`7f21eca43`), upload client (`527fa9841`), corrected SVG/tool descriptions (`52b158de7`), bounded ZIP extraction (`9d24e3700`), truthful skill publication outcomes (`b55bbf62c`), and composite agent deletion (`f94d14d3f`).
- The full native `codex review --base 12d97e7e70a869b72a246c9385776e71e9bdaa22` completed on snapshot `7f21eca43`. It found archive bounds and SVG description issues, subsequently corrected, and confirmed then-open deletion/publication issues. Completion of the review command does not mean a clean verdict. Report: /Users/danielpiatkowski/Documents/Agent-Workspace/omnipus/worktrees/adr090-native-review/.local/adr090/native-review-assessment.md.
- Combined full system-tool, tool, document-runtime, agent-mutation and agent-store packages passed before the latest archive/publication/deletion changes. Focused agent tests and the frontend build also passed. The integrated full skills, utils, agent-store and tools suites passed; focused gateway deletion/list/install checks passed. System-tools found a reverted rejection-message string during integration; the string was restored and the focused regression passed, with the full system-tools rerun pending.
- Fresh security review on `f94d14d3f` is complete with no actionable findings after confirming the explicitly prompt-governed confirmation design. Prometheus resolved its first six findings and found two remaining install/delete descriptions; corrections are awaiting its independent recheck. The silent-failure lens found skill restore and workspace split-store outcome gaps; those and the deletion UI remain open. Remaining independent reviewer lenses and final native delta review are still required.
- A new integration finding remains open: the deletion client ignores the returned mutation state and can show success after failed runtime activation. A separate Sol fixer owns the client and regression tests.
- Real browser verification has started against an isolated local profile and the built application. It has no provider credentials, so this cannot prove live agent or document/model acceptance. Actual workflow and supported-platform acceptance remain open. No CI, push or PR has run.

## Browser and specialist review checkpoint

- Security review on `f94d14d3f` has no actionable findings. Its initially proposed confirmation token was withdrawn because FR-007 explicitly chooses prompt-governed confirmation and forbids that mechanism.
- Silent-failure findings SF-090-001 (skill publication plus failed restoration), SF-090-002 (workspace entity/graph partial saves), and SF-090-003 (deletion UI discarding mutation state) were assigned separate Sol fixers. SF-090-002 is integrated in 265b0307c; all remain open until final verification and independent recheck.
- Type review found retired role constructors/IDs, unrestricted skill outcome states, and redundant inspection-media fields. The skill outcome type is assigned with SF-090-001; the other two remain open.
- Prometheus tool-description findings are corrected in `7366b92fb` and independently rechecked. Browser-discovered roster/profile wording and seeded Ava/Planner descriptions are being corrected; final content recheck remains pending.
- The isolated built app accepted a real browser login and rendered the roster and Mia profile. Browser inspection then exposed a functional mismatch: the Tools editor asked for `tools_cfg`, while the backend advertises `tool_policy_changes`. The corrected UI passes four focused descriptor cases and 193 tests across the affected frontend files. Three semantic mutations were caught and restored. The SPA build passes; rebuilt-binary browser evidence is pending. The earlier broad fixture used the wrong field name too, so it did not prove actual editability.
- Full system-tools rerun passed (66.716 seconds). Full coreagent suite passed after metadata corrections (4.389 seconds). A local contract guard reproduced the handwritten field-descriptor response; the handler now uses the generated descriptor type and the guard passes. Full contract generation and TypeScript compilation pass; generated descriptor cleanup is committed as ee3679c15. Subsequent affected gateway tests compile but are red: 69 top-level failures among the 95 test functions selected by Test(GetAgent|ListAgents|UpdateAgent). At least 45 failures include missing-revision refusals; the remainder require fixture-versus-implementation diagnosis. This is an open regression gate, not a passing gateway suite. Evidence: /Users/danielpiatkowski/Documents/Agent-Workspace/omnipus/worktrees/release-20260917/.local/adr090/gateway-configuration-regressions-red.log (exit 1).

## Current verification follow-up

- Skill failed-restoration outcomes and typed persistence/activation states are integrated in `9c381c638`. Client support for the failure envelope without a live revision is integrated in `539b49bd9`. All 32 tests pass; all three deliberate mutations were caught and restored, and lint plus full contract generation/typecheck passed. Final independent end-to-end recheck remains open.
- The rebuilt application now passes real browser permission editing: Mia's `search_web` setting was changed to `ask`, and a fresh authenticated API read confirmed the saved policy and override. Evidence: /Users/danielpiatkowski/Documents/Agent-Workspace/omnipus/worktrees/release-20260917/.local/adr090/browser-permission-save-proof.json. This supersedes earlier pending-browser statements.
- Deletion UI fixes are complete on the worker branch (`127a5c4d5`, `93d4e81dd`), with 164 affected tests passing, three mutations caught/restored, typecheck and targeted lint passing. Integrated as `4e42b38e0` and `281b55712`; combined parent frontend verification passed all 172 cases across four files; parent typecheck passed. The permission-descriptor tests are being split into a dedicated file to address the remaining profile test-file budget overrun.
- Independent test-quality review found missing actual-turn image privacy/retry coverage, incomplete reader boundary and provider request datasets, omission-preserve and exact workspace partial-state assertions, incomplete exact roster coverage, and an unnecessary-looking Windows test exclusion that still needs investigation. The actual-turn/retry and gateway cases have active owners. Report: /Users/danielpiatkowski/Documents/Agent-Workspace/omnipus/worktrees/release-20260917/.local/adr090/test-quality-final-review.md.
- A Sol worker is repairing the 69 observed gateway regression failures without weakening revision validation. Its corrected regression exposed a real production gap: a valid revision plus the retired updated_at field is accepted instead of rejected with INVALID_INPUT. Production correction is queued after the active update-handler refactor. Another is refactoring the three oversized backend functions without changing behavior or raising guard thresholds. Frontend budget findings and remaining independent reviewer contexts are still open.

## Scope and ownership

| Work package | Requirements | Status |
|---|---|---|
| Public contracts and generated clients | Configuration FR-002/003/007/012 | Upload client integrated; generated field-descriptor cleanup and final generation underway |
| Agent persistence and management | Configuration FR-001–005/007/012 agent portions | Integrated through composite deletion; affected local packages pass; deletion UI correction pending integration |
| Visual reader and private lifecycle | Visual FR-001–015 reader/lifecycle portions | Integrated; focused live-image lifecycle tests pass; real model acceptance remains pending |
| Roster, effective policies and global visibility | Configuration FR-001/008/009 | Integrated; full coreagent and role mutation checks pass; retired exported identity cleanup remains |
| Role prompts and packaged procedures | Configuration FR-009/013 | Original tool findings corrected and rechecked; seeded metadata/UI wording corrected in 189f833f1, final content recheck pending |
| Workspace graph, skill mutation and Ava routing | Configuration FR-006/007 | Workspace partial-state correction integrated as 265b0307c; skill failed-restoration backend integrated as 9c381c638, client validation underway |
| Connector assignment at discovery and dispatch | Configuration FR-003/004/005 | Integrated; combined focused agent tests pass; four earlier mutations caught and restored; review pending |
| Settings and all configuration callers | Configuration FR-002/003/007 | Settings/upload integrated; real browser exposed descriptor mismatch, fixed in 189f833f1 with 193 affected tests passing and three mutations caught; rebuilt browser permission save and fresh readback pass |
| Provider image request matrix and candidate checks | Visual provider requirements and BDD-10/11/12 | Provider and transport commits integrated; local provider suites passed; live-model/document acceptance pending |
| Elicify document packages and actual runtime setup | Configuration FR-010/011; visual document acceptance | Packages, runtime and application wiring integrated; combined runtime tests pass; actual four-format/two-role visual workflow and platform certification pending |
| Local end-to-end verification | All BDD and acceptance requirements | Pending final integrated execution |
| Full code review and seven reviewer lenses | Complete implementation diff | Holistic and full native review complete; security, silent-failure and type reviews complete with follow-up fixes; test-quality review complete with open findings; remaining independent lenses and final delta review pending |
| Prometheus review | All changed prompts, tool descriptions and skills | Initial and final review performed; original tool guidance corrected/rechecked; latest metadata and UI wording awaits recheck |

The requirements and BDD identifiers refer to the accepted companion specifications. This ledger does not replace them or mark omitted requirements complete. The advanced document-skill maturity/competitive campaign remains deferred under Elicify Skills #2 and Omnipus #730; baseline runtime document integration remains required here.

## Review sources and evidence discipline

Prometheus source: /Users/danielpiatkowski/Documents/Agent-Workspace/omnipus/worktrees/release-20260917/.claude/agents/prometheus-prompt-engineer.md. The named agent definition is used in spec-driven review mode, with Sol per the founder's model instruction. Skill review follows its skill-creator branch.

The installed `codex review` command supplies the full code-review pass; the repository's seven-reviewer gate adds correctness/conventions, simplification, comments, tests, silent failures, type design, and security. Reviewers run independently of the author of the reviewed slice. Fixes use separate ownership and are re-reviewed.

Record exact tree/commit, commands, collected tests, failures, mutation checks and runtime evidence before marking any row complete. A helper test, generated schema or prompt snapshot alone is not evidence that a user or agent can reach the feature. No CI result is claimed for this implementation.

## Current integration checkpoint, 2026-09-17

All completed workspace, skill mutation, document runtime/wiring and Settings packages are integrated through `5940635c4`. Commit `32bca9cfe` synchronizes generated artifacts and updates repository runtime guidance. Earlier entries below are historical checkpoints, not the current outstanding-work list.

- Full `scripts/gen-contracts.sh` completed with exit 0, including TypeScript compilation. Its generated differences were reviewed and committed.
- Combined focused agent check completed with exit 0 (21.758 seconds), covering MCP assignment, current dispatch, private image attachment, compressed tool availability, document runtime wiring and General Purpose self-delegation checks.
- Combined backend run passed coreagent, agentmutation, workspace, skills and documentruntime. Config failed on seven unrelated workspace-record selectors in its agent-home rename guard; reviewed exact exceptions were added and the focused guard passed. System tools exposed an outdated global connector-denial expectation and two cascade-delete callers missing revisions; corrections are being tested. This is not a full backend green claim.
- Prometheus report: /Users/danielpiatkowski/Documents/Agent-Workspace/omnipus/worktrees/adr090-review/.local/adr090/prometheus-review.md. Six guidance findings are assigned to a Sol fixer. PROM-090-005 requires the missing authorized management scope/content view for list_skills; it remains open.
- Holistic code review and local upload-client integration are running independently. Native full review, remaining reviewer lenses, final browser checks and live document acceptance are still required. CI has not run.

## Historical integration evidence, 2026-09-17

- Shared contracts: local commits `54635e24e` and `7b9209472`; generated API package test exits zero. Agent and workspace contract mutation checks caught three deliberate defects each. Full generator is not green yet: TypeScript callers and fixtures still lack required revision fields.
- Visual slice integrated as `67da3a886`; roles slice as `2e7f18513`. These are implementation checkpoints, not completion claims. Parent review identified configured byte-budget, sniff accounting and actual resized-dimension issues; visual worker is correcting them with provider integration.
- Client save-state gate: real updateAgent HTTP wrapper tests cover complete/active, saved/inactive, partial/none, missing/malformed envelopes and conflict non-retry. Six original cases failed before implementation; 16 current cases pass. An initial persistence mutation survived because tests lacked contradictory partial+active state; those cases were added and all three mutation categories were caught on rerun. Final restored check and broader caller integration remain separate gates.
- Elicify package inventory pins source `f5c241825045341b9b71a9c1f369c24ba6de4f9c` and every copied asset hash. Source bytes are unchanged apart from added license notices. Public distribution terms remain unresolved; nothing has been pushed or published. Worker runtime readiness is explicitly pending in the inventory.

- Agent management checkpoint integrated as `2e50d5e44`; its worker is finishing create staging/aliases, storage envelopes and affected legacy tests. This checkpoint is not a full-suite pass.
- Document package seeding: full `pkg/skills` suite passed after adding the four approved names to the exact roster; three asset changes (helper, knowledge file, license) were detected against pinned hashes, restored, and the focused seeding check passed again. No agent execution/runtime readiness is inferred from this package check.

- Client mutation transport integrated as `51b6f9a81`: delete helpers require the reviewed revision; graph updates take the generated request; tool updates carry override intent. Agent/workspace creation and updates, graph/tools updates and skill installation all check persistence plus activation before resolving successfully. The two real-fetch test files pass 33 cases; three transport mutations were caught and restored; changed client files pass ESLint. Full frontend typecheck remains red until Settings callers and fixtures are migrated.
- Next Settings worker checkout prepared at /Users/danielpiatkowski/Documents/Agent-Workspace/omnipus/worktrees/adr090-settings. The Sol Settings worker is now running there.

- Ready for integration: agent mutation completion `7997df189`, role-suite repair `1a11ef3e4`, visual provider integration `513e54663`, transport capture tests `4a2775dd5`. These remain worker-branch evidence until integrated and checked together. Role worker reports full local coreagent/tools/config/skills suites green; visual worker reports scoped providers, Bedrock tagged transport, and isolated owner-wake tests green.
- Workspace worker now owns /Users/danielpiatkowski/Documents/Agent-Workspace/omnipus/worktrees/adr090-workspaces; document runtime worker owns /Users/danielpiatkowski/Documents/Agent-Workspace/omnipus/worktrees/adr090-documents. Runtime prefix/sandbox/probe implementation is underway; actual worker execution and four-format acceptance remain unproven.
- Connector tests reproduced unassigned access, stale retained-agent authority after unbinding, and unbinding during policy approval followed by dispatch. Assignment filtering, immutable assignment snapshots, current-registry lookup and a final pre-dispatch check now pass the focused local agent/tools tests. Wildcard-only assigned-server selection is preserved. All four deliberate mutations failed through the expected assertions (assignment filter, snapshot copying, current-registry lookup, post-approval dispatch guard); production was restored and the focused tests passed again.

- Integrated checkpoints now include connector enforcement `c43d43b59`, atomic agent completion `a9ae507fe`, role repairs `8e42374ed`, provider integration `e24691a96`, and transport captures `9ac64c19c`. Combined local tests passed for coreagent, agentmutation, agentstore, config, skills, every default-build provider package and generated API. The tools suite initially found three stale fixtures: explicit connector assignment in a precedence test, the approved 33→35 management catalog count, and the two new readback-tool snapshot entries. After those corrections, the full tools suite passed (113.742 seconds). This does not claim tagged-provider or full-feature end-to-end completion.
- Ava skill authoring/removal guard integrated as `09f2431e4`. Twelve real-file cases cover attended success and zero-write rejection for delegated, unattended and missing-session Ava across create/edit/remove. Each guard was independently removed, caught by the assertions and restored; affected skill-tool tests passed again. Skill revision/replace/install and publication parity remain pending.

- Integration corrections `9407be077`: complete local config and system-tools package rerun passed (42.544 and 94.105 seconds). Cascade deletion tests again execute deletion with reviewed revisions. The Admin connector seed test now verifies the ADR-090 allow ceiling, denial for every other seeded role, and preservation of operator global Deny.
- Full tools package remains red on the document finalizer's direct filesystem read. A Sol fixer owns the real authorization correction; the lint is not suppressed.
- Holistic report: /Users/danielpiatkowski/Documents/Agent-Workspace/omnipus/worktrees/adr090-review/.local/adr090/holistic-review.md. MAJ-006 fixed by the integration caller correction; MAJ-003 upload, MAJ-005 management inventory and MAJ-007 finalizer authorization are actively assigned. Agent entity+SOUL deletion, canonical fallback-model parity, skill cleanup failure reporting, sanitized storage errors and invalid skill revision placeholders remain open. Review-checkout missing frontend dependencies are an environment limitation; the integrated generator's TypeScript check passed in the installed parent checkout.
- Six additional role/readback mutations were all caught by their intended assertions (three management readback, three supporting role grants). Every mutation was restored; the final clean focused suite passed in 1.130 seconds and the isolated checkout is clean. Evidence: /Users/danielpiatkowski/Documents/Agent-Workspace/omnipus/worktrees/adr090-role-mutations/.local/adr090/role-mutations.json.

### Additional implementation delegation — 2026-09-18

Five additional claudez GLM 5.3 workers have been assigned isolated checkouts: Ava create/update capability completeness and inventory validation; workspace read-error classification and partial agent-delete reporting; fresh self-delegation depth limits; verification and correction of reachable silent media omissions; and stale roster/comment/test-name cleanup. Review reports are inputs to verify, not presumed defects. Existing Admin operator, manifest policy, prompt lint and reader/image work retain their ownership. Parent owns integration and serialized focused Go verification; these new assignments are **in progress, not tested or complete**. No CI, push or publication.

### Integration evidence — 2026-09-18 reader and Admin checks

- Reader boundary suite integrated with TYPE-090-003 removal of unused image-carrier metadata. Reader and agent inspection suites both passed locally; authoritative results: `.local/adr090/reader-type-integration-result.json` and corresponding logs. Actual source bytes, decoded dimensions and durable marker evidence remain tested. Reader worker restored all eight tested mutations before integration.
- Admin standalone membership/admission and team-picker patches integrated. Before-fix tests reproduced defects; picker passes after the fix. Initial backend run exposed two fixture assumptions (pre-existing workspace/delegation records) and a real context-injector mismatch: Admin's file root was correct, but the working-directory instruction injector still created/advertised a forged workspace. Follow-up correction/testing is queued; backend Admin slice is **not yet green**.
- Prompt/catalog/capability lint changes integrated; initial focused skill lint run passes. Mutation checks and restored runs remain in flight; do not snapshot or commit prompt/skill files until `.local/adr090/prometheus-integration-result.json` exists and the runner is terminal.
- Full frontend independent read-only grill-code review launched in an isolated snapshot. No frontend-wide clean review is claimed.

### Provider switch and restored prompt checks — 2026-09-18

- GLM workers reached a shared five-hour quota and returned terminal 429 errors. Partial work was preserved; it was not treated as completed implementation or review. Per the founder's explicit instruction, all nine implementation/review sessions resumed through `claudeg` with model `grok-4.6`; CLI initialization confirmed each original session id and the new model. Authoritative handles: `.local/adr090/grok-resumed-workers-20260918.json`.
- Prompt/catalog/capability checks: initial pass, three semantic mutations caught (wrong plan role, denied handoff operation, forbidden Researcher shell invocation), restored pass, core prompt checks pass. `.local/adr090/prometheus-integration-result.json` records all outcomes. Independent Prometheus review remains pending.
- Admin context-injector correction reproduced the mismatch before the change and passed afterwards, including actual outgoing instructions and actual file placement with a forged workspace record. `.local/adr090/admin-context-result.json` records the before/after. Gateway fixture checks initially hit a missing test import; import correction and the complete focused gateway acceptance run are queued, so do not claim that slice green from the earlier run.
