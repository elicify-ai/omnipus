# CI baseline — `feat/context-budget-and-tool-result-routing` — 2026-08-23

**Purpose:** record what CI says about this branch *before* any ADR-066/067/068 implementation lands, so later
failures can be told apart from failures that were already there. This is a measurement only — nothing was fixed.

**Measured commit:** `dc9bc96a` — "docs(plan): implementation plan for ADR-066/067/068 — waves, parallel streams,
review gates" (the branch HEAD at the time it was first pushed). Three docs-only commits (`ed449a2b`, `ce6459c9`,
`ac1d1180` — ADR-067/068/066 task breakdowns) were added to the worktree by a parallel session while the runs were in
flight; they touch only `docs/internal/specs/tasks/*.md` and were not part of either run.

Branch base: `origin/main` (`ad859cbe`) is an ancestor; the branch is ~1,900 commits ahead of `main` (it was cut from
the `release/v0.1.1` / `ci/enforcement-hardening` line).

## Pre-push authorship check

`git log origin/main..HEAD --format='%(trailers:key=Co-authored-by)' | grep -i anthropic` was **not empty**: it
printed seven `Co-Authored-By: Claude …<noreply@anthropic.com>` trailers. All of them belong to commits that were
**already on remote branches** before this push (e.g. `2e14cf25`, `03d27695`, `df25c111`, `45b01b14`, `b3d31878`,
`ba5d34f6`, `c7f60ef0`, `487c75c5` — reachable from `origin/release/v0.1.1` and siblings), i.e. they are inherited
history, not new commits. The check restricted to commits this push added (`git log HEAD --not --remotes`, 66 commits)
found **zero** anthropic trailers. The push went ahead on that basis; the inherited trailers are flagged here because the
CLA gate will still see them on any PR that carries this history to `main`. Not fixed (out of scope — history rewrite).

Push: `git push -u origin feat/context-budget-and-tool-result-routing` → `[new branch]`, tracking set.

## Surface 1 — GitHub Actions `pr.yml` (workflow_dispatch, no PR)

Run: https://github.com/elicify-ai/omnipus/actions/runs/32610977723 — **conclusion: failure**.

| Job | Result | Failing step |
|---|---|---|
| #615 CI Guard: browser real-Chrome test gating | GREEN | |
| #615: real-Chrome browser tests | GREEN | |
| CLI Removed-Verb Guard | GREEN | |
| E2E shard plan check | GREEN | |
| Tool-Error-From-Status Lint | GREEN | |
| Wire-Types Lint | GREEN | |
| ESLint (SPA) | RED | Install Node dependencies (`npm ci`) |
| TypeScript Type Check | RED | Install Node dependencies |
| Verify Contracts | RED | Install Node dependencies |
| Vitest × 6 shards (lib-store, components-chat/-misc/-workspaces/-agents-settings/-layout) | RED | Install Node dependencies |
| Tests | RED | Build SPA (`npm ci` inside) |
| Linter | RED | Build SPA |
| Security Check | RED | Build SPA |
| CGO_ENABLED=0 Build Gate | RED | Build SPA |
| E2E shard plan / E2E matrix / Perf Smoke / Security Tests | skipped (upstream dependency failed) | |

**Single root cause for every RED job** — `npm ci` exits 1 with `ERESOLVE` on the Node 24 runner:

```
npm error While resolving: eslint-plugin-jsx-a11y@6.10.2
npm error Found: eslint@10.8.1
npm error   dev eslint@"10.8.1" from the root project
npm error Could not resolve dependency:
npm error peer eslint@"^3 || ^4 || ^5 || ^6 || ^7 || ^8 || ^9" from eslint-plugin-jsx-a11y@6.10.2
npm error Conflicting peer dependency: eslint@9.39.5
##[error]Process completed with exit code 1.
```

`package.json` pins `eslint: 10.8.1` and `eslint-plugin-jsx-a11y: 6.10.2`; the `eslint` 10.8.1 pin was introduced by
`df25c111` (2026-08-20, "ci: enforcement guards — no baselines, no false greens", inherited from
`origin/release/v0.1.1`). `package-lock.json` does carry `node_modules/eslint` at 10.8.1, so this is a peer-dependency
rejection by `npm ci`, not a lock/manifest mismatch. **Consequence: GitHub produced no Go test, lint, vet, race,
contracts, typecheck, vitest, or e2e signal for this branch at all.** Those signals below come from the Fly worker only.

## Surface 2 — Fly worker `ci-omnipus`, `/cache/runci.sh feat/context-budget-and-tool-result-routing all`

Checkout confirmed: `HEAD: dc9bc96a docs(plan): implementation plan …` (stale-checkout trap checked). Exit codes are the
per-gate `<gate> -> exit N` lines printed by `runci.sh` itself (not the SSH wrapper's code). Final line:
`=== RESULT === GATE FAILURE(S) — see above`; wrapper exit 1.

| Gate | Result | Exit | Notes |
|---|---|---|---|
| cli-verb-guard | GREEN | 0 | |
| npm-ci | RED | 1 | same `ERESOLVE` as GitHub (eslint 10.8.1 vs eslint-plugin-jsx-a11y peer range) |
| gofmt | GREEN | 0 | |
| go-build | GREEN | 0 | |
| go-vet | GREEN | 0 | |
| golangci-lint | RED | 1 | 12 issues, all `gosec` G115 (see below) |
| verify-contracts | GREEN | 0 | |
| typecheck | GREEN | 0 | ran against the worker's cached `node_modules` — unverified whether that tree matches a clean install |
| vitest | GREEN | 0 | 428 files, 7007 passed, 2 expected-fail; same cached-`node_modules` caveat |
| go-test | RED | 1 | `REAL FAILURE (same test failed BOTH runs): pkg/config`; no `FLAKE (passed isolated)` lines in the log |
| go-race | RED | 1 | same `pkg/config` failure, failed twice; `DATA RACE` count in the log: 0 |
| e2e | RED | 1 | 8 of 9 shards PASS; `llm-conformance` FAIL (see below) |

### golangci-lint — first failing lines

```
pkg/sandbox/sandbox_linux.go:280:22: G115: integer overflow conversion uintptr -> int (gosec)
	defer unix.Close(int(rulesetFd))
pkg/sandbox/sandbox_linux.go:293:36: G115: integer overflow conversion uintptr -> int (gosec)
pkg/sandbox/sandbox_linux.go:317:40: G115: … (also :343, :537, :561, :582, :697, :731, :740, :759)
pkg/sandbox/seccomp_linux.go:334:15: G115: integer overflow conversion int -> uint8 (gosec)
			Jt:   uint8(remaining), … // #nosec G115 -- bounds-checked above …
12 issues: * gosec: 12
```

(The `seccomp_linux.go:334` line already carries a `#nosec G115` comment that the linter did not honour — unverified
whether this is a linter-version difference between the worker and GitHub; GitHub never reached its lint step.)

### go-test / go-race — first failing lines

```
--- FAIL: TestNoAgentConfigWorkspaceIdentifier (0.19s)
    rename_guard_test.go:137: reintroduced agent-config .Workspace usage (FR-001/FR-002 — rename the identifier to .Home,
    or add a reviewed file:line entry to allowedWorkspaceIdentifierLines …):
        pkg/migrate/sources/openclaw/openclaw_config.go:374: if c.Agents == nil || c.Agents.Defaults == nil || c.Agents.Defaults.Workspace == nil {
        pkg/migrate/sources/openclaw/openclaw_config.go:377: return rewriteWorkspacePath(*c.Agents.Defaults.Workspace)
        pkg/migrate/sources/openclaw/openclaw_config.go:416: cfg.Agents.Defaults.Workspace = c.GetDefaultWorkspace()
        pkg/migrate/sources/openclaw/openclaw_config.go:862: if entry.Workspace != nil {
        pkg/migrate/sources/openclaw/openclaw_config.go:863: agentCfg.Workspace = rewriteWorkspacePath(*entry.Workspace)
        pkg/migrate/sources/openclaw/openclaw_config.go:889: cfg.Agents.Defaults.Home = c.Agents.Defaults.Workspace
        pkg/migrate/sources/openclaw/openclaw_config.go:916: Home:    a.Workspace,
FAIL	github.com/elicify-ai/omnipus/pkg/config	1.440s
```

This is the only failing Go test on the branch (4 occurrences in the log = 2 gates × contended + isolated run).

### e2e — failing shard `llm-conformance` (spec `tests/e2e/conformance-design-e2e.spec.ts`)

```
First argument must use the object destructuring pattern: _fixtures
   at fixtures/plan-cleanup.ts:72
> 72 | export const test = base.extend<{
```

A Playwright fixture-definition error, not a product assertion: the spec never ran. All other shards
(llm-chat, llm-agents, llm-hot-reload, llm-verifier-eval, llm-light, ui, ui-heavy, stubs) PASS. Skip tracking: 0 skips.

## Summary for the implementation phase

Pre-existing RED before any implementation: (1) `npm ci` ERESOLVE — blocks every Node-dependent GitHub job and so hides
all Go/SPA signal upstream; (2) golangci-lint 12× gosec G115 in `pkg/sandbox/`; (3) `pkg/config`
`TestNoAgentConfigWorkspaceIdentifier` against `pkg/migrate/sources/openclaw/openclaw_config.go`; (4) e2e
`llm-conformance` fixture error in `tests/e2e/fixtures/plan-cleanup.ts:72`. Per Hard Constraint #7 these are ours to fix
before shipping; they are recorded here, not fixed here. Raw logs of this measurement are not committed (worker log
~10.9k lines; GitHub run is linked above).
