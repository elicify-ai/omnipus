# Capture frame and health publication

Approved contract: each capture publishes one versioned VideoHealthEvent stream. A new frame is transitioning until an accepted boundary; an initial ready boundary may be recovered. A boundary alone must preserve an existing lost/recovering/unrecoverable claim, including its attempts and detail. Every claim retains the original full frame and a fresh monotonic version. Current snapshot reads do not advance the version and cannot expose mutable saved audience slices. Uninitialized, stopped, invalidated, or exhausted claims cannot authorize publication.

Gateway publication uses exact registered capture pointer/capture ID and original attachment context. Full immutable frame metadata uses the latest-video slot with the original version validator retained through final writer admission. Registration replay targets the explicitly registered viewer independent of the old event's audience; its boolean is queue admission, not delivery or displayed-picture proof. Stopped-stream fan-out is similarly scoped to the original capture/context. Root owns Begin/Commit hooks and immutable encoder control; media owns the viewer-answer path.

Core tests leave CaptureSession/frame tracker/version claims real, using the existing fake relay at the transport edge. Cases: pending and measured transition; first accepted timestamp zero; each of three active failure states at boundary; new frame discards old failure; callback/current-snapshot audience mutation; absent/stopped/invalidated/exhausted snapshot. Fault probes target lost-state overwrite, retaining prior-frame failure, saved audience aliasing, and snapshot audience aliasing. Gateway cases will cover coherent wire fields, exact registry/capture/context filtering, delayed-version rejection, replay, and stopped-capture replacement. Existing redaction/optional-field checks remain relevant; no full CI run in this lane.

Pre-edit impacts: CaptureSession LOW4 symbols/1 indexed direct caller, onVideoHealth LOW0; videoHealthEventLocked UNKNOWN in the partial index. Manual source scope for the latter is the four health state claims plus root's forthcoming Begin/Commit claim calls. New helpers are unindexed. Callback delivery stays outside cs.mu.

## Core dependency verification

Observed red: retained run 17079 exited 1, with eight intended behavioral failures and four passing unavailable-state controls (12 leaves, 6.246 s). After implementation, retained run 16549 exited 0: all 12 leaves passed, no skips, 2.926 s. Both used `CGO_ENABLED=0 GOMAXPROCS=2 go test -tags goolm,stdjson -p 1 ./pkg/tools/browser -run '^TestCaptureFramePublication' -count=1 -v`.

This is a dependency handoff, not completed production integration. The parent owns actual BeginFrameTransition/CommitFrameBoundary claim/reset hooks; gateway publication/replay remains pending. By the agreed verification sequence, targeted fault injection and race checks will run with that real integration, avoiding a redundant isolated helper cycle. No full CI or production browser acceptance has been claimed.

Pre-commit detect_changes was attempted for the isolated worktree, which is not registered in GitNexus. Manual staged scope is exactly the five core source/test/evidence files; the parent will map the integrated commit against its indexed checkout.

Root integration cases use the actual BeginFrameTransition and CommitFrameBoundary entry points. They require one transition event for changed geometry, no extra event for an identical request, a newer complete ready event at the first boundary (including valid timestamp zero), observer invocation outside the capture lock, and immediate retirement of the prior frame's recovery episode and failure state. These tests are prepared before the production hooks; their red/green evidence is pending.

Actual hook evidence: run72381 reproduced both missing production hooks (no transition event; old recovery context remained live). Run18821 passed both actual-entry-point tests plus all12 publication-core leaves after wiring new-generation reset/claim and successful-boundary claim, with observer delivery outside the capture lock. The same browser selection passed all27 leaves in2.049s, including related recapture controls. The command exited1 only for a separately uncommitted gateway pending-geometry regression. The bounded publication fault/race closeout remains assigned to the UI integration lane; full CI is not claimed.

## Gateway publication integration

Observed red 37726 exited 1: eight intended behavioral failures and one passing foreign-origin control (nine leaves, 15.155 s). Existing fan-out queued critical unscoped health for foreign captures, mismatched capture IDs, canceled or absent attachment contexts, and obsolete versions. Latest-slot delivery and late-viewer replay were absent. The command selected only `^TestBrowserVideoPublication` in `./pkg/gateway` with the same Go environment/tags as above.

Implementation retains the original registry entry, capture identity, attachment context, and event version through latest-slot writer admission. The serializer includes the claimed generation, target, confirmed CSS dimensions, and a ready boundary marker including timestamp zero. Existing fan-out/redaction/optional-field tests are updated to the scoped delivery contract; per-viewer sessions remain independently distinct.

Additional prepared boundaries are stopped capture delivery (current, foreign capture, canceled origin, replaced registry entry before writer) and uninitialized frame rejection. Their red/green evidence is pending. Pre-edit stop impact is LOW with one direct caller/five symbols/one gateway process; the sole factory call's containing method is LOW with two direct callers/six symbols/one process. Existing affected tests are LOW0. New publication helpers are UNKNOWN in the partial index; manual callers are observer fan-out and registered-viewer replay.

### Integrated closeout

Run 68094 passed the original nine publication leaves and four existing health tests. Its five additional boundary cases produced four intended failures (foreign stopped capture, canceled origin, replaced registry before write, uninitialized health) and one passing current-stop control; package duration 7.926 s. Those failures were fixed with original capture/context stop scoping and explicit initialized-frame admission.

After actual frame hooks were integrated, run 8680 stopped before mutations: 16 fixture failures and three passing leaves, 4.250 s. Begin now correctly starts a recapture-settle window. The publication fixture was corrected to signal the registered legacy relay-live callback before the tested loss, keeping CaptureSession/recovery/frame behavior real. No production timer, guard, or expectation was bypassed. Original failed logs are retained with the before-live-fixture suffix.

Corrected driver 20556 exited 0. Initial affected gateway selection passed all 19 leaves, zero skips, 4.030 s. Three deliberate faults were each caught behaviorally: discarding an active failure at a new boundary, aliasing the retained audience, and removing final health-version authorization. Production files were restored after each fault. The final single shuffled race selection passed all 33 leaves (38 test/subtest records), zero failures, zero skips, zero race warnings: browser 3.268 s, gateway 7.625 s. This includes the actual Begin/Commit hooks, versioned snapshot helper, gateway publication/replay, and stopped-stream notification.

Commands used `CGO_ENABLED=0 GOMAXPROCS=2 go test -tags goolm,stdjson -p 1` for focused green/fault runs and `CGO_ENABLED=1 ... -race -shuffle=on` for the restored selection. Gateway selection: `^(TestBrowserVideoPublication|TestBrowserVideoStoppedPublication|TestOnVideoHealth_|TestWatchEncoderLiveness_StopsStaleSessionAndNotifiesAttachedViewer)`. Browser selection: `^TestCaptureFrame(Publication|TransitionPublishesActualBoundaries|TransitionRetiresOldRecovery)`. No full CI or actual-browser displayed-frame acceptance was run here. Replay returns enqueue admission only; media's production viewer handler must invoke it after exact registration and before its scoped answer.

Pre-commit worktree detect_changes was attempted again. The isolated checkout is unregistered, so the parent maps the integrated commit; manual scope is exactly seven gateway/source/test/evidence files and whitespace checks pass.
