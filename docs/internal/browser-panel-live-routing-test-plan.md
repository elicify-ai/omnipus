# Live panel capture routing

Contract: the approved browser-improvements plan preserves each chat panel's own page. Ordinary input requires that panel's committed picture; navigation and an already-owned release remain admissible. Resize/tab recapture and panel-local death must affect only that panel. BrowserManager shutdown/connection invalidation owns whole-browser cleanup.

Tests use the real LiveView, manager capture map, CaptureSession frame admission, and held-input tracking. Only browser command delivery and relay transport are fakes. Inputs use independent panel/target identities and explicit capture identities returned at fixture creation; no production guard is mocked.

Cases: own picture with a foreign operator capture (one command); foreign picture (zero); absent own capture with foreign capture (zero); no captures (zero); owned key release after capture removal (one matched release); navigation with no capture (one command); viewport/tab recapture (own relay once, foreign zero); absent panel recapture (both zero); dead panel (own relay closed once, foreign zero); live panel tab close (neither closed). Existing manager lifecycle cases prove all captures stop on shutdown/invalidation.

Fault probes restore singleton input lookup, restore each recapture lookup, restore singleton death lookup, and omit missing-capture rejection. Focused existing input fixtures will be updated to provide real panel capture proofs. This lane does not implement generation transitions, immutable encoder commands, or real browser acceptance.

Impact before edits: signalRecapture LOW (3 symbols, 1 direct caller), signalRecaptureForTabChange LOW (4 symbols, 2 direct callers), watchForUnexpectedDeath LOW (5 symbols, 2 direct callers), no indexed execution flows. dispatchInputContext was UNKNOWN in the partial index; direct source callers are Registry.InputContext and the LiveView dispatch wrapper. Existing edited fixture symbols were individually queried: indexed tests/helpers LOW, newer input tests UNKNOWN. Manual fallback scopes changes to picture setup/proof arguments and existing panel registration keys.

Observed red: retained command 24374 exited 1. Ten leaves ran; seven behavioral failures and three passing controls in 1.430s. Own-panel input was rejected by the foreign target check, missing-capture input dispatched once, both recapture helpers signaled only the foreign relay (present and missing panel), and panel death closed the foreign relay. Foreign-picture rejection and live-panel tab-end controls passed already.

Verification command 91262: focused affected selection passed 85 leaf cases (94 test/subtest records), zero skips, 9.709s. Five independent faults were caught: singleton input lookup, singleton viewport lookup, singleton tab lookup, singleton death lookup, and accepting a missing capture. Sources restored after every fault. Restored shuffled race passed 82 leaf cases, zero skips/race warnings; three existing fake-tab fixtures failed setup because the host's real memory-pressure probe refused to open fake tabs (7.040s package result). Pinning the existing healthy/measurable memory seam only in those two fixture locations makes their routing contract independent of host load. The production memory policy is unchanged. CloseActiveTab fixture impact LOW0; only those three failed cases require another race run.

Final fixture verification: command 1415 exited 0. Only the three corrected fake-tab cases were rerun with `CGO_ENABLED=1 GOMAXPROCS=2 go test -tags goolm,stdjson -p 1 ./pkg/tools/browser -run '^(TestSwitchTab_WithAViewportAppliedRecapturesOnceWithTheVerifiedSize|TestSwitchTab_OrdinarySwitchReassertsForegroundBeforeTheControlFrame|TestLiveView_CloseActiveTab_NoFalseDeathAndRebindsToSurvivor)$' -race -shuffle=on -count=1 -timeout=60s -json`; all three passed, zero skips/race warnings, 6.504s. Retained 82 passing race leaves plus these three cover the affected 85-case selection. No full package/CI run was performed for this bounded fix.

Before commit, detect-changes command 82829 failed because this isolated worktree is not registered with GitNexus. The approved fallback verified the exact 12-file staged scope and clean whitespace; parent will map the integrated diff. Production changes are only the four per-panel lookups and missing-capture admission in LiveView. Main and the running instance were untouched. Frame transitions and immutable health/encoder publication remain separate integration work.
