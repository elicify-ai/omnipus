name: Weekly Security
on:
  schedule: [{cron: '0 4 * * 1'}]   # Monday 04:00 UTC
  workflow_dispatch:
jobs:
  full-audit:
    runs-on: ubuntu-latest
    timeout-minutes: 30
    permissions:
      contents: read
      security-events: write  # upload grype SARIF
    env:
      CGO_ENABLED: "0"
    steps:
      - uses: actions/checkout@v6
      - uses: actions/setup-go@v6
        with:
          go-version-file: go.mod
      - uses: actions/setup-node@v6
        with:
          node-version: "24"
      - name: Build SPA
        run: |
          npm ci
          npm run build
          rm -rf pkg/gateway/spa
          cp -r dist/spa pkg/gateway/spa
      - name: Install govulncheck
        run: go install golang.org/x/vuln/cmd/govulncheck@latest
      - name: Run govulncheck (full)
        run: govulncheck -tags goolm,stdjson ./... | tee govulncheck.txt
      - name: Install grype
        run: |
          curl -sSfL https://raw.githubusercontent.com/anchore/grype/main/install.sh | sh -s -- -b "$(go env GOPATH)/bin"
      - name: Run grype (SARIF output)
        # --exclude flags duplicate .grype.yaml's exclude list defensively. The
        # config file isn't reliably picked up by every Grype version and the
        # CLI flags are documented as authoritative. node_modules in particular
        # MUST be excluded — esbuild bundles its own Go runtime and every Go
        # stdlib CVE then appears as a critical against a build-time tool.
        run: |
          "$(go env GOPATH)/bin/grype" dir:. -o sarif \
            --exclude './node_modules/**' \
            --exclude './dist/**' \
            --exclude './pkg/gateway/spa/**' \
            --exclude './test-results/**' \
            --exclude './tests/perf/results/**' \
            --exclude './pkg/api/generated/**' \
            --exclude './src/lib/api/generated/**' \
            --exclude './src/routeTree.gen.ts' \
            > grype.sarif
      - name: Upload SARIF
        if: always()
        uses: github/codeql-action/upload-sarif@v4
        with:
          sarif_file: grype.sarif
      - name: Run full security test suite
        run: CGO_ENABLED=0 go test -tags goolm,stdjson -v -count=1 -timeout 25m ./tests/security/...
      - name: Upload audit artifacts
        if: always()
        uses: actions/upload-artifact@v7
        with:
          name: weekly-security-audit
          path: |
            govulncheck.txt
            grype.sarif
          retention-days: 90
