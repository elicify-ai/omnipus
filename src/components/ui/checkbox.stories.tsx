import type { Meta, StoryObj } from '@storybook/react-vite'
import { expect, userEvent, within } from 'storybook/test'
import { Checkbox } from './checkbox'
const meta = { title:'Design System/Checkbox', component:Checkbox, args:{'aria-label':'Archive'}, parameters:{designSystem:{keyboard:[{trigger:'[role="checkbox"]',key:'Space',expectFocus:'[role="checkbox"]'}],pointerTargets:['[role="checkbox"]'],forcedColorTargets:['[role="checkbox"]'],forcedColors:{boundaries:['[role="checkbox"]'],differences:[{cue:'indicator',selector:'[role="checkbox"] svg',property:'color',againstSelector:'[role="checkbox"]',againstProperty:'backgroundColor',actualSystemColor:'HighlightText',againstSystemColor:'Highlight'}],focus:['[role="checkbox"]'],states:[{selector:'[role="checkbox"]',attribute:'data-state',value:'checked'}]},reflowExemptions:[],browserAssertions:[{selector:'[role="checkbox"]',attribute:'role',value:'checkbox'}]}}} satisfies Meta<typeof Checkbox>
export default meta; type Story=StoryObj<typeof meta>
export const Default:Story={play:async({canvasElement})=>{const c=within(canvasElement).getByRole('checkbox');await userEvent.click(c);await expect(c).toBeChecked()}}
export const Checked:Story={args:{defaultChecked:true}}; export const Disabled:Story={args:{disabled:true},play:async({canvasElement})=>{const control=within(canvasElement).getByRole('checkbox');await userEvent.click(control);await userEvent.keyboard('{Space}');await expect(control).toBeDisabled();await expect(control).not.toBeChecked()}}; export const Invalid:Story={args:{'aria-invalid':true},play:async({canvasElement})=>{await expect(within(canvasElement).getByRole('checkbox')).toHaveAttribute('aria-invalid','true')}}
