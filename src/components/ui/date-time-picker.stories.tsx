import { useState } from 'react';import type { Meta,StoryObj } from '@storybook/react-vite';import { expect,userEvent,within } from 'storybook/test';import { DateTimePicker } from './date-time-picker'
function Fixture(){const [value,setValue]=useState<Date|null>(null);return <DateTimePicker value={value} onChange={setValue} aria-label="Trigger at"/>}const meta={title:'Design System/DateTimePicker',component:DateTimePicker,args:{value:null,onChange:()=>{},'aria-label':'Trigger at'},parameters:{designSystem:{keyboard:[{trigger:'[aria-label="Trigger at"]',key:'Enter',expectExpanded:true}],pointerTargets:['[aria-label="Trigger at"]'],forcedColorTargets:['[aria-label="Trigger at"]'],forcedColors:{boundaries:['[aria-label="Trigger at"]'],differences:[{cue:'foreground',selector:'[aria-label="Trigger at"]',property:'color',againstSelector:'[aria-label="Trigger at"]',againstProperty:'backgroundColor'}],focus:['[aria-label="Trigger at"]'],states:[{selector:'[aria-label="Trigger at"]',attribute:'aria-expanded',value:'true'}]},reflowExemptions:[],browserAssertions:[{selector:'[aria-label="Trigger at"]',attribute:'type',value:'button'}]}}} satisfies Meta<typeof DateTimePicker>;export default meta;type Story=StoryObj<typeof meta>
export const Default:Story={render:()=> <Fixture/>,play:async({canvasElement})=>{const trigger=within(canvasElement).getByRole('button',{name:'Trigger at'});await userEvent.click(trigger);await expect(within(document.body).getByRole('button',{name:'Done'})).toBeDisabled()}};export const Closed:Story={render:()=> <Fixture/>};export const Disabled:Story={args:{value:null,onChange:()=>{},'aria-label':'Trigger at',disabled:true},play:async({canvasElement})=>{const trigger=within(canvasElement).getByRole('button',{name:'Trigger at'});await expect(userEvent.click(trigger)).rejects.toThrow(/pointer-events: none/);await expect(trigger).toBeDisabled();await expect(getComputedStyle(trigger).opacity).toBe('0.5');await expect(trigger).toHaveAttribute('aria-expanded','false')}};export const ReadOnly:Story={args:{value:new Date(2026,5,22,14,30),onChange:()=>{},'aria-label':'Trigger at',readOnly:true},play:async({canvasElement})=>{const trigger=within(canvasElement).getByRole('button',{name:'Trigger at'});trigger.focus();await userEvent.click(trigger);await expect(trigger).toHaveFocus();await expect(trigger).toHaveAttribute('aria-disabled','true');await expect(getComputedStyle(trigger).opacity).toBe('1');await expect(trigger).toHaveAttribute('aria-expanded','false')}}
