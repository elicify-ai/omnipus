import type { Meta,StoryObj } from '@storybook/react-vite';import { expect,userEvent,within } from 'storybook/test';import { Input } from './input';import { Label } from './label'
const meta={title:'Design System/Label',component:Label,parameters:{designSystem:{keyboard:[{trigger:'[data-testid="label-input"]',key:'a',expectFocus:'[data-testid="label-input"]'}],pointerTargets:['[data-testid="label-input"]'],forcedColorTargets:['label'],forcedColors:{differences:[{cue:'foreground',selector:'label',property:'color',againstSelector:'[data-design-system-config]',againstProperty:'backgroundColor'}]},reflowExemptions:[],browserAssertions:[{selector:'label',attribute:'for',value:'label-input'}]}}} satisfies Meta<typeof Label>;export default meta;type Story=StoryObj<typeof meta>
export const Default:Story={render:()=> <><Label htmlFor="label-input">Email</Label><Input id="label-input" data-testid="label-input" /></>,play:async({canvasElement})=>{const input=within(canvasElement).getByRole('textbox',{name:'Email'});await userEvent.type(input,'a');await expect(input).toHaveValue('a')}}
export const DisabledPeer:Story={render:()=> <><Label htmlFor="disabled-input">Email</Label><Input id="disabled-input" disabled /></>}
